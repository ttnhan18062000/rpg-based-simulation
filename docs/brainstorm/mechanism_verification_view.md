# Mechanism Verification View

Generated from `registries/mechanisms.yaml` — regenerate with `make mechanism-verification-view`. Do not hand-edit.

33 of 93 mechanisms have a recorded verdict. The remaining 60 are rendered explicitly as `unverified` below, not omitted — a mechanism with no verdict is not the same as a mechanism known to work.

**Static vs runtime evidence, grouped separately below**: `code_trace` proves what the code *says* (reachable, called, a field never written) and can never establish that reachable code has its claimed runtime effect. `census`/`scenario`/`corpus_run` prove what the simulation actually *does*. A `code_trace` row is not equivalent evidence to a runtime-confirmed row.

| Mechanism | Layer | State | Evidence | Instrument | Verdict | Date | Note |
|---|---|---|---|---|---|---|---|
| `action_pacing_readiness` | entity | done | runtime | scenario | observed | 2026-09-16 | Differential scenario (tests/mechanic_scenarios/test_action_pacing_readiness_gate.py): readiness=50.0 withholds a staged attack (INSUFFICIENT_READINESS), readiness=100.0 lets it proceed, through a real Kernel tick against a real compiled world. The gate itself -- what all transitive dependents actually need -- genuinely works. **Split 2026-09-17**: this entry previously also covered the separate agility-scaling recalculation under state `partial`; that half is now its own entry, `readiness_speed_scaling`, since the two halves diverge in real-world outcome (this gate confirmed working; the scaling half confirmed starved) and bundling them under one verdict buried the distinction in prose. This entry is now scoped to the gate only, `state: done` (no known deficiency in the gate itself). |
| `calamity_intensity` | world | done | runtime | corpus_run | contradicted | 2026-09-17 | `CalamityService.apply_calamity_consequences()` is real, non-dead code -- the sole real producer of `region.calamity_intensity` (+0.05 per hero death inside a `region.hazard_level > 0.5` region). A real, instrumented 5000-tick `Kernel.tick_once()` run against `frontier_living_world` found `calamity_intensity` never left `0.0` in any region for the entire run -- not "never crossed the spawn-gate threshold," never changed at all. Same family as `camp`: correct, wired code, defeated by real-world data never meeting its trigger precondition. See `TCK-20260914-CALAMITY-INTENSITY-PRODUCER-NEVER-FIRES` (still open -- the code-level finding stands regardless of whether/when a fix lands). |
| `combat_engagement` | entity | done | runtime | scenario | observed | 2026-09-16 | Corpus: posture gate moved attacks 1960 -> 837. Scenario: risk-rejected posture -> 0 attacks vs no posture -> attack proceeds, all else identical. **World-specificity, added 2026-09-17**: the "1960 -> 837" figure is the `metropolis` synthetic reference scenario specifically, the one world class where the decision-driven `ATTACK` path this gate governs actually fires. In `crowded_frontier`, `quest_dense_frontier`, and `hero_guild_routing`, that same path sees 0-2 real calls per 1000-2000 ticks regardless of this gate's state -- the gate has no measurable effect there, not because it's weak, but because the path it governs is already dormant (see `tactical_decision`). A verdict proves the mechanism works under the conditions measured; this one was never a general claim about the simulation, and is corrected here to say so explicitly rather than leaving "1960 -> 837" to be read as one. |
| `combat_resolution` | entity | done | runtime | corpus_run | observed | 2026-09-17 | Real reachability split by edge, not just a single "is this reached" answer. `CombatResolutionSystem.resolve_attack()` (reached via `tactical_decision` -> `ActionRouter.execute_action()`) had 0-2 real calls per 1000-2000 ticks across `crowded_frontier`/`quest_dense_frontier`/`hero_guild_routing`. `resolve_multi_attack()` (reached via `movement`'s own opportunity-attack mechanic, `src/engine/movement.py`, never through `tactical_decision`/`ActionRouter` at all) had 181-2177 real calls per 1000 ticks in the same worlds. Found while resolving TCK-20260915-CROSS-FACTION-COMBAT-RARITY-INVESTIGATION's own 7th candidate. **`depends_on: [tactical_decision]` removed 2026-09-17** (`TCK-20260917-MECHANISM-IDENTITY-RULES-AND-CHANGE-TAXONOMY`): checked against this registry's own stated definition (`registries/mechanisms.yaml`'s own header, "this mechanism cannot produce a meaningful result without that one already existing/having run") -- `resolve_attack()`/`resolve_multi_attack()` compute a real, meaningful outcome given any attacker/defender pairing regardless of how that pairing was decided; `combat_resolution` does not require `tactical_decision`'s own output to already exist. `tactical_decision` is a real *caller* of `combat_resolution` (execution order), not a functional prerequisite for it -- exactly the conflation the header's own closing line warns against. `movement` is kept: combat resolution reads position/range state that `movement`'s own domain establishes, a genuine functional dependency, even though `movement.py` is what calls `combat_resolution` in code (call direction and `depends_on` direction diverge here by design, not by error). See docs/plans/mechanism_identity_and_change_taxonomy.md §3 for the full reasoning. Multiple entry points into the same internal logic (this entry's own two-caller finding above) is not itself a split signal -- see that same doc's §1. |
| `evolution` | entity | done | runtime | scenario | observed | 2026-09-17 | Real Kernel-tick positive control (docs/plans/mechanic_verification_scenarios_proposal.md pattern): a goblin staged at evolution_points=95 (level 1) forced-attacks and kills an orc in one tick -- EvolutionSystem.evaluate() (src/engine/pipeline.py:381, unconditional, runs after resource_transactions) correctly consolidates the kill's 10 XP, crosses the 100-XP level-2 threshold, and produces evolution_level=2, evolution_points=5 (carry-over), exactly matching docs/mechanics/attribute_progression_contract.md's own worked example. A first attempt at this same control called ApplyPath._apply_entity_update() directly and wrongly read "no level-up" as a wiring defect -- it bypassed this exact phase, which only runs inside the real pipeline. Corrected before reporting; see TCK-20260916-DERIVED-COMBAT-STAT-RECALCULATION-UNOBSERVED-IN-CORPUS's own addendum for the full trace. This mechanism's own code is confirmed correct; see `xp_leveling` for why it still doesn't fire in real corpus play. |
| `readiness_speed_scaling` | entity | partial | runtime | scenario | contradicted | 2026-09-17 | Split out of `action_pacing_readiness` (see that entry's own note and docs/plans/mechanism_identity_and_change_taxonomy.md). Agility-derived `readiness_speed` has a real formula (TCK-20260831-READINESS-SPEED-FORMULA) but only applies via engine/apply.py's `stats_dirty` recalculation, never at spawn. Direct measurement (instrumented call-counting, 3 real corpus worlds, 1000 ticks each) found this path was never observed firing for any of 75 tested entities -- broader than the already-known COMB-318 gap. Root-caused 2026-09-17 (`TCK-20260916-DERIVED-COMBAT-STAT-RECALCULATION-UNOBSERVED-IN-CORPUS`): content- composition/pacing, not a wiring gap -- the combat-XP-to-level-up chain is directly confirmed correct and reachable (see `xp_leveling`'s own verified block), it just never accumulates enough real combat volume to cross a single level threshold within any tested 1000-tick corpus window. `verdict: contradicted` because the implicit "this scales with agility in play" claim does not hold in any tested corpus world, even though the formula and recalculation code are themselves correct. |
| `regional_trauma` | region | done | runtime | corpus_run | contradicted | 2026-09-17 | `region.trauma_score` accumulation/decay is real, wired code (src/world/consequences.py's own natural decay; combat/death events elsewhere accrue it; read by `camp.py`, `boss.py::check_for_boss_spawn()`, `creature_territory.py`, `threat.py`). But the Lair region specifically was directly measured to never accumulate real trauma across a real corpus run -- same shape as `camp`: correct, wired code, defeated by real-world data (no combat occurs in that region) never meeting the accumulation precondition. See `TCK-20260914-LAIR-REGION-TRAUMA-NEVER-ACCUMULATES` (still open). |
| `tactical_decision` | entity | done | runtime | corpus_run | contradicted | 2026-09-19 | The `ATTACK`-intent branch (the decision this mechanism exists to make) is effectively dormant in real corpus play: 0-2 real `CombatResolutionSystem.resolve_attack()` calls per 1000-2000 ticks across `crowded_frontier`, `quest_dense_frontier`, and `hero_guild_routing` -- independently corroborated three separate times, for three unrelated reasons, across six weeks (TCK-20260808-LIFE-ARC-REBIRTH-REACHABILITY- INVESTIGATION, 2026-08; TCK-20260915-CROSS-FACTION-COMBAT-RARITY-INVESTIGATION's own candidate 5, 2026-09-15). state stays `done` -- no evidence the decision logic itself is wrong, only that it essentially never selects ATTACK in these worlds' real play. **Root-caused 2026-09-19** (`TCK-20260917-TACTICAL-ATTACK-PATH-NEVER-FIRES-INVESTIGATION`, real `Kernel.tick_once()` instrumentation, `LocalSequentialExecutor` forced to avoid the real concurrency this arc's own prior addendum found unsafe for a shared-mutable-state probe): two compounding, independently-measured causes, not one. (1) The strategic layer essentially never assigns a `DEFEAT_ENEMY` objective in these worlds' sampled windows (0 occurrences across 3 corpus worlds, 2 sample points each) -- entities have nothing telling them to fight regardless of what's nearby. (2) Even when the brain does run -- itself suppressed 6-25x below the raw strategic-intelligence cadence ceiling by a three-layer sticky-task/cadence gate stack in `scheduler.py`/`cognition.py`, larger than the previously-cited 93.7% figure -- `hostiles` was found non-empty **0 of 1130** real calls across the three corpus worlds. On the rare occasion a fresh decision does happen (2 of 1130 calls, both worlds), it reliably dispatches -- one decision produced 2 and 29 real `execute_attack()` calls respectively via the same sticky-task re-execution mechanism, so there is no emission-vs-dispatch defect, only rarity of the initial decision. `metropolis`, the intended control, proved unreliable for this specific investigation: its own `build_metropolis_state()` perf-harness has real spawn collisions (confirmed via direct construction-time hard-law-violation logging) that make its entities' default task state unrepresentative of curated world content -- disclosed as a limitation, not chased to a separate root cause (out of this investigation's own scope). Whether the decision path firing more often is desirable is a **design** question for the user, unresolved here per this investigation's own Assumption #2 -- what this closes is the mechanism, not the disposition. See `combat_resolution` and `combat_engagement` for the rest of this same finding, and `stored_artifacts/TCK-20260917-TACTICAL-ATTACK-PATH-NEVER-FIRES-INVESTIGATION/` for the full per-world evidence table. |
| `xp_leveling` | entity | partial | runtime | scenario | observed | 2026-09-17 | Filed while resolving TCK-20260916-DERIVED-COMBAT-STAT-RECALCULATION-UNOBSERVED-IN-CORPUS's own discriminating measurement. The threshold-evaluation chain (kill -> ResourceTransferIntent.xp_reward -> conservation.py COMBAT branch -> IdentityUpdate.evolution_points_delta -> EvolutionSystem.evaluate() -> _execute_level_up()) is directly confirmed correct via a real Kernel-tick positive control (see `evolution`'s own verified block) -- state stays out of `gap`/`orphan` territory, the code is not broken. state moves done -> partial because real corpus play never actually crosses a threshold: instrumented across the same 3 real corpus worlds (crowded_frontier, quest_dense_frontier, hero_guild_routing; seed 42; 1000 ticks each), only 0-10 kills occur per world, each granting defender.identity.evolution_level*10 XP (10 XP for the overwhelmingly common level-1 monster kill) against a 100-XP level-2 threshold -- ~10 kills needed, the busiest single entity in any tested world accumulated 5 kills (50 XP). Same family as `camp` (docs/plans/world_composition_precondition_gap_finding.md): correct, wired code, defeated by real-world volume rather than a code defect. Likely downstream of the already-ticketed TCK-20260915-CROSS-FACTION-COMBAT-RARITY-INVESTIGATION (low cross-faction combat volume generally), not independently re-investigated here. Follow-up: TCK-20260917-XP-LEVEL-UP-THRESHOLD-VS-CORPUS-COMBAT-VOLUME (filed, not fixed). |
| `camp` | region | done | static | code_trace | contradicted | 2026-09-16 | CampState is real (spawns monsters, triggers raids, a real clearing-reward loop) but no compiled or procedurally-generated world seeds state.camps -- a permanent no-op in every world today. state stays done (the code is correct and wired, not defective) -- the missing thing is world data, not the mechanism itself. See docs/plans/world_composition_precondition_gap_finding.md; same family as the lair-trauma and calamity-intensity cases. |
| `causal_spatial_memory` | entity | gated | static | code_trace | observed | 2026-09-16 | Was mis-registered `orphan`; MemoryUpdatePhase.apply() is called live from engine/pipeline.py:154-157 via run_phase('memory_update', ..., 'ENABLE_MEMORY_UPDATE') -- a real caller exists, so `orphan` (zero callers) was factually wrong. The flag defaults OFF (feature_flags.py, TCK-20260824-CAUSAL-MEMORY-ROUTE-SCORING: 'wires MemoryUpdatePhase into refine() for the first time'), which is what `gated` means. Found while resolving implemented_by bindings (TCK-20260916-MECHANISM-IMPLEMENTED-BY-BINDING). |
| `city` | region | partial | static | code_trace | observed | 2026-09-19 | `-> regional_sovereignty` edge CONFIRMED KEEP (TCK-20260918-MECHANISM-UNCLASSIFIABLE- DEPENDS-ON-EDGES-RESOLUTION #12). Original grep for "class City" found nothing because a City is not its own class -- the wiring map's own explicit finding: "Already real as data, never named or separated as its own class today a City is a Region, not a place inside one." City-state IS `RegionState`, and `RegionState.owner_faction_id` is `regional_sovereignty`'s own core tracked field (`FactionInfluenceService`) -- the two mechanisms share the exact same underlying state object and field, as direct a functional coupling as this registry records anywhere. |
| `class_assignment` | entity | partial | static | code_trace | observed | 2026-09-19 | `-> race_archetype` edge CONFIRMED KEEP (TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON- EDGES-RESOLUTION #2): `src/content/resolver.py:257-258` -- `self._social.resolve_role_defaults(role_id) for role_id in species.compatible_roles` -- role/class resolution directly iterates the species' own declared `compatible_roles` list. `class_assignment` cannot resolve valid roles without `race_archetype`'s own `SpeciesDefinition.compatible_roles` field already existing, a genuine functional prerequisite, not just execution order. |
| `commitment_betrayal` | entity | done | static | code_trace | observed | 2026-09-19 | `-> combat_resolution` edge REMOVED (TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON- EDGES-RESOLUTION #7): confirmed `commitment_betrayal` has no distinct implementation of its own anywhere in `src/` -- every plausible piece of "betrayer" logic (the reputation label set in `src/domains/commitment/reputation.py:21`) is one of `commitment_pressure_ consequences`'s own already-bound files. Recorded as an identity/duplication question (two registry ids for what may be one real mechanism), not a depends_on question -- a depends_on edge cannot be confirmed against code that isn't independently attributable to this id. Flagged for a future identity-rules pass rather than forced into either verdict here; the reciprocal edge (`commitment_pressure_consequences -> commitment_betrayal`, #17 of the same ticket) carries the same flag on its own entry. |
| `country_lifecycle` | faction | partial | static | code_trace | observed | 2026-09-19 | `-> betrayal_siege_war` REMOVED (TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES- RESOLUTION #11). Correction to the parent audit's own claim ("no country_lifecycle implementation found anywhere"): real code exists -- `FactionDecisionPhase` (`src/engine/faction_decision.py`), confirmed real/live in the atlas's own investigation ("Country" and "Faction" are the same underlying class, wiring map's own Country Lifecycle row) -- the original grep for "CountryLifecycle"/"Country" class names missed it for the same reason `city`'s own search missed `RegionState`: conflation with an existing class name, not absence of code. `FactionDecisionPhase` itself never references siege/war/ betrayal concepts (grep confirmed empty), so the specific `-> betrayal_siege_war` edge has no real read evidence and is removed, distinct from a blanket "no code at all" claim. |
| `cross_episode_grief_nemesis` | faction | done | static | code_trace | observed | 2026-09-16 | Confirmed live, called from Campaign orchestrator (dead ally -> grief concern; repeated betrayal -> party-formation blocker); narrow trigger. |
| `cross_episode_social_consequences` | faction | done | static | code_trace | observed | 2026-09-16 | Was mis-registered `orphan`; evaluate_social_consequence() is called unconditionally (no feature-flag check, gated only on the scenario-recorder infra being present) from domains/campaigns/orchestrator.py at episode-entity-spawn time, so `orphan` (zero callers) was factually wrong. Found via TCK-20260916-MECHANISM-ORPHAN-STATE-BATCH-VERIFICATION. |
| `demographic_cohort_cycle` | region | done | static | code_trace | contradicted | 2026-09-16 | Was mis-registered `orphan`; DemographicCycleService.process_demographics has a real caller (engine/world_dynamics.py:178-179), so `orphan` (zero callers) was factually wrong. Same family as `camp`: correct, wired code defeated by absent data -- the caller is guarded by `if not region.population_cohorts` (worldbuilding/compiler.py:203), and no compiled or procedurally-generated world seeds population_cohorts, so the call is a permanent no-op in every world today. state stays `done` (the code is correct and wired, not defective) -- the missing thing is world data, not the mechanism itself. Found by TCK-20260916-MECHANISM-STATE-CALLER-MISMATCH-DETECTION's own first real run, corrected per peer review rather than left as an open taxonomy question. |
| `emotion` | entity | done | static | code_trace | observed | 2026-09-16 | Was mis-registered `orphan`; EmotionUpdateService.update_on_event() is called unconditionally (no feature-flag check) from engine/pipeline_phases/hardening.py's own NearDeathHardeningPhase, itself invoked via a plain run_phase("near_death_hardening", ...) call with no flag argument -- a real, live, unflagged caller, so `orphan` (zero callers) was factually wrong. Foundation's own investigation.md had already flagged this exact risk ("possibly stale, TCK-20260824 wired a near_death call site") without resolving it; this confirms the flag was correct. Found via TCK-20260916-MECHANISM-ORPHAN-STATE-BATCH- VERIFICATION. |
| `genetics_aptitude` | entity | gated | static | code_trace | observed | 2026-09-16 | Was mis-registered `orphan`; GeneticsSystem.generate_profile_from_seed()/combine_profiles() have a real caller (src/world/reproduction_humanoid.py, real genetic-profile combination for offspring), so `orphan` (zero callers) was factually wrong. That caller is itself gated behind ENABLE_REPRODUCTION_HUMANOID_PATH (default OFF, feature_flags.py), which is what `gated` means. Found via TCK-20260916-MECHANISM-ORPHAN-STATE-BATCH-VERIFICATION. |
| `goal_hierarchy` | entity | done | static | code_trace | observed | 2026-09-19 | Real implementation LOCATED while resolving TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS- ON-EDGES-RESOLUTION's goal_hierarchy cluster (#8-10) -- the parent edge audit's own grep for "goal hierarchy" and `graphify query` both failed because the wiring map's own definition ("Directive -> Project -> Objective -> Action") maps to `StrategicIntelligenceSystem`'s project/objective methods (`evaluate_project_switch`, `resume_project`, `process_project_outcome`, `_resolve_active_objective`, `evaluate_strategic_intent`, `src/systems/strategic_systems/intelligence.py`), never a literally-named "goal hierarchy" symbol. Not bound via `implemented_by` here -- this is one multi-concern 1782-line class also implementing `strategic_intelligence_core` and others via different methods, the same method-level binding gap already recorded on `action_pacing_readiness`/`skill_unlocks` (TCK-20260917-MECHANISM-IMPLEMENTED-BY-COVERAGE- EXTENSION). `-> reputation` REMOVED (#9): no read of any reputation field (`public_reputation`/`regional_reputation`/`clan_reputation`) or import of `ReputationService` anywhere in the strategic/goal-scoring subsystem (`intelligence.py`, `src/ai/goals/`, `src/domains/adventure/`). `-> belief_cycle` CONFIRMED KEEP (#8): the same strategic pass directly calls `BeliefCycleSystem.process_observation()`/ `apply_contradiction()` (`intelligence.py:436,448`) to confirm or contradict leads that feed the same objective-selection pipeline. |
| `information_trust_deception` | entity | gated | static | code_trace | observed | 2026-09-16 | Code read confirms the mechanism is correctly built; currently flag-gated off (see state). |
| `motivation_doctrine` | entity | gap | static | code_trace | observed | 2026-09-16 | Found while tracing dependency edges for TCK-20260916-MECHANISM-DEPENDENCY-GRAPH- POPULATION, not this ticket's own target. src/domains/motivation/__init__.py's own docstring: DoctrineResolver and MotivationBiasService were DELETED (TCK-20260908-DEAD-DOCTRINE-VALUES-CHAIN-RETIREMENT, 2026-09-08) -- resolver.py/service.py no longer exist, confirmed dead in production before removal (see docs/guidelines/intentional_divergences.md #2.53), superseded by adventure_routing's own live personality_bias mechanism. gap, not orphan: orphan means dead code still physically present with zero callers (this registry's own established usage, e.g. target_race); here the implementing code itself is gone, so "zero production callers" would be a false claim about code that doesn't exist -- gap is the value that survives becoming an executable assertion (claims-as-tests phase 2). Only evaluator.py (RoleFitEvaluator, an unrelated gear/skill/role-fit scorer) remains in the module. |
| `opportunity_rumor_seeds` | world | gated | static | code_trace | observed | 2026-09-16 | Code read confirms the mechanism is correctly built; currently flag-gated off (see state). |
| `progression_conversion` | entity | gated | static | code_trace | observed | 2026-09-17 | Confirmed gate is real and correctly documented (DEV-003/DEV-004, docs/guidelines/intentional_divergences.md) while investigating `xp_leveling`. Noted for whoever flips ENABLE_PROGRESSION_EVOLUTION on: its ALLOCATE_AP conversion branch spends unspent_ap, which combat-driven level-ups grant (+5 per level) -- since `xp_leveling` is itself corpus-pacing-starved (see that entry), unspent_ap from combat will be similarly scarce even once this gate opens, on top of the flag being off today. |
| `race_archetype` | entity | done | static | code_trace | observed | 2026-09-19 | Found while resolving TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES-RESOLUTION's #2/#16: the parent edge audit's own grep for "race"/"Race" class names came back empty because `TCK-20260904-EPIC-RACE-TO-SPECIES-TERMINOLOGY` (2026-09-04) renamed `RaceDefinition`/`race_id` -> `SpeciesDefinition`/`species_id` repo-wide, before that audit ran -- a terminology-drift search miss, not an absence of code. Real implementation: `SpeciesDefinition` (`src/content/schema.py:134`), resolved per-entity via `src/content/resolver.py`'s `resolve_living_defaults(species_id)`. |
| `regional_sovereignty` | region | done | static | code_trace | observed | 2026-09-17 | Corrected binding, caught by this split's own state-caller-mismatch re-check (`RegionalSovereigntyService` was the first-guessed binding and has zero real callers anywhere in `src/` -- see the flag below). The real, live ownership-tracking mechanism the atlas's own extensive investigation confirms ("the sovereignty mechanism described above is real and live exactly as documented") is `FactionInfluenceService` (`RegionState.owner_faction_id`, `+-5.0` influence shift per relevant death, conquest at `<= -50.0`/liberation at `>= +50.0`), with 3 real callers (`engine/world_dynamics.py`, `systems/lifecycle_systems/lifecycle.py`, `economy/vacancy.py`). **Flagged, not further split or ticketed here**: `src/world/regional_sovereignty.py::RegionalSovereigntyService` (regional taxation + macroscopic conquest debuffs -- `TAX_RATE_ENTITY`/`TAX_RATE_BUILDING`, `CONQUERED_ATK_DEF_MOD`/`CONQUERED_SPD_MOD`) is real, distinct, non-dead-code, and has ZERO real callers anywhere in `src/` -- a genuine `orphan` by this registry's own vocabulary, found as a side effect of this split rather than this ticket's own target. Not filed as its own ticket here to keep this identity-rules ticket's own scope bounded; worth its own follow-up investigation (same shape as `camp`/`motivation_doctrine`'s own orphan findings) for whoever next touches regional sovereignty. |
| `ruins_mines_battlefields` | region | partial | static | code_trace | observed | 2026-09-19 | Both edges REMOVED (TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES-RESOLUTION #13/#14). The parent audit's own low-confidence candidate, `create_battlefield_scar()` (`src/world/consequences.py:58-77`, `kind="BATTLE_FIELD"`), is confirmed a genuine name match but has ZERO callers anywhere in `src/` -- itself orphan, not a live producer -- and its own body reads neither `trauma_score` nor `owner_faction_id`, so no direct evidence supports either edge even if it were live. Corroborated independently by the wiring map's own verdict for this exact mechanism: "flattened into ordinary Region tags... Gap" -- ruins/ mines/battlefields are real in content (`haunted_battlefield`, `old_mine` tags) but not a separately-coded mechanism with its own dependency-worthy logic today. |
| `self_model` | entity | gated | static | code_trace | observed | 2026-09-16 | Code read confirms the mechanism is correctly built; currently flag-gated off (see state). |
| `status_effects` | entity | orphan | static | code_trace | observed | 2026-09-19 | state corrected partial -> orphan while binding `implemented_by` (TCK-20260917-MECHANISM-IMPLEMENTED-BY-COVERAGE-EXTENSION). The state model (`EntityState.combat.status_effects: List[StatusEffectState]`), the update schema (`StatusEffectUpdate`, `src/core/updates.py`), and the apply-path patch (`StatusEffectPatch`, `src/engine/patches.py`) all exist and are internally consistent -- but a repo-wide grep for `status_effect_update` (the only field that carries a `StatusEffectUpdate` into the apply pipeline) and for `StatusEffectUpdate(` / `effects_add=` finds zero constructions anywhere outside `patches.py`/`updates.py` themselves, and the only test referencing either symbol (`tests/unit/domains/optimization/test_component_patches.py`) exercises the patch/merge logic directly, not through any real game-logic caller. No system in `src/` ever adds a status effect during play. This registry's own established usage (see `motivation_doctrine`'s entry above) is orphan means dead code still physically present with zero callers, distinct from gap (code deleted) -- this is squarely the former: real, reachable, internally-tested code with no production producer. Consequence: the `ATTACKER_STATUS_BLOCKED` frozen/stunned gate read in `src/engine/legality.py` (checked while investigating `action_pacing_readiness`/`combat_resolution` bindings in this same pass) can never actually trigger in real play, since nothing ever populates the list it reads. Not independently re-investigated further here -- flagged as a real, corpus-relevant finding for a future ticket, same treatment as `camp`/`demographic_cohort_cycle`/ `succession`. **Added 2026-09-19, peer review**: this mechanism has 4 real transitive dependents (`combat_resolution` direct; `trauma`, `commitment_betrayal`, `commitment_pressure_consequences` transitive through `combat_resolution`/`trauma`) and ranks #4 by priority (20) with 4 transitive dependents of its own in `mechanism_registry_view.md` -- so the top of the "what to verify next" list currently contains a mechanism whose input never exists in production. `ATTACKER_STATUS_BLOCKED` is the one concrete dead-gate instance actually traced so far; whether the other 3 dependents (a) handle status-effect absence correctly (a real, if never-exercised, dependency), (b) are silently degraded (a branch that assumes effects exist and is simply never taken), or (c) don't actually need this as a functional prerequisite at all (edge-semantics misclassification) is NOT checked here -- three genuinely different findings, not yet distinguished. Cross-reference for whoever picks this up: `TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES-RESOLUTION` (next in this session's own program order) is exactly the audit this question belongs to. **Added 2026-09-19, first direct instance checked**: `combat_resolution → status_effects` (the direct dependent, already `KEEP`-verdicted in the parent edge audit, `src/engine/combat.py:84`) is case (a) -- `if any(s.kind == "frozen" for s in defender.combat.status_effects): atk_mult *= 1.5` handles the empty list correctly via `any()`, but the SHATTER attack-bonus branch is dead in production for the same reason `ATTACKER_STATUS_BLOCKED` is: nothing ever creates a `"frozen"` effect to trigger it. **This is now two independently-confirmed dead branches downstream of this one orphan mechanism** (`legality.py`'s block-gate, `combat.py`'s bonus-gate), out of 4 real transitive dependents -- a pattern, not two coincidences: **when a mechanism is confirmed orphan, every dependent branch that consumes its output is dead by implication**, individually invisible to anyone reading that branch's own code in isolation (both reads above are correctly written; neither looks wrong on its own). `trauma` and `commitment_betrayal`/`commitment_pressure_ consequences` (the other 2 of the 4 transitive dependents) are not yet checked and are worth the same look. Mechanically checkable later without a fresh code read each time: orphan status plus the dependent list this registry already holds is sufficient input, once someone wants to build it -- not attempted here, this ticket's own scope was the coverage batch, not a new detector. |
| `succession` | entity | done | static | code_trace | observed | 2026-09-16 | CORRECTED -- this entry's own prior verdict was itself the epic's first false verification verdict, not a wrong `state`: it recorded `observed`, asserting a repo-wide search had confirmed heir_entity_id is never populated, when it had not. LifecycleSystem.resolve_lifecycle() (lifecycle_systems/lifecycle.py:223-227) calls _select_default_heir() unconditionally when heir_entity_id is None -- a real, bond-strength-scored fallback -- and writes the result via `heir_entity_id_set=heir_id`, this codebase's own "_set" suffix convention for StateUpdate patch fields (same shape as death_reason_set/is_permadeath_set in the same function). engine/patches.py:86 merges heir_entity_id_set onto the real heir_entity_id state field on apply. Lines 229-265 then do real downstream work (heirloom/inventory transfer, feud/dying-wish handling) once a heir resolves. The original search pattern (`heir_entity_id=`) could not match `heir_entity_id_set=` -- the third instance in this epic of grep-shaped evidence under-reporting a real write path via a naming convention (progression_conversion/src/progression/, FairShareProtocol's docstring-only name, now this). No flag gates resolve_lifecycle() or this branch -- run_phase("lifecycle", ...) is called with no flag argument. Corrected via TCK-20260916-MECHANISM-ORPHAN-STATE-BATCH-VERIFICATION's own addendum. |
| `trauma` | entity | orphan | static | code_trace | observed | 2026-09-19 | state corrected done -> orphan while resolving edge #4 of TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES-RESOLUTION. The parent edge audit concluded no per-entity trauma implementation exists at all (only `RegionState.trauma_score` and `WoundState`, both confirmed unrelated) -- that conclusion was itself a search miss, not correct: real per-entity trauma tracking exists (`RecoveryState.trauma_tags`, `confidence_loss`, `retry_readiness`, `src/core/cognition.py`), written by `RecoveryReadinessService.register_near_death()` (`src/domains/emotion/ recovery_service.py:21-28`). But `register_near_death` has ZERO callers anywhere in `src/` -- real, reachable, correctly-written code with no production producer, the same shape as `status_effects`'s own orphan finding, found independently and for an unrelated reason (this ticket's own edge #4, not a coverage-extension pass). `-> combat_resolution` edge kept as KEEP, not removed: the dependency is structurally real (a near-death trigger is conceptually downstream of combat resolution determining lethal/near-lethal outcomes) even though currently unexercised -- same "correct-but-unexercised" case (a) as `combat_resolution -> status_effects`'s own SHATTER-bonus finding, not case (c) (misclassification). Not independently re-investigated further -- a third confirmed instance of this session's own orphan-mechanism pattern, worth noting for whoever eventually builds the mechanical "orphan mechanism implies dead dependent branches" check flagged on `status_effects`'s own entry. |
| `world_boss_spawn` | world | done | static | code_trace | observed | 2026-09-17 | `BossService.check_for_boss_spawn()` (`region.trauma_score >= BOSS_SPAWN_TRAUMA_THRESHOLD`) was directly fixed and proven end-to-end in a real, unmodified corpus-world simulation by `TCK-20260914-LAIR-WORLD-BOSS-MATURITY-GATE-REACHABILITY` (closed) -- three real, compounding silent defects (tier-5 stat fallback, `ancient_core` loot registration, gate threshold values) found and fixed, whole chain proven end to end. |
| `adventure_routing` | entity | done | unverified | — | unverified | — | — |
| `affection_relationship_bonds` | entity | done | unverified | — | unverified | — | — |
| `aging_death` | entity | done | unverified | — | unverified | — | — |
| `attributes_biology` | entity | done | unverified | — | unverified | — | — |
| `belief_cycle` | entity | done | unverified | — | unverified | — | — |
| `belief_institution` | world | partial | unverified | — | unverified | — | — |
| `betrayal_siege_war` | faction | done | unverified | — | unverified | — | — |
| `breakthrough_bonuses` | entity | done | unverified | — | unverified | — | — |
| `build_diversity` | entity | gap | unverified | — | unverified | — | — |
| `building_sabotage` | world | done | unverified | — | unverified | — | — |
| `buildings` | world | done | unverified | — | unverified | — | — |
| `campaigns` | world | done | unverified | — | unverified | — | — |
| `chronicle` | world | done | unverified | — | unverified | — | — |
| `clan` | faction | gap | unverified | — | unverified | — | — |
| `cognition_capacity_fatigue` | entity | done | unverified | — | unverified | — | — |
| `commitment_pressure_consequences` | entity | partial | unverified | — | unverified | — | — |
| `committed_intentions` | entity | orphan | unverified | — | unverified | — | — |
| `concern_intake` | entity | done | unverified | — | unverified | — | — |
| `conversation` | entity | gap | unverified | — | unverified | — | — |
| `cooperation` | entity | done | unverified | — | unverified | — | — |
| `crafting` | world | partial | unverified | — | unverified | — | — |
| `cultural_drift` | world | done | unverified | — | unverified | — | — |
| `declared_cognition_schema` | entity | orphan | unverified | — | unverified | — | — |
| `derived_stats` | entity | done | unverified | — | unverified | — | — |
| `diplomacy` | faction | done | unverified | — | unverified | — | — |
| `entity_role` | entity | done | unverified | — | unverified | — | — |
| `entity_trade` | entity | gap | unverified | — | unverified | — | — |
| `equipment_scoring` | world | done | unverified | — | unverified | — | — |
| `event_interpretation` | region | done | unverified | — | unverified | — | — |
| `fame` | world | done | unverified | — | unverified | — | — |
| `fidelity_drift` | world | done | unverified | — | unverified | — | — |
| `gods_pantheon_blessings` | world | gap | unverified | — | unverified | — | — |
| `group_coordination` | group | orphan | unverified | — | unverified | — | — |
| `guilds` | group | partial | unverified | — | unverified | — | — |
| `interaction_channeling` | entity | done | unverified | — | unverified | — | — |
| `inventory_trade_conservation` | world | done | unverified | — | unverified | — | — |
| `knowledge_model` | entity | gated | unverified | — | unverified | — | — |
| `lair` | region | gap | unverified | — | unverified | — | — |
| `movement` | entity | done | unverified | — | unverified | — | — |
| `narrative_memory` | world | orphan | unverified | — | unverified | — | — |
| `nest` | region | gap | unverified | — | unverified | — | — |
| `party_formation` | group | done | unverified | — | unverified | — | — |
| `perception` | entity | done | unverified | — | unverified | — | — |
| `personality` | entity | done | unverified | — | unverified | — | — |
| `quest_generation_sourcing` | entity | gated | unverified | — | unverified | — | — |
| `quest_reward_distribution` | group | orphan | unverified | — | unverified | — | — |
| `race_collective_force` | faction | gap | unverified | — | unverified | — | — |
| `reputation` | faction | done | unverified | — | unverified | — | — |
| `resource_harvesting` | world | orphan | unverified | — | unverified | — | — |
| `settlement_capacity_axis` | faction | gap | unverified | — | unverified | — | — |
| `skill_unlocks` | entity | partial | unverified | — | unverified | — | — |
| `social_contracts` | faction | done | unverified | — | unverified | — | — |
| `social_memory` | faction | skeleton | unverified | — | unverified | — | — |
| `strategic_intelligence_core` | entity | done | unverified | — | unverified | — | — |
| `strategic_learning_bias` | entity | done | unverified | — | unverified | — | — |
| `strategic_redirection` | entity | orphan | unverified | — | unverified | — | — |
| `team_up` | entity | gap | unverified | — | unverified | — | — |
| `temporal_pressure` | entity | skeleton | unverified | — | unverified | — | — |
| `town_services` | world | done | unverified | — | unverified | — | — |
| `world_generation` | world | done | unverified | — | unverified | — | — |
