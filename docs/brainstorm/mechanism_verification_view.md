# Mechanism Verification View

Generated from `registries/mechanisms.yaml` — regenerate with `make mechanism-verification-view`. Do not hand-edit.

62 of 93 mechanisms have a recorded verdict. The remaining 31 are rendered explicitly as `unverified` below, not omitted — a mechanism with no verdict is not the same as a mechanism known to work.

**Static vs runtime evidence, grouped separately below**: `code_trace` proves what the code *says* (reachable, called, a field never written) and can never establish that reachable code has its claimed runtime effect. `census`/`scenario`/`corpus_run` prove what the simulation actually *does*. A `code_trace` row is not equivalent evidence to a runtime-confirmed row.

| Mechanism | Layer | State | Evidence | Instrument | Verdict | Date | Note |
|---|---|---|---|---|---|---|---|
| `action_pacing_readiness` | entity | done | runtime | scenario | observed | 2026-09-16 | Differential scenario (tests/mechanic_scenarios/test_action_pacing_readiness_gate.py): readiness=50.0 withholds a staged attack (INSUFFICIENT_READINESS), readiness=100.0 lets it proceed, through a real Kernel tick against a real compiled world. The gate itself -- what all transitive dependents actually need -- genuinely works. **Split 2026-09-17**: this entry previously also covered the separate agility-scaling recalculation under state `partial`; that half is now its own entry, `readiness_speed_scaling`, since the two halves diverge in real-world outcome (this gate confirmed working; the scaling half confirmed starved) and bundling them under one verdict buried the distinction in prose. This entry is now scoped to the gate only, `state: done` (no known deficiency in the gate itself). **Bound 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION): `LegalityServiceV2.verify_readiness()` is a real, wired caller-confirmed method (called from `src/engine/pipeline_phases/actions.py:146` and `src/engine/domain/action_router.py:39`, both real, non-test call sites), previously left unbound only because `legality.py` is a large multi-concern class and class-level binding would have misattributed unrelated legality logic to this entry -- resolved via the newly-added method-level binding syntax (`path::Class::method`) instead of leaving it permanently unbound. |
| `calamity_intensity` | world | done | runtime | corpus_run | contradicted | 2026-09-17 | `CalamityService.apply_calamity_consequences()` is real, non-dead code -- the sole real producer of `region.calamity_intensity` (+0.05 per hero death inside a `region.hazard_level > 0.5` region). A real, instrumented 5000-tick `Kernel.tick_once()` run against `frontier_living_world` found `calamity_intensity` never left `0.0` in any region for the entire run -- not "never crossed the spawn-gate threshold," never changed at all. Same family as `camp`: correct, wired code, defeated by real-world data never meeting its trigger precondition. See `TCK-20260914-CALAMITY-INTENSITY-PRODUCER-NEVER-FIRES` (still open -- the code-level finding stands regardless of whether/when a fix lands). **Left unbound 2026-09-20, contradicting evidence found** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION): attempted to bind `apply_calamity_consequences()`, then `mechanism_state_caller_check.py` flagged zero real callers -- re-checked directly rather than trusted, and confirmed: `grep -rn "apply_calamity_consequences" src/` finds only its own definition and one unrelated *comment* in `displacement.py`, never a real call. `CalamityService.process_world_dynamics()` (the method actually called, from `world_dynamics.py:133`) only *reads* `region.calamity_intensity` for boss-spawn region selection, never writes it. This is a stronger finding than this entry's own 2026-09-17 note describes: not "correct, wired code defeated by real-world data never meeting its trigger precondition" (implying the code path is reachable, just data-starved) but code that is never called under any circumstances -- closer to `orphan` than `done`+`contradicted`. Not corrected unilaterally here (this entry's own `verified` block predates this batch and this batch's own rule is not to re-verify already-verified entries) -- flagged for the roadmap session to reconcile against `TCK-20260914-CALAMITY-INTENSITY-PRODUCER-NEVER-FIRES`'s own framing. |
| `combat_engagement` | entity | done | runtime | scenario | observed | 2026-09-16 | Corpus: posture gate moved attacks 1960 -> 837. Scenario: risk-rejected posture -> 0 attacks vs no posture -> attack proceeds, all else identical. **World-specificity, added 2026-09-17**: the "1960 -> 837" figure is the `metropolis` synthetic reference scenario specifically, the one world class where the decision-driven `ATTACK` path this gate governs actually fires. In `crowded_frontier`, `quest_dense_frontier`, and `hero_guild_routing`, that same path sees 0-2 real calls per 1000-2000 ticks regardless of this gate's state -- the gate has no measurable effect there, not because it's weak, but because the path it governs is already dormant (see `tactical_decision`). A verdict proves the mechanism works under the conditions measured; this one was never a general claim about the simulation, and is corrected here to say so explicitly rather than leaving "1960 -> 837" to be read as one. |
| `combat_resolution` | entity | done | runtime | corpus_run | observed | 2026-09-19 | Real reachability split by edge, not just a single "is this reached" answer. `CombatResolutionSystem.resolve_attack()` (reached via `tactical_decision` -> `ActionRouter.execute_action()`) had 0-2 real calls per 1000-2000 ticks across `crowded_frontier`/`quest_dense_frontier`/`hero_guild_routing`. `resolve_multi_attack()` (reached via `movement`'s own opportunity-attack mechanic, `src/engine/movement.py`, never through `tactical_decision`/`ActionRouter` at all) had 181-2177 real calls per 1000 ticks in the same worlds. Found while resolving TCK-20260915-CROSS-FACTION-COMBAT-RARITY-INVESTIGATION's own 7th candidate. **`depends_on: [tactical_decision]` removed 2026-09-17** (`TCK-20260917-MECHANISM-IDENTITY-RULES-AND-CHANGE-TAXONOMY`): checked against this registry's own stated definition (`registries/mechanisms.yaml`'s own header, "this mechanism cannot produce a meaningful result without that one already existing/having run") -- `resolve_attack()`/`resolve_multi_attack()` compute a real, meaningful outcome given any attacker/defender pairing regardless of how that pairing was decided; `combat_resolution` does not require `tactical_decision`'s own output to already exist. `tactical_decision` is a real *caller* of `combat_resolution` (execution order), not a functional prerequisite for it -- exactly the conflation the header's own closing line warns against. `movement` is kept: combat resolution reads position/range state that `movement`'s own domain establishes, a genuine functional dependency, even though `movement.py` is what calls `combat_resolution` in code (call direction and `depends_on` direction diverge here by design, not by error). See docs/plans/mechanism_identity_and_change_taxonomy.md §3 for the full reasoning. Multiple entry points into the same internal logic (this entry's own two-caller finding above) is not itself a split signal -- see that same doc's §1. **2026-09-19 update (`TCK-20260919-COMBAT-ENGAGED-HOSTILES-UNIFY-CATALOG-SEMANTICS`):** the 181-2177 `resolve_multi_attack()` figure above was measured against the pre-fix `LegalityServiceV2.get_engaged_hostiles_at_pos()`, which determined hostility from the raw legacy `Faction` enum rather than the real content catalog -- substantially phantom combat, per `TCK-20260919-COMBAT-HOSTILITY-SOURCE-DIVERGENCE-UNIFICATION`'s own measurement (97% of `hero_guild_routing`'s legacy-triggered engagements were not real catalog hostility). After the fix, real, catalog-correct `resolve_multi_attack()` volume over the same 2000-tick runs: `crowded_frontier` 874 -> 133 (-84.8%), `hero_guild_routing` 1418 -> 58 (-95.9%), `quest_dense_frontier` 0 -> 0 (unchanged, already zero). The large drop is the fix working as intended -- combat volume in these worlds was substantially not the game's own authored hostility model -- not a regression. |
| `evolution` | entity | done | runtime | scenario | observed | 2026-09-17 | Real Kernel-tick positive control (docs/plans/mechanic_verification_scenarios_proposal.md pattern): a goblin staged at evolution_points=95 (level 1) forced-attacks and kills an orc in one tick -- EvolutionSystem.evaluate() (src/engine/pipeline.py:381, unconditional, runs after resource_transactions) correctly consolidates the kill's 10 XP, crosses the 100-XP level-2 threshold, and produces evolution_level=2, evolution_points=5 (carry-over), exactly matching docs/mechanics/attribute_progression_contract.md's own worked example. A first attempt at this same control called ApplyPath._apply_entity_update() directly and wrongly read "no level-up" as a wiring defect -- it bypassed this exact phase, which only runs inside the real pipeline. Corrected before reporting; see TCK-20260916-DERIVED-COMBAT-STAT-RECALCULATION-UNOBSERVED-IN-CORPUS's own addendum for the full trace. This mechanism's own code is confirmed correct; see `xp_leveling` for why it still doesn't fire in real corpus play. |
| `readiness_speed_scaling` | entity | partial | runtime | scenario | contradicted | 2026-09-17 | Split out of `action_pacing_readiness` (see that entry's own note and docs/plans/mechanism_identity_and_change_taxonomy.md). Agility-derived `readiness_speed` has a real formula (TCK-20260831-READINESS-SPEED-FORMULA) but only applies via engine/apply.py's `stats_dirty` recalculation, never at spawn. Direct measurement (instrumented call-counting, 3 real corpus worlds, 1000 ticks each) found this path was never observed firing for any of 75 tested entities -- broader than the already-known COMB-318 gap. Root-caused 2026-09-17 (`TCK-20260916-DERIVED-COMBAT-STAT-RECALCULATION-UNOBSERVED-IN-CORPUS`): content- composition/pacing, not a wiring gap -- the combat-XP-to-level-up chain is directly confirmed correct and reachable (see `xp_leveling`'s own verified block), it just never accumulates enough real combat volume to cross a single level threshold within any tested 1000-tick corpus window. `verdict: contradicted` because the implicit "this scales with agility in play" claim does not hold in any tested corpus world, even though the formula and recalculation code are themselves correct. **Bound 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION): the real formula this note already describes is `LevelingService.recalculate_combat_stats()`, called from `SkillScalingService.get_effective_stats()` (`src/engine/rpg_depth.py`), itself called from `apply.py`'s own `stats_dirty` block (`src/engine/apply.py:616`) -- confirms the note's own "correct code, rarely exercised" reading rather than changing it. |
| `regional_trauma` | region | done | runtime | corpus_run | contradicted | 2026-09-17 | `region.trauma_score` accumulation/decay is real, wired code (src/world/consequences.py's own natural decay; combat/death events elsewhere accrue it; read by `camp.py`, `boss.py::check_for_boss_spawn()`, `creature_territory.py`, `threat.py`). But the Lair region specifically was directly measured to never accumulate real trauma across a real corpus run -- same shape as `camp`: correct, wired code, defeated by real-world data (no combat occurs in that region) never meeting the accumulation precondition. See `TCK-20260914-LAIR-REGION-TRAUMA-NEVER-ACCUMULATES` (still open). **Bound 2026-09-20** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS- RESOLUTION): `RegionalConsequenceService.process_recovery()`, the natural-decay half this note already cites, confirmed real caller at `src/engine/apply_plan.py:101`. |
| `tactical_decision` | entity | done | runtime | corpus_run | contradicted | 2026-09-19 | The `ATTACK`-intent branch (the decision this mechanism exists to make) is effectively dormant in real corpus play: 0-2 real `CombatResolutionSystem.resolve_attack()` calls per 1000-2000 ticks across `crowded_frontier`, `quest_dense_frontier`, and `hero_guild_routing` -- independently corroborated three separate times, for three unrelated reasons, across six weeks (TCK-20260808-LIFE-ARC-REBIRTH-REACHABILITY- INVESTIGATION, 2026-08; TCK-20260915-CROSS-FACTION-COMBAT-RARITY-INVESTIGATION's own candidate 5, 2026-09-15). state stays `done` -- no evidence the decision logic itself is wrong, only that it essentially never selects ATTACK in these worlds' real play. **Root-caused 2026-09-19** (`TCK-20260917-TACTICAL-ATTACK-PATH-NEVER-FIRES-INVESTIGATION`, real `Kernel.tick_once()` instrumentation, `LocalSequentialExecutor` forced to avoid the real concurrency this arc's own prior addendum found unsafe for a shared-mutable-state probe): two compounding, independently-measured causes, not one. (1) The strategic layer essentially never assigns a `DEFEAT_ENEMY` objective in these worlds' sampled windows (0 occurrences across 3 corpus worlds, 2 sample points each) -- entities have nothing telling them to fight regardless of what's nearby. (2) Even when the brain does run -- itself suppressed 6-25x below the raw strategic-intelligence cadence ceiling by a three-layer sticky-task/cadence gate stack in `scheduler.py`/`cognition.py`, larger than the previously-cited 93.7% figure -- `hostiles` was found non-empty **0 of 1130** real calls across the three corpus worlds. On the rare occasion a fresh decision does happen (2 of 1130 calls, both worlds), it reliably dispatches -- one decision produced 2 and 29 real `execute_attack()` calls respectively via the same sticky-task re-execution mechanism, so there is no emission-vs-dispatch defect, only rarity of the initial decision. `metropolis`, the intended control, proved unreliable for this specific investigation: its own `build_metropolis_state()` perf-harness has real spawn collisions (confirmed via direct construction-time hard-law-violation logging) that make its entities' default task state unrepresentative of curated world content -- disclosed as a limitation, not chased to a separate root cause (out of this investigation's own scope). Whether the decision path firing more often is desirable is a **design** question for the user, unresolved here per this investigation's own Assumption #2 -- what this closes is the mechanism, not the disposition. See `combat_resolution` and `combat_engagement` for the rest of this same finding, and `stored_artifacts/TCK-20260917-TACTICAL-ATTACK-PATH-NEVER-FIRES-INVESTIGATION/` for the full per-world evidence table. |
| `xp_leveling` | entity | partial | runtime | scenario | observed | 2026-09-17 | Filed while resolving TCK-20260916-DERIVED-COMBAT-STAT-RECALCULATION-UNOBSERVED-IN-CORPUS's own discriminating measurement. The threshold-evaluation chain (kill -> ResourceTransferIntent.xp_reward -> conservation.py COMBAT branch -> IdentityUpdate.evolution_points_delta -> EvolutionSystem.evaluate() -> _execute_level_up()) is directly confirmed correct via a real Kernel-tick positive control (see `evolution`'s own verified block) -- state stays out of `gap`/`orphan` territory, the code is not broken. state moves done -> partial because real corpus play never actually crosses a threshold: instrumented across the same 3 real corpus worlds (crowded_frontier, quest_dense_frontier, hero_guild_routing; seed 42; 1000 ticks each), only 0-10 kills occur per world, each granting defender.identity.evolution_level*10 XP (10 XP for the overwhelmingly common level-1 monster kill) against a 100-XP level-2 threshold -- ~10 kills needed, the busiest single entity in any tested world accumulated 5 kills (50 XP). Same family as `camp` (docs/plans/world_composition_precondition_gap_finding.md): correct, wired code, defeated by real-world volume rather than a code defect. Likely downstream of the already-ticketed TCK-20260915-CROSS-FACTION-COMBAT-RARITY-INVESTIGATION (low cross-faction combat volume generally), not independently re-investigated here. Follow-up: TCK-20260917-XP-LEVEL-UP-THRESHOLD-VS-CORPUS-COMBAT-VOLUME (filed, not fixed). **Left unbound 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION): this entry's own positive-control scenario (the goblin-kills-orc, evolution_points=95 case) is the *identical* scenario already cited in the `evolution` mechanism's own verified block, which is already bound to `src/engine/evolution.py::EvolutionSystem`. Binding `xp_leveling` to the same class would double-attribute one implementation to two registry ids. This looks like a real identity question (are `xp_leveling` and `evolution` the same mechanism under two names, per this registry's own Merge criterion in `docs/plans/mechanism_identity_and_change_taxonomy.md` §2?) rather than a binding gap -- genuinely ambiguous, not resolved here, flagged for the roadmap session rather than merged unilaterally in a binding-only pass. |
| `affection_relationship_bonds` | entity | done | static | code_trace | inconclusive | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. Investigated, left unbound -- genuinely ambiguous. Zero literal matches for "affection" anywhere in `src/` (a concept-name-not-a-symbol case). Checked the plausible real candidates: `RelationshipModel` (`src/core/cognition.py`) tracks `private_trust`/`shared_quest_count`, not an affection or bond-strength score; `MarriageState`/`MarriageStatus` (`src/core/strategic.py`, used in `src/engine/domain/core_actions.py:425-444`) is a formal marriage-contract action, plausible but not confident; `FactionSocialMemory` is faction-level, not personal. No stored_artifacts citation trail exists for this id. `motivation_doctrine` is this entry's only registry dependent, and its own code is confirmed deleted (see that entry's own verified block), so it offers no further lead. Left unbound rather than guessed; flagged for the roadmap session. |
| `aging_death` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. The real, dedicated old-age check ("Check for natural death (old age)": `if entity.lifecycle.age_ticks >= entity.lifecycle.max_age_ticks: is_dead = True; death_reason = "OLD_AGE"`) lives inside `LifecycleSystem.resolve_lifecycle()`, called from `src/engine/pipeline.py:414` every tick (`run_phase("lifecycle", ..., LifecycleSystem.resolve_lifecycle)`) -- a real, heavily cross-referenced call site (`src/observability/event_extractor.py`, `src/economy/vacancy.py` both cite it directly). Method-level binding used because `resolve_lifecycle()` is one of several methods on `LifecycleSystem` -- `succession` (below) is already class-level-bound to the same class for its own heir/feud-transfer methods (`_transfer_inherited_feud`/`_seed_dying_wish`/`_select_default_heir`); left that existing binding untouched (out of scope for a binding-only pass on already-bound entries), but a future pass could tighten it to method-level too, for the same precision reason. Note: `src/engine/apply.py::ApplyPath._compute_entity_changes` also computes `new_age < life.max_age_ticks` for an entity's own `active` flag -- a second real code path touching the same concept, not investigated further here (out of scope for a binding-only pass); flagging in case it represents unintended duplication rather than intentional redundancy. |
| `attributes_biology` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. The "biological pressures" half of this entry's claim is real and wired: passive per-tick accumulation (`hunger=min(100, bio.hunger + 0.1*cadence...)`, `sleep_debt=min(100, bio.sleep_debt + 0.05*cadence...)`) lives in `ApplyPath._compute_entity_changes` (`src/engine/apply.py`, the cadence-gated `is_bio_due` block), a direct `replace()` rather than the `BiologicalUpdate.hunger_delta` field pattern -- worth noting since grepping only for a positive `hunger_delta=` literal would wrongly conclude no passive accumulation exists. The "attributes" half (raw strength/vitality/agility/etc values) is read live throughout `derived_stats`'s own binding (`SkillScalingService.get_effective_stats`, below). Method- level binding used because `_compute_entity_changes` also computes a second, separate age-related check (`new_age < life.max_age_ticks`, feeding an entity's own `active` flag) in the same method -- this is NOT `aging_death`'s own bound implementation (that entry is bound separately to `LifecycleSystem.resolve_lifecycle()`'s own OLD_AGE death check, elsewhere) but a second, not-yet-investigated code path touching the same age concept; flagged on `aging_death`'s own entry as a possible unintended duplication, out of scope to resolve here. |
| `belief_cycle` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. Real, dedicated implementation already cited by `goal_hierarchy`'s own verified note (`BeliefCycleSystem.process_observation()`/`apply_contradiction()`, `intelligence.py:436,448`) -- `BeliefCycleSystem` itself lives in its own single-purpose file (`src/systems/strategic_systems/belief.py`: lead decay, rumor processing, observation confirmation, contradiction), with multiple real, non-test callers: `src/engine/pipeline.py:408` (`run_phase("belief_staleness_decay", ..., BeliefCycleSystem.resolve_lead_staleness(state))`), `src/systems/social_systems/guilds.py` (`process_rumor`), and `intelligence.py`'s own calls above. Class-level binding is precise -- no other mechanism shares this file. |
| `betrayal_siege_war` | faction | partial | static | code_trace | contradicted | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. State correction, done -> partial. The "siege_war" half is real and live: `MilitaryConflictPhase` ("WAR pair detection, siege, territory transfer, war exhaustion") is called unconditionally from `src/engine/pipeline.py:291`. The "betrayal" half is not: `Betrayal(FactionDirective)` (`src/engine/faction_decision.py:97`, "Break an ALLIED relation") has zero real constructor calls anywhere in `src/` -- `diplomatic_actions.py::handle()`'s own dispatch branch for it (`_handle_betrayal`) is real, reachable code, but nothing ever constructs a `Betrayal` instance to dispatch. Possible Split candidate per this registry's own identity rule (`docs/plans/mechanism_identity_and_change_taxonomy.md` §1: "if the two halves could sit in different state values, they are two mechanisms") -- flagged, not performed here, same restraint as the `commitment_betrayal` merge non-execution in the prior batch. |
| `breakthrough_bonuses` | entity | partial | static | code_trace | contradicted | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. State correction, done -> orphan -> **partial** (self-corrected same batch, see below). `BreakthroughService. apply_bonuses()` is real and genuinely wired into the live apply path (`SkillScalingService.get_effective_stats()` calls it, itself called from `src/engine/apply.py:616`'s `stats_dirty` block) -- the bonus-*application* side works. But the acquisition side has zero real producers anywhere in `src/`: grepped every `breakthroughs_add=`/`active_breakthroughs=` construction site repo-wide (evolution/level- up code in `src/engine/evolution.py`, spawn/content resolution in `src/core/builder.py`, `src/content/`) and none ever assigns a non-empty value. `apply_bonuses()` is therefore always called with an empty set in real play. **Corrected `orphan` -> `partial`, same batch**: `mechanism_state_caller_check.py`'s own `orphan_with_callers` finding caught this immediately after the first pass -- `orphan` means zero real callers (this registry's own established definition, e.g. `target_race`), but `BreakthroughService.apply_bonuses()` has a real, live, cross-file caller (`rpg_depth.py:367`), so `orphan` was factually wrong, not just imprecise. This is the same shape as `readiness_speed_scaling`'s own state (`partial`, `verdict: contradicted`): real, wired, called code whose real-world outcome never materializes because of a separate gap upstream -- not dead code with zero callers. |
| `building_sabotage` | world | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. `BuildingSabotageSystem.resolve()` confirmed real, unconditional caller at `src/engine/pipeline.py:334` (`run_phase("building_sabotage", ...)`), matching this mechanism's own already-recorded split rationale above (reads/writes `building.hp` directly, never a service transaction). |
| `buildings` | world | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. Bound to the data class itself (this mechanism's own claim is about the data existing and being placed), not `src/town/buildings.py::BuildingRegistry` (a template-lookup registry that turned out to have zero real callers repo-wide -- an orphan finding in its own right, not filed as its own mechanism here since no registry entry names it). Real `BuildingState` construction confirmed at `src/worldbuilding/compiler.py:538` (`WorldCompiler`, see `world_generation`'s own binding above) and in performance-scenario generators. |
| `camp` | region | done | static | code_trace | contradicted | 2026-09-16 | CampState is real (spawns monsters, triggers raids, a real clearing-reward loop) but no compiled or procedurally-generated world seeds state.camps -- a permanent no-op in every world today. state stays done (the code is correct and wired, not defective) -- the missing thing is world data, not the mechanism itself. See docs/plans/world_composition_precondition_gap_finding.md; same family as the lair-trauma and calamity-intensity cases. **Bound 2026-09-20** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS- RESOLUTION): bound to `CampState` itself, matching this entry's own already-stated identity claim. |
| `campaigns` | world | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. Search-failure trap avoided: the only `CampaignOrchestrator(` construction inside `src/` is its own class-docstring usage example, not real code -- a first-pass search would wrongly conclude orphan. Real, non-test, non-docstring usage found in `tools/calibrate_simq.py` (`orchestrator = CampaignOrchestrator(manifest, ...)`, driving `run_episode()` N times) -- a genuine calibration tool, not a test. |
| `causal_spatial_memory` | entity | gated | static | code_trace | observed | 2026-09-16 | Was mis-registered `orphan`; MemoryUpdatePhase.apply() is called live from engine/pipeline.py:154-157 via run_phase('memory_update', ..., 'ENABLE_MEMORY_UPDATE') -- a real caller exists, so `orphan` (zero callers) was factually wrong. The flag defaults OFF (feature_flags.py, TCK-20260824-CAUSAL-MEMORY-ROUTE-SCORING: 'wires MemoryUpdatePhase into refine() for the first time'), which is what `gated` means. Found while resolving implemented_by bindings (TCK-20260916-MECHANISM-IMPLEMENTED-BY-BINDING). |
| `chronicle` | world | orphan | static | code_trace | contradicted | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. State correction, done -> orphan. Corroborated by an already-filed ticket found during this investigation, `TCK-20260912-CAMPAIGN-CHRONICLE-API-REGISTRY-NEVER-POPULATED-IN-PRODUCTION` (see its own comment in `src/api/routes/chronicle.py`): `ChronicleCompiler()` is only ever instantiated in its own module's docstring example and 2 test files -- zero real production callers. Real, present, purpose-built code (compiler + renderer + grouper + namer + significance scorer, a whole subsystem), never actually invoked outside tests. |
| `city` | region | partial | static | code_trace | observed | 2026-09-19 | `-> regional_sovereignty` edge CONFIRMED KEEP (TCK-20260918-MECHANISM-UNCLASSIFIABLE- DEPENDS-ON-EDGES-RESOLUTION #12). Original grep for "class City" found nothing because a City is not its own class -- the wiring map's own explicit finding: "Already real as data, never named or separated as its own class today a City is a Region, not a place inside one." City-state IS `RegionState`, and `RegionState.owner_faction_id` is `regional_sovereignty`'s own core tracked field (`FactionInfluenceService`) -- the two mechanisms share the exact same underlying state object and field, as direct a functional coupling as this registry records anywhere. **Bound 2026-09-20** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS- RESOLUTION): bound to `RegionState` itself, matching this entry's own already-stated identity claim ("City-state IS RegionState") rather than `FactionInfluenceService`, which is `regional_sovereignty`'s own separate binding. |
| `class_assignment` | entity | partial | static | code_trace | observed | 2026-09-19 | `-> race_archetype` edge CONFIRMED KEEP (TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON- EDGES-RESOLUTION #2): `src/content/resolver.py:257-258` -- `self._social.resolve_role_defaults(role_id) for role_id in species.compatible_roles` -- role/class resolution directly iterates the species' own declared `compatible_roles` list. `class_assignment` cannot resolve valid roles without `race_archetype`'s own `SpeciesDefinition.compatible_roles` field already existing, a genuine functional prerequisite, not just execution order. **Bound 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION): bound to `SocialDefaultsResolver.resolve_role_defaults()` -- the specific method that resolves one role's own definition -- rather than the shared `resolve_living_defaults()` call site cited above, which also does `race_archetype`'s own species-resolution work in the same method body and would misattribute if bound here instead. |
| `cognition_capacity_fatigue` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. Search-failure trap avoided: `docs/strategy/bounded_cognition_feature_spec.md`'s own `CognitionCapacityBuilder`/ `CognitionCapacityProfile` names and field names (`planning_budget`, `active_slice_limit`, etc.) do not exist anywhere in `src/` -- a spec describing a design that shipped under different names, not evidence of missing code. Also two different `CapacityService` classes exist (`src/strategy/cognition_capacity.py`, used by `intelligence.py`'s own never-called `derive_cognition_profile()`; `src/strategy/capacity.py`, the real live one) -- a same-name-different-thing trap. The actual live implementation is `CapacityEnforcementPhase.enforce()`, docstring "Authoritative phase to ensure entities do not exceed cognitive bandwidth" (STRAT-185), called unconditionally from `src/engine/pipeline.py:427` and marked `must_run_every_tick=True` in `phase_graph.py` -- reads `entity.strategic.profile.max_active_projects`/`.interruption_resistance` (real field `profile`, not the `cognition_profile` name several other candidates use), with real consumers confirmed at `src/systems/strategic_systems/detour.py:207`, `src/systems/strategic_systems/intelligence.py:1069,1727`, `src/ai/goals/scorers.py:258`, `src/town/guild.py:90`. |
| `commitment_betrayal` | entity | orphan | static | code_trace | contradicted | 2026-09-20 | `-> combat_resolution` edge REMOVED (TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON- EDGES-RESOLUTION #7): originally recorded as a merge candidate with `commitment_pressure_consequences` -- "no distinct implementation of its own anywhere in `src/`" -- but that search only covered `src/domains/commitment/`. **Superseded 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION): a real, distinct, purpose-built implementation exists and was missed -- `BetrayalRecord` (`src/core/models/social.py`: `contract_id`/`betrayer_id`/`victim_id`/ `severity`/`tick`), an entity-level record for exactly this concept, contract-tied like `commitment_pressure_consequences`'s own `ContractState` model but a genuinely separate type. State corrected `done` -> `orphan`, not merged: grepped every `BetrayalRecord(` construction site repo-wide, found zero -- the type is defined and referenced by nothing, a real, present, purpose-built dataclass that is never actually populated. The merge-candidate premise (`docs/plans/mechanism_identity_and_change_taxonomy.md` §2) was itself based on an incomplete search and is corrected there too. A second, faction-level candidate was also checked and set aside: `Betrayal(FactionDirective)` (`src/engine/faction_decision.py`) breaks an ALLIED relation between factions -- also confirmed never constructed anywhere, but `layer: faction` conceptually, not this entity-level mechanism; not investigated further here as it's out of this batch's scope, flagged in case it is some other faction-layer mechanism's own unbound orphan. |
| `country_lifecycle` | faction | partial | static | code_trace | observed | 2026-09-19 | `-> betrayal_siege_war` REMOVED (TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES- RESOLUTION #11). Correction to the parent audit's own claim ("no country_lifecycle implementation found anywhere"): real code exists -- `FactionDecisionPhase` (`src/engine/faction_decision.py`), confirmed real/live in the atlas's own investigation ("Country" and "Faction" are the same underlying class, wiring map's own Country Lifecycle row) -- the original grep for "CountryLifecycle"/"Country" class names missed it for the same reason `city`'s own search missed `RegionState`: conflation with an existing class name, not absence of code. `FactionDecisionPhase` itself never references siege/war/ betrayal concepts (grep confirmed empty), so the specific `-> betrayal_siege_war` edge has no real read evidence and is removed, distinct from a blanket "no code at all" claim. **Bound 2026-09-20** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS- RESOLUTION): class-level, this note's own already-cited real implementation. |
| `crafting` | world | partial | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. Judgement call, not a certainty: the plausible-looking `CraftingSystem.craft()` (`src/systems/economy_systems/crafting.py`) is a real class but a real code comment (`src/engine/intent/action_intent.py:210`) explicitly warns "do not call CraftingSystem.craft() directly, it bypasses that authoritative path" -- confirmed via grep, `CraftingSystem.craft()` has zero real callers anywhere. The actual live crafting path is `ResourceTransactionResolver.resolve()`'s own `"CRAFTING"` branch (`src/core/conservation.py:133`), the SAME class already bound to `inventory_trade_ conservation` above -- crafting is one branch inside that resolver's single `resolve()` method, not separable at this registry's current method-level granularity (branch-level binding does not exist). Bound to the same class rather than left unbound, since the real live code is directly confirmed and cited precisely; flagged here as a judgement call because the binding cannot distinguish this mechanism's own branch from its siblings'. |
| `cross_episode_grief_nemesis` | faction | done | static | code_trace | observed | 2026-09-16 | Confirmed live, called from Campaign orchestrator (dead ally -> grief concern; repeated betrayal -> party-formation blocker); narrow trigger. **Bound 2026-09-20** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS- RESOLUTION): file-level, since both `GriefUrgencyImporter` (grief) and `NemesisRelationImporter` (nemesis) live in the same file and both are this one mechanism's own concern, not two unrelated symbols sharing a file. Real callers confirmed at `src/domains/campaigns/orchestrator.py:966` and `src/engine/kernel.py:745`. |
| `cross_episode_social_consequences` | faction | done | static | code_trace | observed | 2026-09-16 | Was mis-registered `orphan`; evaluate_social_consequence() is called unconditionally (no feature-flag check, gated only on the scenario-recorder infra being present) from domains/campaigns/orchestrator.py at episode-entity-spawn time, so `orphan` (zero callers) was factually wrong. Found via TCK-20260916-MECHANISM-ORPHAN-STATE-BATCH-VERIFICATION. |
| `demographic_cohort_cycle` | region | done | static | code_trace | contradicted | 2026-09-16 | Was mis-registered `orphan`; DemographicCycleService.process_demographics has a real caller (engine/world_dynamics.py:178-179), so `orphan` (zero callers) was factually wrong. Same family as `camp`: correct, wired code defeated by absent data -- the caller is guarded by `if not region.population_cohorts` (worldbuilding/compiler.py:203), and no compiled or procedurally-generated world seeds population_cohorts, so the call is a permanent no-op in every world today. state stays `done` (the code is correct and wired, not defective) -- the missing thing is world data, not the mechanism itself. Found by TCK-20260916-MECHANISM-STATE-CALLER-MISMATCH-DETECTION's own first real run, corrected per peer review rather than left as an open taxonomy question. |
| `derived_stats` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. `SkillScalingService.get_effective_stats()` orchestrates the full derived-stat pipeline (attribute base via `LevelingService.recalculate_combat_stats()` + breakthrough bonuses + class-tier bonuses + wound/scar penalties), called from `src/engine/apply.py:616` inside the `stats_dirty` block -- a real, authoritative-apply-path caller. Method-level binding used because `SkillScalingService` also defines `calculate_skill_damage()`, an unrelated combat-damage-scaling concern, on the same class. `readiness_speed_scaling`'s own binding (`LevelingService.recalculate_combat_stats`) computes one specific field within this same pipeline; kept as a separate, more specific citation there rather than merged into this entry's own binding. |
| `emotion` | entity | done | static | code_trace | observed | 2026-09-16 | Was mis-registered `orphan`; EmotionUpdateService.update_on_event() is called unconditionally (no feature-flag check) from engine/pipeline_phases/hardening.py's own NearDeathHardeningPhase, itself invoked via a plain run_phase("near_death_hardening", ...) call with no flag argument -- a real, live, unflagged caller, so `orphan` (zero callers) was factually wrong. Foundation's own investigation.md had already flagged this exact risk ("possibly stale, TCK-20260824 wired a near_death call site") without resolving it; this confirms the flag was correct. Found via TCK-20260916-MECHANISM-ORPHAN-STATE-BATCH- VERIFICATION. |
| `entity_role` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. `EntityRole` (IntEnum) is read across roughly 15 real files (`src/world/spawn.py`, `src/engine/legality.py`, `src/engine/military_conflict.py`, `src/engine/evolution.py`, etc.) -- e.g. `EvolutionSystem.evaluate()` gates skill-learning on `entity.identity.role == EntityRole.HERO`. Symbol-level binding since `enums.py` defines 9 unrelated enums. |
| `equipment_scoring` | world | orphan | static | code_trace | contradicted | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. State correction, done -> orphan. `EquipmentService`'s own docstring calls itself "the canonical implementation" for equipment scoring, replacement, auto-equip, and repair -- but every single reference to `EquipmentService` anywhere in `src/` is internal to its own file (`equipment.py`, its own methods calling each other); zero external imports or callers found repo-wide (`from src.core.equipment import`, `auto_equip`, `get_gear_score`, `should_replace`, `repair_equipment` all return zero hits outside the file itself and tests). Real, well-built, entirely unused code. |
| `genetics_aptitude` | entity | gated | static | code_trace | observed | 2026-09-16 | Was mis-registered `orphan`; GeneticsSystem.generate_profile_from_seed()/combine_profiles() have a real caller (src/world/reproduction_humanoid.py, real genetic-profile combination for offspring), so `orphan` (zero callers) was factually wrong. That caller is itself gated behind ENABLE_REPRODUCTION_HUMANOID_PATH (default OFF, feature_flags.py), which is what `gated` means. Found via TCK-20260916-MECHANISM-ORPHAN-STATE-BATCH-VERIFICATION. |
| `goal_hierarchy` | entity | done | static | code_trace | observed | 2026-09-19 | Real implementation LOCATED while resolving TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS- ON-EDGES-RESOLUTION's goal_hierarchy cluster (#8-10) -- the parent edge audit's own grep for "goal hierarchy" and `graphify query` both failed because the wiring map's own definition ("Directive -> Project -> Objective -> Action") maps to `StrategicIntelligenceSystem`'s project/objective methods (`evaluate_project_switch`, `resume_project`, `process_project_outcome`, `_resolve_active_objective`, `evaluate_strategic_intent`, `src/systems/strategic_systems/intelligence.py`), never a literally-named "goal hierarchy" symbol. Not bound via `implemented_by` here -- this is one multi-concern 1782-line class also implementing `strategic_intelligence_core` and others via different methods, the same method-level binding gap already recorded on `action_pacing_readiness`/`skill_unlocks` (TCK-20260917-MECHANISM-IMPLEMENTED-BY-COVERAGE- EXTENSION). `-> reputation` REMOVED (#9): no read of any reputation field (`public_reputation`/`regional_reputation`/`clan_reputation`) or import of `ReputationService` anywhere in the strategic/goal-scoring subsystem (`intelligence.py`, `src/ai/goals/`, `src/domains/adventure/`). `-> belief_cycle` CONFIRMED KEEP (#8): the same strategic pass directly calls `BeliefCycleSystem.process_observation()`/ `apply_contradiction()` (`intelligence.py:436,448`) to confirm or contradict leads that feed the same objective-selection pipeline. **Bound 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION): all 5 methods this note already names, bound at method level now that the binding syntax exists (`path::Class::method`) -- resolves the gap this note itself recorded rather than leaving it open a second batch running. |
| `guilds` | group | partial | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. `GuildAction.visit()` confirmed real caller at `src/engine/pipeline_phases/guild_visit.py:78` (`GuildVisitPhase`), itself gated behind `ENABLE_GUILD_QUEST_GENERATION` -- which defaults `ON` (`feature_flags.py`), so effectively live, not flag-suppressed. A sibling class, `GuildIntelSystem` (`src/systems/social_systems/guilds.py`), was checked and found to have zero real callers anywhere -- not bound here, flagged as its own smaller finding. |
| `information_trust_deception` | entity | gated | static | code_trace | observed | 2026-09-16 | Code read confirms the mechanism is correctly built; currently flag-gated off (see state). **Investigated, left unbound 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION): the most plausible named candidate, `SourceTrustUpdateService.update()` (`src/domains/information/trust.py`), has ZERO real callers anywhere in `src/` -- not flag-gated-off, simply never invoked, a different shape than this note's own "flag-gated" framing describes. `InformationBeliefPhase.apply()` (`src/domains/information/phase.py`, gated behind `ENABLE_BELIEF_ASSIMILATION`) is a real, wired candidate but its own concern (observation routing/belief assimilation) reads closer to `belief_cycle`'s own scope than to trust/deception specifically, and misattributing it here risks the same error class as the `trauma` incident. Genuinely ambiguous without the original 2026-09-16 investigator's own working notes (not found in `stored_artifacts/`) -- left unbound rather than guessed; flagged for the roadmap session as worth a closer look, since the discrepancy between this note's "flag-gated" framing and the zero-caller candidate found here is itself notable. |
| `interaction_channeling` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. `InteractionComponent`'s own docstring ("State for multi-tick channeling logic") names this mechanism directly. `InteractionSystem.enforce()` (`src/engine/interaction.py`, the file's only class) is called from `src/engine/pipeline.py:333` (`run_phase("interaction_enforcement", ..., InteractionSystem.enforce)`), a real per-tick pipeline phase. Five real progress-advancing call sites confirm active use: `systems/social_systems/guilds.py`, `systems/world_systems/harvesting.py`, `systems/economy_systems/{chests,town_service}.py`, `systems/economy_systems/loot.py`. |
| `inventory_trade_conservation` | world | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. `ResourceTransactionResolver.resolve()` is real and live, called from `src/engine/economy.py:96,179` -- the same class `xp_leveling`'s own verified note (entity-layer batch) already cites as "conservation.py COMBAT branch." This mechanism's own claim is the resolver's general atomic-conservation law, not any one branch. |
| `knowledge_model` | entity | gated | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. `KnowledgeModelService` (single-purpose class, own file) is one of the 4 services `SelfModelUpdatePhase.apply()` orchestrates (see `self_model`'s own verified block) -- gated by the same `ENABLE_SELF_MODEL_COGNITION` flag transitively, matching this entry's own `gated` state. |
| `motivation_doctrine` | entity | gap | static | code_trace | observed | 2026-09-16 | Found while tracing dependency edges for TCK-20260916-MECHANISM-DEPENDENCY-GRAPH- POPULATION, not this ticket's own target. src/domains/motivation/__init__.py's own docstring: DoctrineResolver and MotivationBiasService were DELETED (TCK-20260908-DEAD-DOCTRINE-VALUES-CHAIN-RETIREMENT, 2026-09-08) -- resolver.py/service.py no longer exist, confirmed dead in production before removal (see docs/guidelines/intentional_divergences.md #2.53), superseded by adventure_routing's own live personality_bias mechanism. gap, not orphan: orphan means dead code still physically present with zero callers (this registry's own established usage, e.g. target_race); here the implementing code itself is gone, so "zero production callers" would be a false claim about code that doesn't exist -- gap is the value that survives becoming an executable assertion (claims-as-tests phase 2). Only evaluator.py (RoleFitEvaluator, an unrelated gear/skill/role-fit scorer) remains in the module. **`depends_on` edges removed 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND- CLAIMS-RESOLUTION, resolving the last 2 `unaudited_depends_on_edges`): both `-> goal_hierarchy` and `-> affection_relationship_bonds` were unauditable via code-trace (the implementing code they'd need to be confirmed against is deleted) and, per `docs/guidelines/intentional_divergences.md` #2.53's own description of the deleted chain's real behavior (`compute_bias_multiplier()` read only `class_id`-based `IdentityDoctrine`/`ValuePreferenceProfile` records, never any goal-hierarchy or relationship-model output), neither edge shows evidence of ever having been a genuine functional dependency in the first place -- same conflation shape as the `combat_resolution -> tactical_decision` edge already removed in `docs/plans/mechanism_identity_and_change_taxonomy.md` §3. Removed rather than left unaudited on a `gap`-state mechanism whose own dependency claims can never be verified now that its code is gone. |
| `opportunity_rumor_seeds` | world | gated | static | code_trace | observed | 2026-09-16 | Code read confirms the mechanism is correctly built; currently flag-gated off (see state). **Bound 2026-09-20** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS- RESOLUTION): both services are called from `WorldEmergencePhase.execute()` (`src/domains/world_emergence/phase.py`), itself called from `src/engine/pipeline.py:355` via `run_phase("world_emergence", ..., "ENABLE_WORLD_EMERGENCE")` -- flag defaults OFF, confirming the note's own "flag-gated" framing exactly. |
| `party_formation` | group | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. `GroupPhase`'s own docstring: "Pipeline phase responsible for coordinating group formation, updates, and removals." Real, unconditional caller confirmed at `src/engine/pipeline.py:501`. Internally dispatches to `GroupSystem.update_groups()` (`src/systems/world_systems/groups.py`) -- several OTHER files reference `GroupSystem.update_groups()` only in comments, not real calls, which could have produced a false "zero callers" conclusion if `GroupSystem` alone had been checked instead of its real caller `GroupPhase`. |
| `perception` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. `PerceptionFilterService.filter()` scores candidate signals via `SignalSalienceEvaluator.evaluate()` (`src/domains/perception/salience.py`) and budget-clamps them into a `PerceptionUpdate` -- called from `src/domains/perception/phase.py:43`, a real, non-test integration into the per-tick pipeline. `filter.py` defines only this one service class plus its own data shapes (`PerceptionBudget`/`PerceptionUpdate`), so class-level binding is precise without misattributing an unrelated concern. |
| `personality` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. No single "personality system" -- `PersonalityComponent` (greed/bravery/sociability/industry) is a pure data component read directly by at least 8 real, distinct call sites: `src/engine/tactical.py`, `src/engine/movement.py`, `src/engine/cognition.py` (panic reduction), `src/ai/goals/scorers.py` (combat-vs-flee utility scoring), `src/systems/social_systems/clan_lifecycle.py`, `src/systems/social_systems/party_lifecycle.py`. Symbol-level binding to the data model itself, since consumption is genuinely distributed rather than centralized in one service (`state.py` defines many unrelated components, so file-level would be imprecise). |
| `progression_conversion` | entity | gated | static | code_trace | observed | 2026-09-17 | Confirmed gate is real and correctly documented (DEV-003/DEV-004, docs/guidelines/intentional_divergences.md) while investigating `xp_leveling`. Noted for whoever flips ENABLE_PROGRESSION_EVOLUTION on: its ALLOCATE_AP conversion branch spends unspent_ap, which combat-driven level-ups grant (+5 per level) -- since `xp_leveling` is itself corpus-pacing-starved (see that entry), unspent_ap from combat will be similarly scarce even once this gate opens, on top of the flag being off today. |
| `quest_generation_sourcing` | entity | orphan | static | code_trace | contradicted | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. State correction, gated -> orphan. `QuestGenerationSystem` (`src/systems/world_systems/quests.py`, `generate_from_scar()`/`generate_from_blockers()`/`quest_to_project()`, real purpose-built code with its own compliance IDs STRAT-150..163) has ZERO real callers anywhere in `src/` for any of its 3 methods -- `src/systems/quests.py` merely re-exports the class, it is never actually invoked. This is `orphan` (dead code physically present, zero callers), not `gated` (a feature flag exists and is off) -- no `ENABLE_QUEST_GENERATION`-shaped flag was found guarding it; `ENABLE_GUILD_QUEST_GENERATION` is a real, live, unrelated flag guarding a *different* mechanism (`GuildVisitPhase`, guild-specific quest boards), not this class. A `done`/`gated` claim with nothing calling it is itself the finding this batch exists to surface, per this batch's own explicit instruction. |
| `race_archetype` | entity | done | static | code_trace | observed | 2026-09-19 | Found while resolving TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES-RESOLUTION's #2/#16: the parent edge audit's own grep for "race"/"Race" class names came back empty because `TCK-20260904-EPIC-RACE-TO-SPECIES-TERMINOLOGY` (2026-09-04) renamed `RaceDefinition`/`race_id` -> `SpeciesDefinition`/`species_id` repo-wide, before that audit ran -- a terminology-drift search miss, not an absence of code. Real implementation: `SpeciesDefinition` (`src/content/schema.py:134`), resolved per-entity via `src/content/resolver.py`'s `resolve_living_defaults(species_id)`. **Bound 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION): bound to `SpeciesDefinition` itself (the data definition this entry's own claim is about), not `resolve_living_defaults` -- that resolver method also performs `class_assignment`'s own role resolution in the same function body, so binding it here would double-attribute one method to two mechanism entries; see `class_assignment`'s own binding below instead. |
| `regional_sovereignty` | region | done | static | code_trace | observed | 2026-09-17 | Corrected binding, caught by this split's own state-caller-mismatch re-check (`RegionalSovereigntyService` was the first-guessed binding and has zero real callers anywhere in `src/` -- see the flag below). The real, live ownership-tracking mechanism the atlas's own extensive investigation confirms ("the sovereignty mechanism described above is real and live exactly as documented") is `FactionInfluenceService` (`RegionState.owner_faction_id`, `+-5.0` influence shift per relevant death, conquest at `<= -50.0`/liberation at `>= +50.0`), with 3 real callers (`engine/world_dynamics.py`, `systems/lifecycle_systems/lifecycle.py`, `economy/vacancy.py`). **Flagged, not further split or ticketed here**: `src/world/regional_sovereignty.py::RegionalSovereigntyService` (regional taxation + macroscopic conquest debuffs -- `TAX_RATE_ENTITY`/`TAX_RATE_BUILDING`, `CONQUERED_ATK_DEF_MOD`/`CONQUERED_SPD_MOD`) is real, distinct, non-dead-code, and has ZERO real callers anywhere in `src/` -- a genuine `orphan` by this registry's own vocabulary, found as a side effect of this split rather than this ticket's own target. Not filed as its own ticket here to keep this identity-rules ticket's own scope bounded; worth its own follow-up investigation (same shape as `camp`/`motivation_doctrine`'s own orphan findings) for whoever next touches regional sovereignty. |
| `reputation` | faction | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. `ReputationService.combine_public_reputation()` confirmed real caller at `src/core/builder.py:676`. Chosen over `src/domains/commitment/reputation.py` (a different, commitment-specific reputation-label module already effectively covered by `commitment_pressure_consequences`'s own binding) and `src/core/cognition.py`'s `PublicReputationProfile` (a data class, not a service) -- this mechanism's own `layer: faction` points at the general faction-facing reputation service, not the commitment-specific one. |
| `ruins_mines_battlefields` | region | gap | static | code_trace | observed | 2026-09-19 | Both edges REMOVED (TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES-RESOLUTION #13/#14). The parent audit's own low-confidence candidate, `create_battlefield_scar()` (`src/world/consequences.py:58-77`, `kind="BATTLE_FIELD"`), is confirmed a genuine name match but has ZERO callers anywhere in `src/` -- itself orphan, not a live producer -- and its own body reads neither `trauma_score` nor `owner_faction_id`, so no direct evidence supports either edge even if it were live. Corroborated independently by the wiring map's own verdict for this exact mechanism: "flattened into ordinary Region tags... Gap" -- ruins/ mines/battlefields are real in content (`haunted_battlefield`, `old_mine` tags) but not a separately-coded mechanism with its own dependency-worthy logic today. **State corrected 2026-09-20** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND- CLAIMS-RESOLUTION): `partial` -> `gap`, applying this note's own already-stated conclusion ("not a separately-coded mechanism... Gap" per the wiring map's own verdict) -- no fresh investigation performed, just the natural implication of what this entry already documented. Left unbound: no real implementing code exists to bind. |
| `self_model` | entity | gated | static | code_trace | observed | 2026-09-16 | Code read confirms the mechanism is correctly built; currently flag-gated off (see state). **Bound 2026-09-20** (TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION): this note's own unbound claim resolved to `SelfModelUpdatePhase.apply()`, called from `src/engine/pipeline.py:192` -- `run_phase("self_model", update, lambda u: SelfModelUpdatePhase.apply(state, u), "ENABLE_SELF_MODEL_COGNITION")` -- confirming the flag-gated framing exactly (`ENABLE_SELF_MODEL_COGNITION` defaults OFF in `feature_flags.py`). Orchestrates 4 sub-services (`SelfAssessmentService`, `NeedInterpretationService`, `CapabilityEstimateService`, `KnowledgeModelService`); `knowledge_model` is bound separately below to the one of those four with its own dedicated registry entry. |
| `skill_unlocks` | entity | partial | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. Real, wired implementation: `LevelingService.get_unlocked_skills()` (`src/progression/leveling.py`), called from `EvolutionSystem.evaluate()` (`src/engine/evolution.py:119`, `skills_to_learn.extend(LevelingService.get_unlocked_skills(lvl))`) for HERO-role entities on level-up -- a real, non-test caller in the authoritative apply path, not merely defined. Method-level binding used (not class- or file-level) because `LevelingService` also implements unrelated leveling concerns via other methods (`get_xp_required`, `process_progression`, `recalculate_combat_stats`, already bound to `readiness_speed_scaling` below) -- previously left unbound for exactly this reason (TCK-20260917-MECHANISM-IMPLEMENTED-BY-COVERAGE-EXTENSION) until method-level binding syntax existed. State stays `partial`, not re-verified upward to `done` here (out of scope for a binding-only pass) -- this confirms the code exists and is wired, not that its real-world outcome is fully verified. |
| `social_contracts` | faction | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. `diplomatic_actions.py::handle()` dispatches `TreatyOffer`/`TradeAgreement`/ `NonAggressionPact`/`AllianceProposal` (the real social-contract types; `Betrayal` is also dispatchable but has zero real producers -- see `betrayal_siege_war`'s own entry, a separate mechanism). Real caller confirmed at `src/engine/pipeline.py:265`, which also calls `compute_common_enemy_pairs()` to generate real `AllianceProposal` instances -- this is the "diplomatic_transitions" phase (Phase 8d), distinct from `FactionDecisionPhase` (`country_lifecycle`'s own binding below), confirmed by direct code read, not assumed from the similar naming. |
| `status_effects` | entity | orphan | static | code_trace | observed | 2026-09-19 | state corrected partial -> orphan while binding `implemented_by` (TCK-20260917-MECHANISM-IMPLEMENTED-BY-COVERAGE-EXTENSION). The state model (`EntityState.combat.status_effects: List[StatusEffectState]`), the update schema (`StatusEffectUpdate`, `src/core/updates.py`), and the apply-path patch (`StatusEffectPatch`, `src/engine/patches.py`) all exist and are internally consistent -- but a repo-wide grep for `status_effect_update` (the only field that carries a `StatusEffectUpdate` into the apply pipeline) and for `StatusEffectUpdate(` / `effects_add=` finds zero constructions anywhere outside `patches.py`/`updates.py` themselves, and the only test referencing either symbol (`tests/unit/domains/optimization/test_component_patches.py`) exercises the patch/merge logic directly, not through any real game-logic caller. No system in `src/` ever adds a status effect during play. This registry's own established usage (see `motivation_doctrine`'s entry above) is orphan means dead code still physically present with zero callers, distinct from gap (code deleted) -- this is squarely the former: real, reachable, internally-tested code with no production producer. Consequence: the `ATTACKER_STATUS_BLOCKED` frozen/stunned gate read in `src/engine/legality.py` (checked while investigating `action_pacing_readiness`/`combat_resolution` bindings in this same pass) can never actually trigger in real play, since nothing ever populates the list it reads. Not independently re-investigated further here -- flagged as a real, corpus-relevant finding for a future ticket, same treatment as `camp`/`demographic_cohort_cycle`/ `succession`. **Added 2026-09-19, peer review**: this mechanism has 4 real transitive dependents (`combat_resolution` direct; `trauma`, `commitment_betrayal`, `commitment_pressure_consequences` transitive through `combat_resolution`/`trauma`) and ranks #4 by priority (20) with 4 transitive dependents of its own in `mechanism_registry_view.md` -- so the top of the "what to verify next" list currently contains a mechanism whose input never exists in production. `ATTACKER_STATUS_BLOCKED` is the one concrete dead-gate instance actually traced so far; whether the other 3 dependents (a) handle status-effect absence correctly (a real, if never-exercised, dependency), (b) are silently degraded (a branch that assumes effects exist and is simply never taken), or (c) don't actually need this as a functional prerequisite at all (edge-semantics misclassification) is NOT checked here -- three genuinely different findings, not yet distinguished. Cross-reference for whoever picks this up: `TCK-20260918-MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES-RESOLUTION` (next in this session's own program order) is exactly the audit this question belongs to. **Added 2026-09-19, first direct instance checked**: `combat_resolution → status_effects` (the direct dependent, already `KEEP`-verdicted in the parent edge audit, `src/engine/combat.py:84`) is case (a) -- `if any(s.kind == "frozen" for s in defender.combat.status_effects): atk_mult *= 1.5` handles the empty list correctly via `any()`, but the SHATTER attack-bonus branch is dead in production for the same reason `ATTACKER_STATUS_BLOCKED` is: nothing ever creates a `"frozen"` effect to trigger it. **This is now two independently-confirmed dead branches downstream of this one orphan mechanism** (`legality.py`'s block-gate, `combat.py`'s bonus-gate), out of 4 real transitive dependents -- a pattern, not two coincidences: **when a mechanism is confirmed orphan, every dependent branch that consumes its output is dead by implication**, individually invisible to anyone reading that branch's own code in isolation (both reads above are correctly written; neither looks wrong on its own). `trauma` and `commitment_betrayal`/`commitment_pressure_ consequences` (the other 2 of the 4 transitive dependents) are not yet checked and are worth the same look. Mechanically checkable later without a fresh code read each time: orphan status plus the dependent list this registry already holds is sufficient input, once someone wants to build it -- not attempted here, this ticket's own scope was the coverage batch, not a new detector. |
| `strategic_intelligence_core` | entity | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-ENTITY-LAYER-UNBOUND-CLAIMS-RESOLUTION. `fused_strategic_pass()` is the pipeline's own literal per-tick strategic-intelligence entry point -- `src/engine/pipeline.py:398`: `run_phase("strategic_intelligence", update, lambda u: StrategicIntelligenceSystem.fused_strategic_pass(state, u, cadence=cadence))`, docstring "Fused pass for strategic intelligence (Blockers, Concerns, Intents)" -- matching this mechanism's own "core" naming as the umbrella entry point, distinct from `goal_hierarchy`'s own 5 project/objective methods on the same class. |
| `succession` | entity | done | static | code_trace | observed | 2026-09-16 | CORRECTED -- this entry's own prior verdict was itself the epic's first false verification verdict, not a wrong `state`: it recorded `observed`, asserting a repo-wide search had confirmed heir_entity_id is never populated, when it had not. LifecycleSystem.resolve_lifecycle() (lifecycle_systems/lifecycle.py:223-227) calls _select_default_heir() unconditionally when heir_entity_id is None -- a real, bond-strength-scored fallback -- and writes the result via `heir_entity_id_set=heir_id`, this codebase's own "_set" suffix convention for StateUpdate patch fields (same shape as death_reason_set/is_permadeath_set in the same function). engine/patches.py:86 merges heir_entity_id_set onto the real heir_entity_id state field on apply. Lines 229-265 then do real downstream work (heirloom/inventory transfer, feud/dying-wish handling) once a heir resolves. The original search pattern (`heir_entity_id=`) could not match `heir_entity_id_set=` -- the third instance in this epic of grep-shaped evidence under-reporting a real write path via a naming convention (progression_conversion/src/progression/, FairShareProtocol's docstring-only name, now this). No flag gates resolve_lifecycle() or this branch -- run_phase("lifecycle", ...) is called with no flag argument. Corrected via TCK-20260916-MECHANISM-ORPHAN-STATE-BATCH-VERIFICATION's own addendum. |
| `town_services` | world | done | static | code_trace | observed | 2026-09-20 | Flagged, not further split here: within town_services, CHURCH's own BLESSING/RESURRECTION services are confirmed dead -- data-label strings with zero real code reading them anywhere in src/, and CHURCH is not placed in any world module (TCK-20260907-CHURCH-CONTENT-AUTHORING, closed, Completion Summary). Inn/Guild/Shop/Blacksmith services are live and consumed by real code. Not split into its own row per this ticket's own "no forced uniform granularity" rule -- no real dependent needs Church's services specifically, so there is no seam a split would serve today. Revisit if that changes. **Bound 2026-09-20** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS- RESOLUTION): `ShopSystem.enforce()`/`BlacksmithSystem.enforce()` both confirmed real, unconditional callers in `src/engine/pipeline.py:371,223`. Caution on this note's own prior "Inn... services are live" claim: `InnAction.rest()` (`src/town/inn.py`) was independently re-checked this pass and has ZERO references anywhere outside its own file -- the opposite of what the prior note says. Not bound here (state stays `done` since Shop/Blacksmith alone already justify it); flagged as a real discrepancy in the prior claim, worth its own look. |
| `trauma` | entity | done | static | code_trace | observed | 2026-09-19 | SELF-CORRECTION, 2026-09-19: a same-day earlier edit to this entry (state done -> orphan, implemented_by -> RecoveryReadinessService, while resolving edge #4 of TCK-20260918- MECHANISM-UNCLASSIFIABLE-DEPENDS-ON-EDGES-RESOLUTION) was a real misattribution, caught and reverted the same day by checking this mechanism's own original Foundation-era citation (`stored_artifacts/TCK-20260915-MECHANISM-REGISTRY-FOUNDATION/investigation.md:199`: "`trauma` | `combat_resolution` | done | atlas `entity-modification#0`; wiring-map `CMB -.-> TRM`"). `entity-modification#0` is the atlas's "Trauma: A Lasting Physical Consequence" card -- Wound (temporary) and Scar (permanent) as trauma's own two stages, explicitly the PHYSICAL combat-injury system, never the psychological one. The earlier edit found real orphan code (`RecoveryReadinessService.register_near_death()`, `src/domains/emotion/recovery_service.py`, zero callers) that is ALSO plausibly describable as "trauma" in English, and attributed it to this registry entry without checking the entry's own original citation trail first -- exactly the kind of unforced error this registry's own citation discipline exists to prevent. That orphan code is real and still orphaned, but it implements a different, currently-unregistered concept (near-death psychological state), not this mechanism. Real implementation: `WoundService` (`src/engine/rpg_depth.py`) -- `create_wound()` is called from `CombatResolutionSystem._get_wound_infliction()` (`src/engine/combat.py:611`, itself called from all 4 real attack-resolution call sites: lines 194, 288, 504, 562) when damage exceeds 25% of the defender's max HP, exactly matching the atlas card's own described formula. `get_wound_stat_penalties()`/`get_scar_stat_penalties()` are read in `tactical_decision`'s own code (`src/engine/tactical.py:48,57`). Fully live, confirmed real callers on both the write and read sides -- not "correct-but-unexercised" the way `combat_resolution -> status_effects` is; this dependency is both structurally real and actively exercised. `systems:` corrected `[cognition]` -> `[combat]` to match: wound/scar is a combat consequence, not a cognitive process, and the prior `[cognition]` assignment was itself downstream of the same misattribution. `-> combat_resolution` edge: still KEEP, now on stronger grounds than the reverted entry's own "structurally real, currently unexercised" framing -- wounds are created BY combat resolution as a direct result of its own damage calculation, a textbook functional prerequisite, fully exercised in real play. |
| `world_boss_spawn` | world | done | static | code_trace | observed | 2026-09-17 | `BossService.check_for_boss_spawn()` (`region.trauma_score >= BOSS_SPAWN_TRAUMA_THRESHOLD`) was directly fixed and proven end-to-end in a real, unmodified corpus-world simulation by `TCK-20260914-LAIR-WORLD-BOSS-MATURITY-GATE-REACHABILITY` (closed) -- three real, compounding silent defects (tier-5 stat fallback, `ancient_core` loot registration, gate threshold values) found and fixed, whole chain proven end to end. **Bound 2026-09-20** (TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS- RESOLUTION): method-level, since `BossService` also defines `check_for_lair_spawn()`/ `resolve_boss_death()`, separate concerns this entry's own note does not describe. |
| `world_generation` | world | done | static | code_trace | observed | 2026-09-20 | TCK-20260920-MECHANISM-WORLD-FACTION-REGION-GROUP-UNBOUND-CLAIMS-RESOLUTION. `WorldCompiler.compile()` (called as a static method, not via instantiation -- an initial `WorldCompiler(` grep found nothing for this reason) is real and live, called from `src/domains/campaigns/orchestrator.py:747` for real campaign world setup, also from `src/worldbuilding/cli.py:282` and `src/lab/orchestrator.py`. Places real `BuildingState` instances (see `buildings`' own binding below), confirming this is the real world-setup entry point. |
| `adventure_routing` | entity | done | unverified | — | unverified | — | — |
| `belief_institution` | world | partial | unverified | — | unverified | — | — |
| `build_diversity` | entity | gap | unverified | — | unverified | — | — |
| `clan` | faction | gap | unverified | — | unverified | — | — |
| `commitment_pressure_consequences` | entity | partial | unverified | — | unverified | — | — |
| `committed_intentions` | entity | orphan | unverified | — | unverified | — | — |
| `concern_intake` | entity | done | unverified | — | unverified | — | — |
| `conversation` | entity | gap | unverified | — | unverified | — | — |
| `cooperation` | entity | done | unverified | — | unverified | — | — |
| `cultural_drift` | world | done | unverified | — | unverified | — | — |
| `declared_cognition_schema` | entity | orphan | unverified | — | unverified | — | — |
| `diplomacy` | faction | done | unverified | — | unverified | — | — |
| `entity_trade` | entity | gap | unverified | — | unverified | — | — |
| `event_interpretation` | region | done | unverified | — | unverified | — | — |
| `fame` | world | done | unverified | — | unverified | — | — |
| `fidelity_drift` | world | done | unverified | — | unverified | — | — |
| `gods_pantheon_blessings` | world | gap | unverified | — | unverified | — | — |
| `group_coordination` | group | orphan | unverified | — | unverified | — | — |
| `lair` | region | gap | unverified | — | unverified | — | — |
| `movement` | entity | done | unverified | — | unverified | — | — |
| `narrative_memory` | world | orphan | unverified | — | unverified | — | — |
| `nest` | region | gap | unverified | — | unverified | — | — |
| `quest_reward_distribution` | group | orphan | unverified | — | unverified | — | — |
| `race_collective_force` | faction | gap | unverified | — | unverified | — | — |
| `resource_harvesting` | world | orphan | unverified | — | unverified | — | — |
| `settlement_capacity_axis` | faction | gap | unverified | — | unverified | — | — |
| `social_memory` | faction | skeleton | unverified | — | unverified | — | — |
| `strategic_learning_bias` | entity | done | unverified | — | unverified | — | — |
| `strategic_redirection` | entity | orphan | unverified | — | unverified | — | — |
| `team_up` | entity | gap | unverified | — | unverified | — | — |
| `temporal_pressure` | entity | skeleton | unverified | — | unverified | — | — |
