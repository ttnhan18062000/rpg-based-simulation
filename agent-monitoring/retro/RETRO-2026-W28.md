# Agent Monitoring Retro — 2026-W28

_2026-07-06 → 2026-07-12_

---

## Run Summary

| Metric | Value |
|---|---|
| Total runs | 28 |
| Completed (DONE) | 27 (96%) |
| Gate failures | 1 |
| Avg duration | 0 min |
| Avg agents per run | 8.5 |
| Total agent calls | 229 |

## Gate Failure Breakdown

| Gate | Count | % of runs |
|---|---|---|
| NEEDS_HUMAN_INPUT | 1 | 3% |

## Reason Codes

| Reason | Count |
|---|---|
| dod_condition_failed | 11 |
| needs_human_input | 2 |

## Tier Distribution

| Tier | Count | Scoped | DONE count | DONE rate |
|---|---|---|---|---|
| epic | 3 | 0 | 3 | 100% |
| hotfix | 8 | 0 | 8 | 100% |
| standard | 17 | 0 | 16 | 94% |

## Agent Status Distribution

| Agent | Calls | ok | failed | blocked | skipped |
|---|---|---|---|---|---|
| architecture-reviewer | 38 | 24 | 4 | 0 | 10 |
| done-checker | 26 | 15 | 11 | 0 | 0 |
| finalizer | 7 | 7 | 0 | 0 | 0 |
| implement-ticket | 10 | 10 | 0 | 0 | 0 |
| implementer | 23 | 22 | 1 | 0 | 0 |
| investigator | 22 | 17 | 0 | 0 | 5 |
| orchestrator | 24 | 24 | 0 | 0 | 0 |
| parity-updater | 15 | 8 | 0 | 0 | 7 |
| plan-fixer | 1 | 1 | 0 | 0 | 0 |
| planner | 26 | 18 | 0 | 3 | 5 |
| test-scoper | 15 | 15 | 0 | 0 | 0 |
| ticket-scoper | 22 | 22 | 0 | 0 | 0 |

## Summary Quality

| Issue | Count |
|---|---|
| Empty summary (current schema) | 0 |
| Legacy-format records (summary field not applicable) | 0 |
| Truncated (>200 chars) | 0 |

## Slow Runs (> 30 min)

_No slow runs this period._

## Notes

**What failed most?** `done-checker` (Verify gate): 11 failed / 26 calls (42%) this week, all
`dod_condition_failed`. Only 1 run ended the week with a non-DONE final status (`NEEDS_HUMAN_INPUT`,
from a `planner` block), so the DoD failures are mostly self-corrected via re-run within the same
ticket rather than surfacing as a final gate failure — but a 42% first-pass DoD failure rate is the
single largest source of rework this period. `architecture-reviewer` is next (4 failed / 38 calls,
10 skipped for hotfix), well below done-checker's rate.

**What was slow?** Nothing — no runs over 30 min this week, avg duration reports as 0 min (durations
aren't being populated on most run records; `avg_dur` divides only over records with a `duration_s`
field, so this metric is likely under-sampled rather than genuinely instant — worth checking whether
`record_run.py` is consistently writing `duration_s`).

**Tagging / tag-registry-check feature (`TCK-20260706-SCOPE-TAG-REGISTRY-CHECK`,
`TCK-20260706-CREATE-TICKETS-TAG-CHECK`):** Zero `tag_registry_rejection` reason-code events all-time
(2359 events / 530 runs). The Scope-phase tag registry check has never once blocked a run. Given the
tag registry has grown steadily (verified `ai`/`documentation`/`workflows`/`skills` were already
registered before this week's two tickets), this reads as the check doing its job quietly rather than
being dead code — ticket-scoper agents are consistently using already-registered tags. No action
needed, but this gate remains functionally unverified against a real rejection; if a genuinely new tag
comes up, watch for the first real trigger to confirm the block path still works end-to-end.

**`mistag_warning` (security-adjacent tagging heuristic):** Also zero triggers all-time. Same caveat —
unverified against a true positive. No ticket this week or historically has had Related Code Areas
matching the credential/secret/password/etc. substrings without a `security` tag.

**Skill-selection (`suggested_skills`) feature:** Per a prior investigation event
(`TCK-20260705-WORKFLOW-TAG-TUNING-INVESTIGATION`, already on record), `suggested_skills` is log-only
and never consumed downstream — only the ticket's raw `tags` field (ground truth) drives the
Security-Review gate, by design per `TCK-20260705-WORKFLOW-SECURITY-GATE`'s review finding (the gate
must not depend solely on the optional LLM-derived field). Confirmed still true this week: `Scope`
summaries note `suggested_skills=[]` for both new hotfix tickets (tags `ai`/`documentation`/`workflows`/
`skills` don't map to any suggested skill), consistent with the four-tag mapping table.

**Security-Review gate — notable gap:** 0 `Security-Review` phase events in the *entire* monitoring
history (530 runs). No ticket has ever carried the `security` tag, so the gate added by
`TCK-20260705-WORKFLOW-SECURITY-GATE` has never actually fired in a live run — it's exercised only by
unit tests, not by real ticket traffic. Recommend a deliberate canary ticket (or the next genuinely
security-relevant change) to confirm the live trigger path still works, since a gate that has never
fired is the highest-risk kind of "working" code.

**What to change?** Pulled all 11 failing Verify summaries this week — two mechanical causes dominate,
both pre-Verify hygiene steps the implementer/finalizer keeps skipping rather than genuine scope
misses:
- **6/11** cite uncleaned `data/runs/` (one case had 139 leftover run directories) — the
  "clean up: `rm -rf data/runs/* reports/release_proof/*`" step in the project's After-Work workflow
  rule is being missed on the *first* pass consistently enough to be the top DoD blocker.
- **3/11** cite `make knowledge-index-update` not run after `docs/` edits.
- Remaining 2 are genuine content gaps (unfilled Completion Summary placeholder, undocumented
  follow-up findings) — not mechanical, no fix needed beyond normal review.
Concrete action: move the `data/runs`/`reports/release_proof` cleanup and the `docs/`-change
knowledge-index check earlier in the pipeline — e.g. as an implementer-phase step or a Test-phase
precondition — instead of relying on done-checker to catch it at Verify. This would cut the 42%
first-pass DoD failure rate roughly in half without touching any of the "real" gap conditions.

**What worked?** The two hotfix tickets closed this session (`TCK-20260708-SKILL-CREATE-TICKETS-WORKFLOW-TOOL-STALE`,
`TCK-20260708-SKILL-IMPLEMENT-TICKET-PHASE-DRIFT`) both went Scope → Implement → Test → Parity(skipped,
P0-safe) → Verify → Finalize with zero retries and zero DoD failures — the hotfix fast path and the
parity-skip-eligibility optimization (no `src/` files + `behavior_changed=false` + P0-scan-clear) are
working as intended for docs-only changes.
