# Test Plan — TCK-20260914-LAIR-WORLD-BOSS-MATURITY-GATE-REACHABILITY

**Blocked, same reason as plan.md.** Once a fix direction is chosen, minimum real coverage per the
ticket's own acceptance criteria: a constructed test proving spawn logic is correct once past the
gate (not just that the `if` branch is entered) — a spawned world boss/Lair occupant with real,
formidable stats (tier-5 defined, not falling back to tier-1), a real registered loot item, and the
gate's own two halves reaching their thresholds within a realistic run length. Filled in once a
direction is picked.
