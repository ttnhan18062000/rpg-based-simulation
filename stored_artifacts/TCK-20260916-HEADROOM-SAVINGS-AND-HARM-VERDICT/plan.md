# Plan — TCK-20260916-HEADROOM-SAVINGS-AND-HARM-VERDICT

1. Correct the ticket's own "on abandon: execute the revert runbook" instruction in place before
   acting on it — split into "abandon Phase 2 (needs no revert, nothing was enabled)" versus
   "whether to keep the MCP registration (the user's own call, a recommendation only)."
2. Re-read `TCK-20260916-HEADROOM-MCP-EXPLICIT-TRIGGER-TRIAL`'s own recorded savings table
   directly.
3. Re-run the harm-check baseline's own exact command at this ticket's own later moment (same
   window start) for a real, matched post-trial comparison.
4. State the payload-weighting finding: the one payload that compressed is an index this repo's
   own conventions already push agents to query rather than read whole; the two named primary
   targets (real monitoring JSONL, the large graph) measured 0%; the shipped retrieval-preference
   guidance further reduces whole-file-read opportunities.
5. State the harm check's own honest limits: a ceiling effect (no degradation headroom in a
   `done_rate=1.0`/zero-failures baseline), the hand-orchestration-dominance blind spot already
   named in the baseline ticket, and cache-hit degradation being genuinely unmeasurable with this
   repo's current tooling (not merely unattempted).
6. Record the verdict: abandon Phase 2 specifically because input-side savings are immaterial —
   the ticket's own named fork to evaluating Caveman's proxy, not a dead end for the epic.
7. Recommend (not decide) keeping the MCP registration itself, with the trade-off stated.
8. Update the epic ticket's own Acceptance Criteria and Status to reflect the verdict.
9. Check whether the folder-move rule applies (all children in
   `tickets/todos/headroom-context-compression-trial/` done) — it does not, since child 5 stays
   `BLOCKED` in that same folder.
10. Close via the standard hand-orchestrated path: `stored_artifacts/`,
    `record_hand_orchestrated_closure.py`, `docs/REGISTRY.yaml` regeneration.
