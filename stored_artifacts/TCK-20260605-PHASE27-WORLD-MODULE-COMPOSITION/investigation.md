# Investigation - Phase 27

Let's investigate how `WorldCompositionSpec` is currently defined and where we should put `ResolvedModuleContribution` and `WorldCompositionNormalizer`.
We'll check `src/worldassembly/schema.py` to see the existing schemas.
