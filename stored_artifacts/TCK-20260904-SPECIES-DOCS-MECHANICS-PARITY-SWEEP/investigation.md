---
status: active
layer: mechanics
authority: P1
audience: agent
ticket_id: TCK-20260904-SPECIES-DOCS-MECHANICS-PARITY-SWEEP
artifact_type: investigation
tags: [content, schema]
---

# Investigation — TCK-20260904-SPECIES-DOCS-MECHANICS-PARITY-SWEEP

## Mechanics files (5/5 checked, all had real hits)

All 5 listed mechanics files had genuine live-behavior-describing terminology hits (species-hostility
escalation in `02_combat_laws.md`, the imitation-fidelity stored-key + `SpeciesDefinition` citation and
bravery-bias correlation prose in `04_strategic_cognition.md`, the aptitude-multiplier concept in
`attribute_progression_contract.md`, the "same-species" pairing rule in `05_world_evolution.md`).
`content_usage_matrix.md` was already clean — it's auto-regenerated by `test_content_usage_matrix.py`
and was correctly regenerated as a side effect of children 1-3's own test runs.

## Parity ledger shards (7/7 checked)

Confirmed via a full grep-to-entry-ID mapping (not spot-checked) exactly which entries had real
"race" hits. Two categories found:

1. **Live-description entries citing now-renamed code/tests** (10 entries: PROG-120, COMB-317,
   STRAT-263, STRAT-251, INFRA-329, INFRA-400, SOC-257, SUB-380, SUB-381, SUB-382): `text`/
   `v2_evidence`/`test_path` updated via `tools/parity_ledger_writer.py::write_entry()` (never raw
   YAML edit) to reflect the real renamed symbols and, where the citation pointed at a file/test
   children 1-2 renamed (`test_race_relations_legality_wiring.py` →
   `test_species_relations_legality_wiring.py`, etc.), the corrected path — each cited test
   re-run and confirmed passing, not assumed.

2. **Blocked by a pre-existing, unrelated schema validation rule** (10 entries: STRAT-169,
   SOC-034/035/036/041/065, SUB-164/240/241, WORLD-019/047): all share `priority: P0` with
   `test_path: null` and a generic `v2_evidence: "Implementation proven via exhaustive checklist
   audit Phase 1-11"` placeholder — a legacy V1-era stub category (confirmed cross-referencing
   `docs/compliance/checklist.md`'s matching rows, which themselves cite a now-nonexistent file
   layout: `src/ai/personality.py`, `src/world/spawn_config.py`,
   `src/systems/world_systems/generator.py` — none of these paths exist in the current `src/`
   tree). `docs/parity_ledger/schema.json`'s own rule (mirrored in `parity_ledger_writer.py`'s
   `validate_entry()`) requires `priority: P0` entries to carry a non-null `test_path` — these 10
   entries already violate that rule, independent of and predating this ticket. `write_entry()`
   correctly refuses to write them without a real test_path, and fabricating one to unblock the
   write would repeat the exact SUB-327 fabricated-citation mistake this repo's own precedent
   warns against. Left unchanged — a pre-existing, larger, separate data-quality problem (part of
   the same ~1317-entry `missing_test_path` health-finding population reported by
   `tools/parity_index.py health`), not this ticket's to fix. The `text` field's own "race"
   wording is real (describes the same creature-classification concept: NPC kind mapping,
   terrain species labels, spawn-tier defaults, trait prefixes) but stays stale until a real
   test_path is authored for these entries, which is out of scope here.

## Remaining `docs/` corpus (narrowed from the epic's 81-file candidate list)

Grepped the full `docs/` tree (excluding `docs/archive/`, already-covered mechanics/parity_ledger)
and categorized every hit:

- **Fixed** (16 files): live architecture/audit/guideline/contract docs describing real, still-
  existing code paths with stale terminology —
  `docs/architecture/2026-08-10-cognition-driven-adventure-eligibility-design.md`,
  `docs/audits/{D01_rpg_feature_impact,D21_entity_lifecycle_foundation_layers,
  D22_dormant_content_wiring}.md`, `docs/content/pipeline_contract.md`,
  `docs/core/{attributes_and_classes,items_and_inventory}.md`,
  `docs/guidelines/{fallback_retirement_criteria,intentional_divergences}.md`,
  `docs/simulation/quest_contract.md`, `docs/simulation_quality/{entity_lifecycle_score,
  event_type_coverage}.md`, `docs/world/assembly_contract.md`, plus the epic's own idea-37-name
  citations in `docs/plans/rpg_design_roadmap/{rpg_design_roadmap,rpg_m9_corpus_test_coverage_epic,
  rpg_m2_foundational_systems_epic}.md`. All confirmed `status: active` frontmatter, not archived/
  historical.

- **Left alone, distinction recorded** (deliberate scope boundary, documented here per the
  ticket's own Acceptance Criteria):
  - `docs/brainstorm/**` (including `docs/brainstorm/codex/**`): treated as frozen historical
    investigation record, same spirit as the epic's own Out-of-Scope treatment of the 3 named
    brainstorm HTML sources — these `.md` review/proposal docs narrate what was discussed/decided
    at a point in time (e.g. "Idea 37 — race relations, with careful content/balance design"),
    and rewriting them risks silently revising the historical record rather than reconciling it
    (the established precedent from `2026-08-27-brainstorm-plan-crosswalk-review.md` is to
    reconcile in `rpg_design_roadmap.md`, not edit the investigation record itself).
  - `docs/plans/rpg_design_roadmap/{rpg_m2_foundational_systems_epic,rpg_m4_beyond_city_epic,
    rpg_m9_corpus_test_coverage_epic,rpg_direction_alignment_audit,
    rpg_idea66_region_place_rebuild_plan}.md`'s remaining free-form descriptive prose
    ("race-diversity corpus gap," "race composition," "race data," "13-race roster," "race pairs,"
    "race-relations matrix/pattern" as a generic noun phrase rather than idea 37's literal name):
    large, actively-evolving planning-epic documents describing proposed/future work, not
    committed current behavior. Left unchanged, distinct from the idea-37-name citations (which
    were fixed, per the epic's explicit Scope item 3) and from live-code citations (also fixed).
    Mass-editing this descriptive narrative without a deep per-epic review of intent was judged
    lower-value and higher-risk than the mechanics/parity/live-contract fixes already made.
  - `rpg_design_roadmap.md`'s own "## Terminology correction: Race -> Species (2026-09-04)"
    section (lines 551-568): this IS the epic's own scoping-time reconciliation note — a
    historical decision record describing the situation *as found when the epic was scoped*, not
    live current-code documentation. Left unedited; it already correctly narrates the decision.
  - Every "race condition"/concurrency hit (kernel/worker/replay/observability docs) — confirmed
    unrelated to the creature-classification concept, not touched.
  - `docs/compliance/checklist.md`'s matching "race tier" rows: same legacy V1 stub category as
    the blocked parity-ledger entries above (confirmed via the nonexistent source-file citations)
    — left unchanged for the same reason.
  - `docs/audits/D10_test_coverage.md`'s `TCK-20260624-FIX-RACE-TESTS` citation: a historical
    ticket-ID literal (grouped with `FIX-WORKER-SHUTDOWN`/`PERF-GUARD-INFRA` — concurrency-test
    fixes, not species), not this concept at all.

## Validation

`tools/validate_frontmatter.py docs/` reports 355 pre-existing violations across 858 files — cross-
referenced every one of this ticket's 25 edited files against that list: zero appear. All 355 are
pre-existing, unrelated repo-wide frontmatter debt (archive status mismatches, missing frontmatter
in brainstorm/codex files, `docs/guides/` missing `status`, `docs/plans/idea_*.md`'s invalid
`status: idea` value, etc.) — none introduced or touched by this ticket.

`tools/parity_index.py build` and `health` both run clean (exit 0) after all edits; the `health`
findings reported (missing_test_path, legacy_unstructured, etc.) are the same pre-existing
~1317-entry baseline population, not new findings from this ticket's edits.
