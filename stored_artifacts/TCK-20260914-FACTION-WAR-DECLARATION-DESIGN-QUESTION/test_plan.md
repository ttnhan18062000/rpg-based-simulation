# Test Plan — TCK-20260914-FACTION-WAR-DECLARATION-DESIGN-QUESTION

**Blocked, same reason as plan.md.** Once a fix direction is chosen, minimum real coverage per the
ticket's own acceptance criteria would need to prove, in a real run: at least one faction pair
actually reaches `WAR` (not just unit-level construction of the boundary condition), and that the
new mechanism(s) breaking the circularity (military-strength divergence and/or a non-war territory/
tension producer) don't themselves introduce a new unreachable precondition. Filled in once a
direction is picked.
