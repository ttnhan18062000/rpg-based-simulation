# Implementation Plan for TCK-20260408-PH1-2-DS-REMEDIATION

## Steps

### Step 1: Unify Reputation Model
1. Inspect `src/core/models/reputation.py` and `src/core/models/life_events.py` for differences.
2. Merge essential fields into `src/core/models/life_events.py:ReputationProfile`.
3. Delete `src/core/models/reputation.py`.
4. Update all imports in `src/core/entities/entity.py`, `src/core/logic/reputation_service.py`, etc.
5. Update `IdentityAspect` to use the unified model.

### Step 2: Fix Inspection & Explanation
1. Update `src/api/schemas.py` to include `BeliefRecordSchema` (if missing or incorrect).
2. Modify `EntityPresenter.to_full_schema` to serialize `entity_memory` as a list of `BeliefRecordSchema`.
3. Modify `AIPresenter.get_explanation` to fix the distance calculation `pos -> belief.pos`.

### Step 3: Authoritative Gossip Ordering
1. Modify `ActionSystem.apply_action_state_transitions`.
2. Move `cls._process_social_interpretation` and `cls._process_proximity_gossip` BEFORE `cls._apply_updates`.
3. Ensure that updates generated by these passes are included in the cumulative `all_updates` list.

### Step 4: Diverse Archetype Seeding
1. Locate the entity builder/generator (e.g., `src/core/world/builder.py` or similar).
2. Ensure that `archetype` is not allowed to silently default to `BALANCED` for all entities.
3. Implement a sampler that assigns archetypes based on a configurable distribution.

### Step 5: Cleanup Legacy personality
1. Delete `src/core/logic/personality.py` (Ocean-based).
2. Check for any remaining references and remove them.

### Step 6: Verification
1. Run existing tests in `tests/behavioral/` or similar.
2. Create new tests specifically for:
    - Belief-based distance calculation in explanations.
    - Authoritative gossip application in a single tick.
    - Diverse archetype assignment.
