---
status: historical
layer: observability
authority: P2
audience: agent
ticket_id: TCK-20260904-ARTIFACT-RETENTION-CLASSIFICATION
artifact_type: investigation
tags: [ai, agent-monitoring, data-quality, documentation]
---

# Investigation — TCK-20260904-ARTIFACT-RETENTION-CLASSIFICATION

## Current Behavior

This ticket is documentation-only (M2 of `docs/plans/agent_infrastructure/ai_first_hardening_epics/telemetry_retention_epic.md`). There is no source-code component to describe; "current behavior" here means the current, uncoordinated state of each artifact class's retention treatment.

- **`agent-monitoring/data/YYYY-Www/{runs,events,tools}.jsonl`** — already fully classified and resolved by the shipped `TCK-20260902-MONITORING-WEEKLY-SHARDING-EPIC` and `TCK-20260903-MONITORING-UNIFIED-WEEKLY-EPIC`. Both verified `DONE` in this working copy:
  - `tickets/done/agent-monitoring-weekly-sharding/TCK-20260902-MONITORING-WEEKLY-SHARDING-EPIC.md:17-18` — `## Status` / `DONE`.
  - `tickets/done/agent-monitoring-unified-weekly-data/TCK-20260903-MONITORING-UNIFIED-WEEKLY-EPIC.md:19-20` — `## Status` / `DONE`.
  - Referential-integrity tool `tools/agent-monitoring/verify_referential_integrity.py` exists and is committed, confirming the epic doc's claim of a shipped, report-only join checker.
  - The epic doc's own M1 note (`docs/plans/agent_infrastructure/ai_first_hardening_epics/telemetry_retention_epic.md:20-57`) is corroborated by these two DONE tickets — the "M1 superseded" claim is real, not an unverified assertion.
- **`stored_artifacts/{id}/`** and **`tickets/done/*.md`** — committed, never pruned; no formal retention statement exists anywhere in `docs/` today. Treated as permanent by convention (referenced constantly by `docs/REGISTRY.yaml`, `tools/registry_query.py`, and every ticket's "Prior Work" investigation step), never explicitly written down as a retention decision.
- **`agent-monitoring/retro/RETRO-*.md`** — committed, generated by `tools/agent-monitoring/generate_retro.py`; same "permanent by convention, not by written policy" gap.
- **`tickets/working_log.csv`** — committed, append-only in principle, but currently data-quality-broken (per this ticket's own summary and the epic doc's table). The parser/cleanup fix is out of scope here and tracked separately by `tickets/todos/ai-first-hardening-h1-h2-followon/TCK-20260904-WORKING-LOG-CSV-PARSER.md` (confirmed to exist, still in `tickets/todos/`, i.e. not yet implemented).
- **`graphify-out/`** — gitignored build output of the `graphify` CLI. Verified directly (not trusted from the epic doc's own claims):
  - `.gitignore:256-259` — four commented-out (disabled) narrower ignore rules for specific `graphify-out/` subpaths (`cache/*`, `.graphify_uncached.txt`, `.graphify_root`, `.graphify_python`) — these lines are present but inert (commented).
  - `.gitignore:260` — `graphify-out/*` (the actual active ignore rule).
  - `.gitignore:261` — `src/graphify-out/` (a second, path-qualified ignore rule).
  - The ticket's own Scope text cites "lines 260-261" for the *active* rules — confirmed exactly correct. The epic doc's earlier "lines 256-261" is also accurate but includes the four inert commented lines above the active ones; 260-261 is the more precise citation for "the ignore rule" itself.
  - `grep -rn "graphify-out" .github/workflows/` returns zero matches — confirmed zero CI references.
  - `git ls-files graphify-out/` returns zero tracked files — confirmed nothing under this path is committed.
  - `tests/tools/test_code_health_impact.py:26-40` documents and enforces this directly: a code comment states "graphify-out/ is entirely gitignored (never committed)" and a `_requires_graphify` skip marker (`shutil.which("graphify") is not None and (_REPO_ROOT / "graphify-out" / "graph.json").exists()`) gates 4 real-path tests, explicitly designed so CI without a prebuilt graph skips rather than fails.
  - `tickets/done/TCK-20260818-HOTFIX-GRAPHIFY-CLI-TEST-MISSING-INDEX-SKIP.md` exists and is `DONE` (confirmed via `## Status` / `DONE`), corroborating that this skip-if-missing pattern was a deliberate, already-landed fix for exactly this gitignored/rebuildable relationship.
- **`knowledge-index/`** — gitignored local semantic-search cache. Verified directly:
  - `.gitignore:263-264` — a comment ("Knowledge search index (local only — rebuild with: make knowledge-index)") followed by the active rule `knowledge-index/` at line 264. The ticket's citation of line 264 is exactly correct.
  - `Makefile:331` — the comment `# developer env only — not CI` immediately precedes the `knowledge-index:` target block; the target itself (`Makefile:334`) also repeats "(developer env only — not CI)" in its own `##` help text. Both citations in the ticket (`Makefile:331`) and the epic doc are accurate.
  - `docs/guidelines/agent_working_environment.md:267-280` — the existing "Other Local, Gitignored Caches (Knowledge Gateway MCP)" table **already has a `knowledge-index/knowledge.db` row** (line 278, citing `.gitignore:264` verbatim) and a second `knowledge-index/retrieval_cache.db` row (line 280). Confirmed present exactly as the ticket asserts.
  - `tools/hooks/post-commit-reindex.sh` exists (cited in ticket's Related Code Areas) and pairs with `docs/guidelines/agent_working_environment.md:82-87`'s description of an installable post-commit hook that runs `make knowledge-index-update` and "self-skips if `knowledge-index/` does not exist" — i.e. rebuild automation already exists and is documented.
- **`.claude/current_run` sidecar(s)** — already ephemeral/run-scoped by design (per the M2 table in the epic doc); no change needed. This matches the project's own memory note that `.claude/current_run` is a single shared file whose cross-session contamination was previously a live bug (unrelated to retention classification, not re-litigated here).

## Mechanics / Engine Constraints

None. This ticket touches no `src/` code, no simulation formula, and no `docs/mechanics/` or `docs/engine/` law. It is pure documentation/process classification. No Mechanics Bible chapter or engine contract constrains what this doc can say.

## Docs Requiring Update

- `docs/guidelines/artifact_retention_classification.md`: new committed doc — the ticket's primary deliverable, containing the 8-class retention-classification table with the two open questions resolved.
- `docs/guidelines/agent_working_environment.md`: recommended (see Risks/Recommendations below) — add a `graphify-out/graph.json` row to the existing "Other Local, Gitignored Caches" table (`docs/guidelines/agent_working_environment.md:267-280`), which currently covers only the 3 Knowledge Gateway MCP caches and is missing this adjacent, also-gitignored, also-rebuildable cache. If the implementer defers this instead of doing it in this ticket, this bullet's status should be re-evaluated — see the explicit go/defer recommendation in Risks and Open Questions.
- `docs/plans/agent_infrastructure/ai_first_hardening_epics/telemetry_retention_epic.md`: this ticket closes out M2 of the epic; the epic doc's M2 row/section should be updated to note "M2 — shipped, see TCK-20260904-ARTIFACT-RETENTION-CLASSIFICATION" the same way M1 already records its own shipped/superseded status inline, so the epic doc does not read as still-open once this ticket lands.

The `docs/agent-monitoring/schema.md` doc (path: `docs/agent-monitoring/schema.md`, under `docs/agent-monitoring/`) is not required to change for this ticket: it documents the `agent-monitoring/data/YYYY-Www/` schema, which M1 (already shipped, already reflected there) fully covers. This ticket's M2 scope is the *other* 7 artifact classes, none of which that schema doc describes.

The `docs/observability/retrieval_retention_redaction_policy.md` doc (path: `docs/observability/retrieval_retention_redaction_policy.md`) is not required to change for this ticket: it is a narrowly-scoped operational data-handling policy for the Knowledge Gateway MCP's own retrieval cache/event fields (MAY/PROHIBITED field lists), a different and already-settled question from this ticket's repo-wide artifact-class retention classification. It is cited below only as directory-placement precedent, not as a doc this ticket touches.

## Parity Ledger Overlap

None. Searched `docs/parity_ledger/*.yaml` for "retention", "graphify-out", "knowledge-index", and "artifact" — the only hits are unrelated existing entries about certification-report CI-artifact upload/retention (`infrastructure.yaml`, e.g. the `TCK-20260627-P2M-CI-ARTIFACTS` entry around line 2834) and `ArtifactBudgetRegistry` size-budget entries (line 2154) — both are pre-existing, already-`verified` entries about a different subsystem (certification proof artifacts, not the repo-artifact-class retention this ticket classifies). No P0 entry is touched. No new parity ledger entry is warranted since this ticket changes no simulation-observable behavior.

## Prior Work

- `tickets/done/agent-monitoring-weekly-sharding/TCK-20260902-MONITORING-WEEKLY-SHARDING-EPIC.md` and `tickets/done/agent-monitoring-unified-weekly-data/TCK-20260903-MONITORING-UNIFIED-WEEKLY-EPIC.md` — both confirmed `DONE`; together they are M1's real, shipped substitute. No further action needed on the M1 scope.
- `tickets/done/TCK-20260818-HOTFIX-GRAPHIFY-CLI-TEST-MISSING-INDEX-SKIP.md` — confirmed `DONE`; established the precedent that `graphify-out/graph.json` being absent/gitignored is a legitimate, already-handled "skip, don't fail" condition in tests, reinforcing the graphify-out/ recommendation below.
- `tickets/done/TCK-20260728-RETRIEVAL-RETENTION-REDACTION.md` (registry tags: `[ai, observability, agent-monitoring]`) — the direct precedent for a retention-style doc's placement decision. Its own `stored_artifacts/TCK-20260728-RETRIEVAL-RETENTION-REDACTION/plan.md:129-133` states the doc "lands at `docs/observability/retrieval_retention_redaction_policy.md` rather than `docs/ai/*_decision.md`... by direct structural analogy to `docs/observability/loki_label_policy.md`" — i.e. `docs/observability/` is reserved in this repo for *operational data-handling policies of a specific subsystem's own live telemetry/log shape* (label cardinality, cache field redaction), not repo-wide artifact-lifecycle classification spanning ticket/doc/index artifacts that have nothing to do with observability instrumentation. This precedent argues *against* placing the new doc under `docs/observability/` (see doc-placement decision below).
- `docs/guidelines/agent_working_environment.md`'s "Other Local, Gitignored Caches" table is the closest existing structural precedent for *this* ticket's table shape (artifact / real path / what it is / gitignored? / how to rebuild) — reused as the template for the new doc's table.

## Risks and Open Questions

1. **Doc location — resolved.** Recommendation: `docs/guidelines/artifact_retention_classification.md`, not `docs/observability/`. Justification: `docs/observability/` in this repo (confirmed via directory listing: `agent_ops_dashboard_contract.md`, `decision_trace_contract.md`, `hard_law_monitor.md`, `loki_label_policy.md`, `prometheus_metrics.md`, `read_model_service_contract.md`, `retrieval_retention_redaction_policy.md`) is consistently used for contracts/policies about the *live observability subsystem's own* runtime behavior and data shape — dashboards, event bus schemas, invariant monitors, one subsystem's cache/log field policy. This ticket's doc instead classifies retention across `stored_artifacts/`, `tickets/done/`, `working_log.csv`, `graphify-out/`, `knowledge-index/`, `.claude/current_run` — a cross-cutting engineering-process/workflow concern, most of which (tickets, stored artifacts, dev-tool caches) is not observability telemetry at all. `docs/guidelines/` (confirmed via directory listing: `agent_working_environment.md`, `design_patterns.md`, `fallback_retirement_criteria.md`, `frontmatter_schema.md`, `intentional_divergences.md`, `tag_taxonomy.md`) is explicitly described in `docs/guidelines/README.md` as "Standards and conventions for developers" — a direct fit, and `agent_working_environment.md` already hosts the structurally nearest-analogous table (local gitignored cache classification) in this exact directory. `layer: guidelines` also already exists in `registries/layer_registry.jsonl` with the description "Cross-cutting process/convention docs (tagging, patterns, taxonomy)" — a closer semantic match than `layer: observability`'s "Agent monitoring, dashboards, event bus, telemetry".
2. **graphify-out/ open question — resolved.** Recommendation: **build-output / gitignored, no change needed.** Evidence: zero CI references, zero tracked files, an already-committed test-skip pattern (`tests/tools/test_code_health_impact.py`) and hotfix ticket (`TCK-20260818-HOTFIX-GRAPHIFY-CLI-TEST-MISSING-INDEX-SKIP`) that both treat "gitignored and possibly absent" as the correct, deliberate state, not a gap to fix. Classification: **Ephemeral / Run-scoped** (rebuildable derived index, same category as `knowledge-index/`), recommended treatment: keep gitignored, rebuild via `graphify update .` per `CLAUDE.md`'s own Graphify Integration section.
3. **knowledge-index/ open question — resolved.** Recommendation: **build-output / gitignored, no change needed**, same as graphify-out/. Evidence: `.gitignore:264`, `Makefile:331`'s explicit "developer env only — not CI" comment, and existing rebuild automation (`make knowledge-index`, `make knowledge-index-update`, `tools/hooks/post-commit-reindex.sh`) all already documented in `docs/guidelines/agent_working_environment.md`. Classification: **Ephemeral / Run-scoped**.
4. **Whether to add a graphify-out/ row to `agent_working_environment.md`'s existing table now vs. defer — a genuine judgment call, resolved as: do it now, in this ticket.** The existing table's intro text says "one of three local, disposable caches" (`docs/guidelines/agent_working_environment.md:269`) and the section header is scoped `(Knowledge Gateway MCP)` — `graphify-out/` is not an MCP cache, so a literal one-row insert would leave the "one of three" sentence and the parenthetical section title inconsistent with the new row. This is a real but small edit (change "one of three" to "one of four", broaden or footnote the parenthetical scope claim, add one table row citing `.gitignore:260-261` and `graphify update .`) — not a one-line drop-in. Given the evidence is already fully gathered for the new committed doc (no extra investigation cost) and the project's own prior guidance (`feedback_agent_infra_scope_openness`) that agent-infra doc fixes may go beyond a minimal patch rather than accumulate "flagged as future work" notes, the recommendation is to make this small edit in the same ticket rather than deferring it. If the implementer disagrees and defers anyway, the ticket's Out of Scope section requires this be named explicitly as a follow-up (owner: whoever next edits `docs/guidelines/agent_working_environment.md`, trigger: next graphify-out/-adjacent ticket) rather than left silent.
5. **working_log.csv row must not duplicate or block on `TCK-20260904-WORKING-LOG-CSV-PARSER`.** Confirmed this sibling ticket exists at `tickets/todos/ai-first-hardening-h1-h2-followon/TCK-20260904-WORKING-LOG-CSV-PARSER.md` and is not yet implemented (still in `tickets/todos/`, not `tickets/done/`). The new doc's `working_log.csv` row must classify it (Institutional / "keep permanent once parser is fixed") and reference the sibling ticket by ID only — no duplication of its scope, no gating this ticket's completion on it landing first, consistent with the ticket's own explicit instruction.
6. **M3 boundary.** The epic doc's M3 (ownership/lifecycle documentation) explicitly shares a References/Out-of-Scope section with M2 in the source epic doc but is a separate milestone with its own ticket — confirmed the ticket's Out of Scope section already states this; no conflation risk found, just flagging it stays correctly excluded.

## Anti-Drift Hazards

- Do not let the new doc's `graphify-out/`/`knowledge-index/` rows silently drift into re-litigating the open questions as still-open — the acceptance criteria and this ticket's own Request Summary explicitly require resolving them now with the evidence already gathered, not carrying them forward a second time (the epic doc itself flags this as a risk: "this milestone's output should resolve it with real evidence, not carry the 'open question' forward a second time").
- Do not implement the `working_log.csv` parser/cleanup fix as part of this ticket — it is explicitly out of scope and tracked by the sibling ticket.
- Do not conflate M3 (ownership/lifecycle documentation) with this ticket's M2 scope, despite the shared References/Out-of-Scope boundary in the source epic doc.
- Do not treat `docs/observability/retrieval_retention_redaction_policy.md` as a doc this ticket must update — it is a different, already-settled question (retrieval cache field redaction) that only serves as directory-placement precedent here.
- If the `agent_working_environment.md` table edit (Risk/Recommendation 4) is done, do not silently drop or reword the "one of three" framing without updating it to match the new row count — an inconsistent count is the kind of small doc defect that erodes trust in the table.
