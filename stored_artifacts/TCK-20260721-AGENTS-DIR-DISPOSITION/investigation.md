---
status: historical
layer: ai
authority: P2
audience: agent
ticket_id: TCK-20260721-AGENTS-DIR-DISPOSITION
artifact_type: investigation
tags: [ai, workflows, process-improvement]
---

# Investigation — TCK-20260721-AGENTS-DIR-DISPOSITION

## Current Behavior

### `.agents/` inventory (4 top-level paths, 42 files)

**`.agents/task.md`** — 0 bytes. Trivial archive/retire candidate; carries no content to lose.

**`.agents/rules/` (9 files)** — an earlier, simpler precursor of `CLAUDE.md`'s current rule set:
- `AGENTS.md` (168 lines), `workflow.md` (49 lines), `ticket.md` (105 lines), `testing.md` (32 lines), `architecture.md` (50 lines), `authoritative_mechanics.md` (20 lines), `done.md` (20 lines), `graphify.md` (14 lines) are each near-verbatim earlier drafts of sections that now live, materially extended, in `CLAUDE.md` (compare e.g. `.agents/rules/workflow.md`'s "Before/During/After Work" against `CLAUDE.md`'s "Workflow Rule" — same skeleton, but `CLAUDE.md` adds tag/layer registries, `docs/REGISTRY.yaml` regeneration, `agent-monitoring/` requirements, and tier routing that `.agents/rules/*` never mentions). `ticket.md`'s required-section list omits `## Tier` and `## Priority` entirely and has no frontmatter block at all — confirming an older ticket shape.
- `authoritative_mechanics.md:18` cites a "17-phase apply sequence"; `CLAUDE.md` and the current `docs/engine/authoritative_pipeline.md` describe a 32-phase refinement sequence — a concrete numeric drift, not just "generically older."
- `engine_contracts.md` (224 lines) is the one exception: it contains prescriptive per-subsystem "when kernel loop / authoritative pipeline / performance contract changes, update doc X and parity ledger entry Y" detail that `CLAUDE.md`'s current Engine Contracts section (a summary table only) does not have. This file is not a strict subset of current `CLAUDE.md` content — it has operational detail that would need a home if retired outright.

**`.agents/skills/` (18 dirs)** vs **`.claude/skills/` (16 dirs)**:
- 11 names overlap: `api-design-principles`, `architecture`, `backend-testing`, `brainstorming`, `debugging-strategies`, `doc-coauthoring`, `frontend-design`, `prompt-builder`, `python-performance-optimization`, `python-testing-patterns`, `test-driven-development`. Sampled `api-design-principles/SKILL.md` — byte-identical (`diff` empty) between the two trees, but the ticket's own Assumption warns this framing is "only partially accurate" for the full set; only one pair was diffed here.
- 5 names exist only in `.claude/skills/`: `agent-monitoring-retro`, `create-tickets`, `implement-epic`, `implement-ticket`, `simq-audit` — these are the project's own live workflow-support skills, never duplicated into `.agents/`.
- 7 names exist only in `.agents/skills/`: `clean-code`, `codebase-search`, `code-review`, `create-skill`, `graphify`, `receiving-code-review`, `requesting-code-review`. Of note: `.agents/skills/graphify/SKILL.md` is superseded by a **user-level global** skill (`~/.claude/skills/graphify/SKILL.md`, registered in this session's own global `CLAUDE.md`), not a project-level `.claude/skills/` entry — its correct disposition is "superseded by a different scope," not "needs migrating into `.claude/skills/`."
- No currently-passing test asserts any `.agents/skills/*/SKILL.md` content (confirmed below) — reclassifying any skill subdirectory carries no test-breakage risk.

**`.agents/workflows/` (8 files, listed in ticket's Related Code Areas)** — these are *not* duplicates of `.claude/workflows/*.js`. They are short (10–26 line) YAML-frontmatter-only declarative contracts (`name`, `description`, `allowed_actions`, `forbidden_actions`, `input_schema`, `output_artifacts`), e.g. `.agents/workflows/generate-simulation-setup.md`. `.claude/workflows/*.js` are the real executable orchestration (90–1300 lines each, e.g. `implement-ticket.js` at 1300 lines). Confirmed via grep: neither `.claude/workflows/generate-simulation-setup.js` nor `prepare-simulation-execution.js` reference `allowed_actions`, `forbidden_actions`, `.agents`, or `WorkflowRegistry` anywhere — the declarative contract in `.agents/workflows/*.md` is parsed by `WorkflowRegistry` but enforced nowhere at runtime.

### `WorkflowRegistry` (`src/lab/registry.py:31-111`)

- `WorkflowSkill` (registry.py:8-29) is a frozen, `extra="allow"` Pydantic model for a workflow/skill contract (`name`, `purpose`, `input_schema`, `allowed_actions`, `forbidden_actions`, `output_artifacts`, `approval_required`, `context_budget`, `failure_behavior`).
- `WorkflowRegistry.__init__` (registry.py:36-40) takes `workflows_dir`/`skills_dir` paths. `scan_and_register()` (registry.py:65-93) globs `*.md` under `workflows_dir` and `*/SKILL.md` under `skills_dir`, parsing YAML frontmatter (`_parse_frontmatter`, registry.py:42-63) into `WorkflowSkill` instances. `get_workflow()`/`get_skill()` (registry.py:95-105) raise `ValueError` for unknown names.
- Exported in `src/lab/__init__.py:60-63` and `__all__` (line 150), alongside the rest of the "Lab" simulation-experimentation package (`ScenarioLabOrchestrator`, `GenerateSimulationSetupWorkflow`, etc. — this is the project's simulation-research harness, unrelated to the meta `.claude/workflows/*.js` ticket pipeline, despite parallel naming).
- **Zero production instantiation confirmed.** `grep -rn "WorkflowRegistry" src/` returns only the class definition and its two `__init__.py` export lines — no call site. `graphify query "WorkflowRegistry"`'s 33-node BFS subgraph shows every `imports`/`calls` edge from `WorkflowRegistry` terminating at `test_workflow_registry.py`; none terminate in any other `src/` module. `src/lab/cli.py` (the `rpg-lab` console-script entry point) never references `registry` or `WorkflowRegistry`. `src/lab/workflows.py` (the 8 real workflow classes: `GenerateSimulationSetupWorkflow` L87, `PrepareSimulationExecutionWorkflow` L596, `RegisterSimulationResultWorkflow` L868, `CompactSimulationDataWorkflow` L1116, `InvestigateSimulationResultWorkflow` L1571, `ProposeSimulationEnhancementsWorkflow` L2062, `UpdateSimulationKnowledgeWorkflow` L2324, `RevertSimulationKnowledgeWorkflow` L2589) never reference `allowed_actions`, `forbidden_actions`, or `WorkflowRegistry`.
- **Planned wiring was abandoned, not merely never attempted.** `stored_artifacts/TCK-20260524-LAB-GUARDRAILS/plan.md:30` explicitly proposed: "Integrate the dynamic frontmatter loading via `WorkflowRegistry` in all workflow workflows (e.g. `GenerateSimulationSetupWorkflow`, `PrepareSimulationExecutionWorkflow`, `InvestigateSimulationResultWorkflow`)." That ticket's own `investigation.md:15` repeats the same intent. But `TCK-20260524-LAB-GUARDRAILS`'s **delivered** `Files Changed` (`src/lab/context.py`, `src/lab/workflows.py`, two new test files) never touches `registry.py`, and `src/lab/context.py`'s guardrail limits (`max_evidence_packs`, `max_known_issues`, `max_previous_runs` — context.py:57-58,63-64,159-160,165-166) are sourced from a plain `limits: dict` parameter passed directly into the builder methods, not from `WorkflowRegistry`-parsed frontmatter. This is direct evidence the registry's intended production consumer was scoped, then dropped in favor of a simpler mechanism — not that wiring is merely pending.

### `test_real_registry_contracts` (`tests/unit/lab_agent/test_workflow_registry.py:92-117`)

Instantiates `WorkflowRegistry(project_root / ".agents" / "workflows", project_root / ".agents" / "skills")` against the **real, live** directories (not fixtures) and asserts:
- `"GenerateSimulationSetup"` and `"PrepareSimulationExecution"` are present in `list_workflows()`.
- `PrepareSimulationExecution.allowed_actions` contains `"resolve_specs"`; `forbidden_actions` contains `"execute_simulation_command"` and `"run_simulation_directly"`.
- `GenerateSimulationSetup.allowed_actions` contains `"create_draft_specs"`; `forbidden_actions` contains `"run_simulation"`, `"promote_trusted_specs"`, `"update_rulebooks_directly"`.

Ran the full file directly: `.venv/bin/python3 -m pytest tests/unit/lab_agent/test_workflow_registry.py -v` → **4 passed** (`test_scan_and_register_success`, `test_workflow_fields_validation`, `test_unknown_workflow_rejected`, `test_real_registry_contracts`), confirming the ticket's baseline assumption is current as of this investigation.

## Mechanics / Engine Constraints

Not applicable. This ticket concerns agent-infrastructure/dev-tooling disposition, not simulation mechanics or the engine kernel/authoritative pipeline. No chapter of `docs/mechanics/` or contract in `docs/engine/` constrains this work.

## Parity Ledger Overlap

None. Searched `docs/REGISTRY.yaml` for `lab/registry`, `WorkflowRegistry`, and `.agents` — the only match is the `TCK-20260524-WORKFLOW-REGISTRY` ticket entry itself (a ticket-index row, not a parity-ledger row). No file under `docs/parity_ledger/*.yaml` references `src/lab/registry.py`, `WorkflowRegistry`, or `.agents/`. This confirms the ticket's own expectation ("likely none").

## Prior Work

- **`TCK-20260524-WORKFLOW-REGISTRY`** (done, `tickets/done/TCK-20260524-WORKFLOW-REGISTRY.md`) — built `WorkflowRegistry`/`WorkflowSkill` and populated `.agents/workflows/*.md` as Phase 14 "M93" work. Source design doc `docs/archive/sim-obs-test/lab_phase14.md:266-282` states the explicit rationale: adopting "the identical Antigravity-Style Markdown Spec & Skill Model" instead of a static config file, specifically "to guarantee zero drift between the agent's instructions and the Python backend's enforcement gates." No enforcement gate was ever built to consume it — the rationale's second half was never delivered.
- **`TCK-20260524-LAB-GUARDRAILS`** (done, `tickets/done/TCK-20260524-LAB-GUARDRAILS.md`) — planned (per its own `plan.md`/`investigation.md`) to wire `WorkflowRegistry` into context-budget enforcement; delivered a simpler plain-dict `limits` parameter instead, and its `Files Changed` list confirms `registry.py` was never touched. This is the strongest single piece of evidence that the registry's intended call site was considered and abandoned.
- **`TCK-20260705-SIX-SKILLS-INVESTIGATION`** (done) — methodological precedent for this ticket: a classification-only investigation (verdict per item, evidence-backed) that deliberately did **not** implement any wiring/retirement itself, deferring execution to a more general follow-on mechanism. Worth mirroring: this ticket's own AC framing ("determination... does not assume either answer in advance") matches that precedent's discipline.
- **`docs/ai/agent_infrastructure_audit.md`** — a broader July audit that separately flagged "Skill catalog shows signs of not being curated" (unrelated `.claude/skills/frontend-design`/`prompt-builder` zero-invocation concern) — adjacent maintenance debt, not this ticket's scope, but worth not conflating.
- **`docs/plans/agent_infrastructure/idea_provider_agnostic_agent_orchestration.md`** — the source proposal. Its own Current-State Inventory (line 58) already pre-labels `.agents/` as "Existing but stale; must be disposed of before new Codex work," and its "Existing `.agents/` precedent" section (lines 63-73) defines the exact three-way classification (`retain and migrate` / `replace` / `archive/retire`) this ticket's AC1 requires. This is framing from the requesting proposal, not itself an authoritative classification — the per-path work is still this ticket's to do.
- **`docs/plans/agent_infrastructure/idea_provider_agnostic_agent_orchestration_finding_01_claude.md`** — the direct predecessor finding (2026-07-20) that discovered `WorkflowRegistry`'s live dependency on `.agents/`, confirmed `src/lab/cli.py` never imports it, and explicitly warned that classifying `.agents/workflows/*.md` or `.agents/skills/*/SKILL.md` as `archive/retire` "without also updating or retiring `test_real_registry_contracts` in the same change would produce a real regression." This investigation independently re-confirmed every claim in that finding.

## Risks and Open Questions

- **Core open question (per AC3, not resolved here by design):** whether `WorkflowRegistry` is retained as intended future production wiring or retired as dead code. Evidence strongly favors "dead/unwired today" — zero production callers, plus one concrete, named instance (`LAB-GUARDRAILS`) of its intended wiring being scoped and then dropped in favor of a simpler mechanism. But the class itself (a small, tested, working YAML-frontmatter → Pydantic-contract parser) is not necessarily worthless independent of `.agents/` as its source directory — the parent epic's own Open Decision #1 (`idea_provider_agnostic_agent_orchestration.md:428-430`) is still unresolved: whether the new provider-agnostic `agent-orchestration/` contract format should be "reviewable YAML, validated Python models, or both." `WorkflowRegistry`'s parsing pattern is structurally close to that undecided target shape. **Treat "is `.agents/` stale" and "is the `WorkflowRegistry` parsing logic reusable" as two separable questions** — do not let evidence for the first collapse the second by default.
- **Test-pairing risk (AC4):** whatever `.agents/workflows/`/`.agents/skills/` disposition is chosen, `test_real_registry_contracts` hardcodes real paths and field-level assertions tied to exactly two files' content (`generate-simulation-setup.md`, `prepare-simulation-execution.md`). Any archive/retire or content rewrite of those two specific files breaks this test unless the test is updated in the *same* change — this is not a "test later" risk, it's coupled to the classification decision itself.
- **Scope-boundary judgment call:** the containment rule bars "production Claude/Codex workflows, hooks, monitoring writers, live ticket artifacts, or the shared monitoring JSONL corpus." `src/lab/registry.py` and its test are real production code for the (separate) simulation-research "Lab" feature, not the meta dev-ticket-workflow system — physically editing them (vs. only writing a decision-record doc) is a legitimate in-scope "isolated... fixture" change per the ticket's own Scope wording and is required by AC4 regardless of which disposition is chosen. Flagging this only so the boundary isn't second-guessed mid-implementation.
- `.agents/skills/`'s 7 `.agents`-only subdirectories carry no test coverage — reclassifying them is test-risk-free, but their *content* (not diffed here beyond one sample) should be spot-checked before a blanket "duplicate, therefore archive" call, per the ticket's own Assumption about partial-only duplication.

## Anti-Drift Hazards

- Do not rewrite or archive `.agents/workflows/generate-simulation-setup.md` or `prepare-simulation-execution.md` content without updating `test_real_registry_contracts`'s specific field assertions (lines 103-104, 108-109, 113-117) in the same change.
- Do not conflate ".agents/ is stale scaffolding" (well-evidenced) with "WorkflowRegistry's code is worthless" (not established) — AC1 (per-path `.agents/` classification) and AC3 (WorkflowRegistry disposition) are separate determinations even though they're evidentially linked.
- Do not implement the Codex adapter, the shared `agent-orchestration/` contract format, or any change to `.claude/workflows/*.js`, `.claude/agents/*.md`, `.claude/skills/*`, or `CLAUDE.md` — all explicitly out of scope for this ticket and blocked by the parent epic's exit gate.
- Do not touch `tools/agent-monitoring/*`, `agent-monitoring/*.jsonl`, or in-flight sibling tickets' domains (`TCK-20260721-CODEX-CAPABILITY-MATRIX`, `TCK-20260721-MONITORING-WRITER-DECISION`) — per `SEQUENCE.md`'s containment rule, these run in parallel and are out of this ticket's scope.
- Do not assume all 11 `.agents/skills/` ∩ `.claude/skills/` name-overlaps are byte-identical duplicates from the one sampled pair — verify per-directory if the classification depends on exact duplication, not just name overlap.
- Do not misclassify `.agents/skills/graphify/SKILL.md` as needing migration into `.claude/skills/` — its active replacement already exists at user/global scope (`~/.claude/skills/graphify/SKILL.md`), a different tier entirely from project-local skills.
- `.agents/rules/engine_contracts.md`'s prescriptive per-subsystem update-rule detail is not present anywhere in current `CLAUDE.md` — a straight "replace, CLAUDE.md already supersedes this" classification would silently drop real operational content that the other 8 `.agents/rules/*` files don't carry; treat it as a distinct case from its siblings.
