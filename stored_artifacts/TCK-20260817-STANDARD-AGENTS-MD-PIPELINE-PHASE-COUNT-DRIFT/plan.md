---
status: historical
layer: engine
authority: P2
audience: agent
artifact_type: plan
ticket_id: TCK-20260817-STANDARD-AGENTS-MD-PIPELINE-PHASE-COUNT-DRIFT
tags: [ai, engine, bug]
---

# Plan — TCK-20260817-STANDARD-AGENTS-MD-PIPELINE-PHASE-COUNT-DRIFT

## Approach
Fix the authoritative source of truth (`docs/engine/authoritative_pipeline.md`'s phase table)
first, then propagate the corrected count/order to every currently-active document that cites it.
Do not touch any `tickets/done/`, `stored_artifacts/`, `docs/archive/`, `docs/plans/archive/`, or
`docs/audits/` file that cites the old count as a historical snapshot — those are frozen records,
not live documentation.

## Steps
1. **`docs/engine/authoritative_pipeline.md`**: rebuild the phase table with all 37 real phases in
   their real `pipeline.py::refine()` order, inserting the 6 missing phases
   (`faction_awareness`, `diplomatic_transitions`, `military_conflict` after `blacksmith`;
   `gold_sink` after `town_resolution`; `guild_visit` after `quest_rewards`; `paid_information`
   after `shop`) with real citations pulled from their surrounding code comments (E53Bc/Bd, E53Ca,
   E33C, the TCK-20260807 wiring ticket, E42C respectively). Update "31 Phases"/"these 31 phases"
   prose to "37 Phases"/"these 37 phases".
2. **`tools/agent_orchestration_codex_adapter/generator.py`**: update
   `_AUTHORITATIVE_PIPELINE_NOTE`'s "32-phase"/"32 phases" to "37-phase"/"37 phases".
3. **Regenerate `AGENTS.md`** (and its companion `.agents/skills/*` copies) via
   `render_codex_guidance(repo_root, repo_root)` — never hand-edit the generated file per its own
   header comment.
4. **`CLAUDE.md:258`**: update the `authoritative_pipeline.md` row's "32-phase" to "37-phase".
5. **Living skill docs** citing the old count as current fact (not `.agents/skills/`, which is
   itself generated by step 3): `.claude/skills/debugging-strategies/SKILL.md`,
   `.claude/skills/cognition-strategy/SKILL.md`, `.claude/skills/python-performance-optimization/SKILL.md`,
   `.claude/skills/combat-mechanics/SKILL.md`, `.claude/skills/systems-economy/SKILL.md`. For the
   3 skill docs with specific numbered phase-# table rows (`cognition-strategy`, `combat-mechanics`,
   `systems-economy`), recompute each cited phase's real new number from the corrected table
   (phases before the first insertion point, #8, are unaffected; phases from #9 onward shift by
   the count of new phases inserted before them).
6. **`tests/agent_orchestration_codex_adapter/test_agents_md_generation.py`**: the batch's
   originally-failing test (`test_agents_md_pipeline_note_matches_live_engine_doc`) will pass once
   steps 1-2 land. A second test in the same file
   (`test_agents_md_states_authoritative_32_phase_pipeline`) was NOT in the original failing set
   (it was passing only because both sides of its comparison were stale-but-matching) — it will
   start failing once step 3 regenerates `AGENTS.md` with the corrected count, so it must be
   updated (and renamed to drop the now-wrong "32" from its own name) in the same change, not left
   broken.
7. Search for any other currently-active (non-archived/non-historical) file citing "32-phase"/"31
   phase" and fix in the same pass; verified via `grep -rln` across `.py`/`.md`/`.js`, confirmed
   every remaining hit after steps 1-6 is a legitimately frozen historical record.

## Out of scope
- `docs/ai/agents_dir_disposition.md`'s citation of "32-phase" — this is a factual snapshot inside
  a disposition-table analysis of a *different* archived file's staleness, written at a point in
  time; it is not describing current live state on an ongoing basis the way a skill doc's "this is
  how the pipeline works today" claim is.
- Any change to `src/engine/pipeline.py` itself — the 37-phase reality is correct production
  behavior; only documentation was stale.
- Any parity ledger entry — `docs/parity_ledger/` has no entry referencing pipeline phase count
  (verified via grep), so none needs updating.
