---
status: historical
layer: ai
authority: P2
audience: agent
ticket_id: TCK-20260709-CONCERN-INVESTIGATOR-AGENT
artifact_type: investigation
tags: [ai, investigator, workflows]
---

# Investigation — TCK-20260709-CONCERN-INVESTIGATOR-AGENT

## Current Behavior

### `.claude/workflows/create-tickets.js` — Investigate phase (`.claude/workflows/create-tickets.js:202-424`)
- `phase('Investigate')` at line 208 kicks off `pipeline(comprehension.concerns, (concern) => agent(...))` at lines 290-414.
- The per-concern `agent(...)` call at lines 296-412 does **not** pass an `agentType` option — only `{ label: 'investigate:${concern.id}', schema: INVESTIGATION_SCHEMA }` (line 411). Per the JS→tool-call translation table in `docs/ai/agents.md:17-20` and `.claude/skills/create-tickets/SKILL.md:36-37`, `await agent(prompt, { label: 'L' })` (no `agentType`) maps to a plain `Agent(prompt: prompt)` call with no `subagent_type` — this resolves to the harness default (`general-purpose`), exactly as the ticket's Request Summary states.
- The inline prompt (lines 297-410) restates, per-concern, the full mandatory-ordering protocol: Step 0 semantic retrieval (`tools/knowledge_search.py`, line 315), Step 1 graphify (`graphify query`, line 328; raw graph.json fallback, lines 330-336), Step 2 `docs/REGISTRY.yaml` query via `tools/registry_query.py`'s `candidate_tags_from_text`/`filter_registry` (lines 344-361), Step 3 `working_log.csv` grep + `stored_artifacts/` cross-reference (lines 366-374), Step 4 code file reads (lines 376-383), Step 5 test discovery (lines 385-391), Step 6 AC-signal derivation (lines 393-401), Step 7 tier assessment (lines 403-406). This ~115-line block is authored once per `agent()` call site, not read from a shared file — it is the single point of truth today, and any future edit to this methodology (e.g., in `investigator.md`) has no mechanical link back here.
- `INVESTIGATION_SCHEMA` (lines 210-263) is a structured JSON contract: `concern_id`, `files_found`, `constraints`, `existing_tests`, `related_tickets`, `ac_signals`, `risks`, `is_duplicate`, `duplicate_of`, `tier_recommendation`, `summary`. This is fixed by the Structure phase's consumption pattern (lines 520-549, `investigationBundle`) — **out of scope** per the ticket, and the ticket's AC #3 explicitly requires this schema stay byte-identical.
- Consumers of `agentType`: two other call sites in the same file already use it — `{ agentType: 'ticket-scoper', ... }` (Write phase, line 785) and implicitly the default (no `agentType`) for `comprehend` (line 184) and `monitoring-write`/`structure`/`link-epic` (lines 139, 622, 908) — these are intentionally generic, not investigation-shaped, so `agentType` usage is selective, not blanket.

### `.claude/agents/investigator.md` (this file — read in full as the agent executing this ticket)
- Frontmatter (added by `TCK-20260707-SUBAGENT-FRONTMATTER`, lines 1-4): `name: investigator`, `description: ...`. **No `tools:` field.**
- Contract: takes a `ticket_id` (line "You receive a ticket ID"), requires `tickets/inprogress/{ticket_id}.md` to already exist, and always writes two markdown files to `staging_artifacts/{ticket_id}/` (`investigation.md`, `test_plan.md`). This is fundamentally incompatible with create-tickets.js's Investigate phase, which runs **before** any ticket file exists (it investigates a `concern` object, not a `ticket_id`) and must **return JSON**, not write files — confirmed by reading both files side by side. The ticket's Scope item 3 ("structurally distinct... not a reuse of it") is well-founded, not a stylistic preference.
- Methodology structure (Finding Prior Work section) already encodes the REGISTRY.yaml-first / `tickets/done/` fallback ordering that create-tickets.js's inline prompt duplicates independently — this is the "single source of truth" the ticket wants centralized.

### `.claude/agents/architecture-reviewer.md` (reference example, per ticket's Related Code Areas note)
- Frontmatter is the identical 4-line `name`/`description` block (lines 1-4). No `tools:` restriction here either, despite this agent's job being purely advisory/read-only (produces a verdict, never edits code) — it is **not** actually an example of tool-scoping-via-frontmatter; it is an example of a narrowly-scoped **prompt contract** (Registry Lookup → What to Review → Output, `architecture-reviewer.md:10-62`) with no enforced tool restriction. The ticket's phrasing ("reference example of a review-only, narrowly-scoped agent definition") is accurate for prompt/output scoping, but potentially misleading if read as evidence that tool restriction is already solved here — it is not.

### All 12 `.claude/agents/*.md` files (full read, `.claude/agents/*.md`)
Every file follows the identical 2-field frontmatter pattern:
```
---
name: <filename-minus-.md>
description: <one sentence>
---
```
None of the 12 declares a `tools:`, `model:`, or `color:` field. This was a **deliberate, disclosed choice** in `TCK-20260707-SUBAGENT-FRONTMATTER` (Scope item 4: "Do not add `tools:`, `model:`, or any other optional frontmatter field... Tool/model scoping per role is a plausible follow-up improvement but is out of scope here"). That ticket's own Assumptions/Open Questions section flags this explicitly as "a deliberate minimality choice, not an oversight — flagged here in case a reviewer wants that folded in instead of left as a follow-up." **This ticket is that follow-up**, but scoped to one new agent rather than retrofitting the existing 12.

### Tool-scoping mechanism — how it is actually expressed (not in any `.claude/agents/*.md` body today)
- The Agent tool's own live subagent-type listing (visible in this session's system reminder) shows a `Tools:` annotation per type, e.g. `Explore: ... (Tools: All tools except Agent, Artifact, ExitPlanMode, Edit, Write, NotebookEdit)` for the **built-in** `Explore` type, versus `investigator: ... (Tools: All tools)`, `architecture-reviewer: ... (Tools: All tools)`, and identically `(Tools: All tools)` for all other 11 custom `.claude/agents/*.md`-defined types.
- This confirms two things: (1) the harness **does** compute and can display a per-agent tool restriction — it is not a display-only artifact of built-in types; (2) every existing custom `.claude/agents/*.md` agent currently resolves to the unrestricted default ("All tools") because none of them declares a `tools:` frontmatter field — directly consistent with `TCK-20260707`'s disclosed no-`tools:`-field choice.
- No file in this repo (`.claude/settings.json`, `.claude/settings.local.json`, any `.claude/agents.json` — the latter does not exist) declares a separate tool-restriction mapping keyed by agent name. `.claude/settings.json`'s `permissions.allow` list (read in full) is a Bash/tool **allow-list for the top-level session**, not a per-subagent restriction — it does not scope what `Agent(subagent_type: "investigator", ...)` itself can call once spawned.
- `.claude/skills/prompt-builder/SKILL.md:100-106` contains a `tools: ["[appropriate tools based on functionality]"]` frontmatter example, but this is a **different, unrelated format** — it is the GitHub Copilot `.prompt.md` template (per that skill's own description: "Guide users through creating high-quality GitHub Copilot prompts"), evidenced by the accompanying `agent: "[agent|ask|edit...]"` field, which is Copilot-specific vocabulary with no Claude Code equivalent. **This is not evidence for or against Claude Code's own `.claude/agents/*.md` frontmatter spec** — flagging so a future reader doesn't mistake it for confirmation.
- **Conclusion on the open question the ticket implicitly raises**: no `.claude/agents/*.md` file in this repo has ever declared a `tools:` field, so there is no in-repo precedent proving it is honored by this harness. However, the harness's own live Agent-type listing already exposes a per-type `Tools:` restriction mechanism that behaves consistently with Claude Code's publicly documented subagent frontmatter spec (`name`, `description`, `tools`, `model`), and the *absence* of restriction on all 12 existing custom agents is fully explained by the *absence* of the field, not by the field being non-functional. This is the strongest available evidence, but it is inference, not an in-repo functional test — `TCK-20260707-SUBAGENT-FRONTMATTER` demonstrated this harness has at least one surprising subagent-registration behavior (no mid-session hot-reload of `.claude/agents/`, requiring a session restart before a new/changed agent becomes callable) that was **not** documented anywhere until that ticket's live smoke test surfaced it. A `tools:` field added to `concern-investigator.md` should be treated the same way: plausible-but-unverified until smoke-tested live, and the same session-restart requirement almost certainly applies.

## Mechanics / Engine Constraints
None. This ticket is agent/workflow tooling (`.claude/agents/`, `.claude/workflows/create-tickets.js`) — no `docs/mechanics/` or `docs/engine/` chapter governs subagent definitions or the create-tickets pipeline. `CLAUDE.md`'s Context Scan Mandatory section and the Hard Rule "Do not run grep, find, raw file reads, or spawn Explore agents for investigation before first calling `search_docs` (MCP) and `graphify query`" are the only durable-process constraints in play, and they are precisely what this ticket's new agent must encode as its system prompt (per Scope item 1).

## Parity Ledger Overlap
None. Grepped all 8 `docs/parity_ledger/*.yaml` files for "agent", "investigator", "create-tickets" — the only hits are unrelated uses of the word "agent" (`src/lab/` agentic simulation lab entries in `infrastructure.yaml`, and a `WorldEmergencePhase and agent observability systems` mention in `world_dynamics.yaml` referring to gameplay-entity agents, not Claude Code subagents). No P0/P1/P2 parity ledger entry governs `.claude/agents/*.md` or `.claude/workflows/create-tickets.js`. No entry needs updating as part of this ticket.

## Prior Work
Queried `docs/REGISTRY.yaml` (1346 entries, 1077 `type: ticket`) filtered by `related_code_areas` overlap with `.claude/agents/` / `create-tickets` and by tag intersection with `[ai, investigator, workflows, agent-monitoring]`:

- **`TCK-20260707-SUBAGENT-FRONTMATTER`** (`tickets/done/`, hotfix, DONE) — the ticket that made all 12 `.claude/agents/*.md` files callable `subagent_type`s at all, by adding the `name`/`description` frontmatter block this new file must also use. Its Scope item 4 explicitly deferred `tools:`/`model:` scoping as "a plausible follow-up improvement" — **this ticket is effectively that follow-up**, though scoped narrowly to one new agent rather than retrofitting the 12. Its Test Summary is the only in-repo evidence of harness subagent-registration behavior: **a session restart is required after editing `.claude/agents/*.md` before new/changed types become callable** — this will apply to `concern-investigator.md` too and should be called out explicitly in the plan/implementer handoff so the smoke test (AC-adjacent, "does not grant Edit/Write/NotebookEdit") isn't attempted mid-session and wrongly reported as failing.
- **`TCK-20260607-MON-AGENTS`** (`tickets/done/`, DONE, stored artifacts present at `stored_artifacts/TCK-20260607-MON-AGENTS/`) — added a `summary` field convention to all agent Output specifications (schema agents get a `summary` schema field; free-text agents like `investigator`/`planner`/`parity-updater` are instructed to lead with a one-sentence summary). Its investigation.md flags: "If a new schema agent is added to implement-ticket.js in the future, it must add `summary` to its schema" (Anti-Drift Hazards). **Directly applicable**: `INVESTIGATION_SCHEMA` in `create-tickets.js` already has a `summary` field (line 258-261) and `pushEvent('Investigate', ..., inv.summary, null)` (line 420) already consumes it — so `concern-investigator`'s output contract is already summary-compliant by construction; no new gap here, but the new agent's own system prompt should state the ≤200-char one-sentence-summary convention explicitly, matching this precedent, rather than relying on the schema description string alone.
- **`TCK-20260705-TAG-REGISTRY-QUERY`** (tags: `tagging, taxonomy, registry, investigator`) — extended `investigator`'s own REGISTRY.yaml prior-work search to filter by tags in addition to layer (`tools/registry_query.py`'s `candidate_tags_from_text`/`filter_registry`, referenced directly in this very investigator role's own instructions). `create-tickets.js`'s Investigate-phase Step 2 (lines 344-361) already calls this same helper inline. This confirms the tag-aware registry query pattern the new agent should inherit rather than reinvent.
- **`TCK-20260708-SKILL-CREATE-TICKETS-WORKFLOW-TOOL-STALE`** (`tickets/done/`, hotfix, DONE) — most recent prior touch of `.claude/skills/create-tickets/SKILL.md`'s Action section (not the workflow `.js` file itself). Confirms the current, correct pattern for that skill file's translation-table format ("Do not call the Workflow tool — it is not available. Execute the workflow directly:") — relevant if this ticket's implementation needs to touch `SKILL.md`'s Investigate-phase description to mention the new `agentType: 'concern-investigator'` (not explicitly in this ticket's Scope, but the SKILL.md's own Pipeline section (lines 48-75) currently restates the same investigate methodology inline as the `.js` file and would drift out of sync with the new agent-owned methodology if left untouched — flagged as a risk below, not assumed in/out of scope).
- No prior ticket previously attempted to add a `tools:`-restricted or read-only-scoped custom agent — this is a first-of-its-kind change in this repo's `.claude/agents/` history.

## Risks and Open Questions

1. **Unverified mechanism (blocking-adjacent, must be resolved by testing, not assumption)**: whether a `tools:` frontmatter field on a `.claude/agents/*.md` file is actually honored by this harness has never been tested in this repo. The Agent-tool's live type listing shows the harness supports per-type tool restriction (built-in `Explore` has one), and the complete absence of restriction on all 12 existing custom agents is consistent with (not proof against) the field working — but this is inference. **AC #1 ("does not grant Edit/Write/NotebookEdit tool access") can be satisfied structurally by adding a `tools:` field that omits them**, but whether the harness *enforces* that restriction (vs. silently ignoring an unrecognized frontmatter key and falling back to full access) is unconfirmed until a live smoke test is run — mirroring exactly the smoke-test discipline `TCK-20260707` had to apply for basic subagent registration. The plan must include this smoke test as a required step, not an optional nice-to-have.
2. **Session-restart precedent**: `TCK-20260707` found the harness does not hot-reload `.claude/agents/` mid-session. Adding `.claude/agents/concern-investigator.md` will likely require the same restart before `Agent(subagent_type: "concern-investigator", ...)` resolves. The plan/implementer should anticipate this rather than treating a first-attempt "Agent type not found" as a bug.
3. **SKILL.md drift**: `.claude/skills/create-tickets/SKILL.md`'s Pipeline section (lines 48-75) currently documents the Investigate-phase methodology inline, duplicating what will become `concern-investigator.md`'s system prompt. The ticket's Scope does not mention `SKILL.md`, only `create-tickets.js` and `docs/ai/agents.md`. Leaving `SKILL.md` untouched risks a third copy of the same methodology going stale (the exact failure mode this ticket exists to fix for the `.js`/`investigator.md` pair) — flagged as an open scope question for planning, not resolved here.
4. **`suggested_skills`/tag-mapping surface, no direct effect**: `create-tickets.js`'s Structure phase (lines 594-608) has a closed 4-tag → skill mapping table (`api-design`, `debugging`, `performance`, `security`) unrelated to the Investigate phase's own agent dispatch — confirmed no interaction with this ticket's change; noted only to rule it out.
5. **Manual/ad hoc invocation open question** (already flagged in the ticket's own Assumptions/Open Questions): whether `concern-investigator` should be documented as directly invocable outside `create-tickets.js`. This is explicitly left to planning per the ticket text — the investigation surfaces no code-level blocker either way (a `.claude/agents/*.md` file with `name`/`description` frontmatter is directly `Agent(subagent_type: ...)`-callable regardless of whether any workflow references it, as `world-debugger`/`simulation-analyst` demonstrate — both are "when to invoke directly" agents with no required workflow caller).

## Anti-Drift Hazards

- **Schema drift**: `INVESTIGATION_SCHEMA` (create-tickets.js:210-263) must stay byte-identical (ticket AC #3) — the new agent's system prompt must describe this exact same JSON contract, not a paraphrase or updated set of fields, or the Structure phase (`investigationBundle`, line 520) will silently receive malformed data.
- **Frontmatter minimalism precedent**: `TCK-20260707` established that `.claude/agents/*.md` frontmatter changes must be byte-identical/additive to avoid destabilizing the other 11 agents' registration — do not touch any of the other 11 files' frontmatter while adding this one.
- **Do not widen `investigator`'s contract**: the ticket's Assumptions section already forecloses this ("this was the explicit recommendation... don't risk the standard-tier ticket pipeline that already depends on `investigator`'s current file-writing behavior"). `investigator.md` itself must not be edited by this ticket's implementation.
- **Tool-scoping must be verified, not assumed working**: shipping `tools:` frontmatter without a live smoke test would repeat the exact class of "documented but broken" gap `TCK-20260707` fixed (subagent registration silently not working despite correct-looking config) — this time for tool restriction rather than type registration.
- **`create-tickets.js`'s per-concern prompt reduction (Scope item 3) must not drop concern-specific substitutions**: the current inline prompt interpolates `concern.id`, `concern.title`, `concern.description`, `concern.domain_area`, `concern.type_hint`, `concern.priority_hint`, `concern.raw_excerpts`, and the derived `registryLayers` (via `DOMAIN_TO_LAYERS`, lines 266-288) — the shrunk prompt must still pass all of these, just without restating the *methodology* that becomes durable system-prompt content instead.
- **Out-of-scope workflows must stay untouched**: `implement-ticket.js`/`implement-epic.js` also use `agentType: 'investigator'` for the standard-tier pipeline (per `docs/ai/agents.md`'s Ticket Lifecycle Agents section and the Agent Summary Table) — this ticket's new `concern-investigator` type must not be substituted there; the ticket's Out of Scope section already states this but it is easy to accidentally "clean up" during implementation since both agents share "investigator" in the name.
