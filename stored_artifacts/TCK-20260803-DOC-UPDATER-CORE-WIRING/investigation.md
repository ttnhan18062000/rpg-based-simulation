---
status: historical
layer: ai
authority: P2
audience: agent
ticket_id: TCK-20260803-DOC-UPDATER-CORE-WIRING
artifact_type: investigation
tags: [workflows, documentation]
---

# Investigation — TCK-20260803-DOC-UPDATER-CORE-WIRING

## Current Behavior

**`.claude/agents/parity-updater.md`** (52 lines, `/home/u24desktop/Working/rpg-based-simulation/.claude/agents/parity-updater.md`) — the direct structural template. Confirmed real shape:
- Lines 1-4: frontmatter (`name: parity-updater`, one-line `description:`).
- Lines 6-8: one-paragraph role intro ("You are a ... subagent ... After a behavior change is implemented, you update ...").
- Lines 10-23: `## Step 0 — Expected-Subsystem Context` — describes the orchestrator-injected preamble line and what the orchestrator does *after* the agent's turn ends (re-runs `cross_reference_touched`), told to the agent as context, not an instruction to self-run.
- Lines 25-38: `## Parity Ledger Files` — a `File | Subsystem` table.
- Lines 40-56: `## Entry Schema` — YAML field shape block (not a table).
- Lines 58-67: `## What to Do` — numbered steps.
- Lines 69-71: `## Output` — one-sentence-summary contract + structured report fields incl. `verified_by`.

This is exactly the section skeleton the ticket's Scope item 1 asks `doc-updater.md` to follow (Step 0 context → rules table/prose → What to Do → Output). One divergence to carry forward carefully: `parity-updater.md` line 65 tells the agent to append intentional divergences to `docs/guidelines/v2_intentional_divergences.md`, but CLAUDE.md and the design doc both cite the real path as `docs/guidelines/intentional_divergences.md` (no `v2_` prefix) — confirmed the real file is `docs/guidelines/intentional_divergences.md` (CLAUDE.md's own Authoritative Mechanics Rule section names it this way, and `docs/ai/agents.md:265` also cites it without `v2_`). This is a pre-existing typo in `parity-updater.md`, out of this ticket's scope to fix (`parity-updater.md` is explicitly read-only per this ticket's Out of Scope), but `doc-updater.md` must NOT copy the typo — use `docs/guidelines/intentional_divergences.md`.

**`.claude/workflows/implement-ticket.js`** (1493 lines total) — confirmed exact seam, all ticket-cited line numbers reconfirmed accurate by direct read on this date:
- Line 771: `)` — closes the Implement phase's `agent()` call (`{ label: 'implement', schema: IMPL_SCHEMA, agentType: 'implementer' }`).
- Lines 773-791: doc-staleness gate comment block (`// ─── Doc-staleness gate (orchestrator-run, no agent call) ─────────────────────`, starting exactly at line 773) plus the `docsToUpdate`/`docStalenessFilesArgs`/`docsToUpdateArgs` variable construction.
- Line 792: `const docStalenessFilesArgs = implementation.files_changed.map(f => \`"${f}"\`).join(' ')` — confirmed exact text and line.
- Lines 794-796: the `bash()` call invoking `doc_staleness_check.py` — confirmed exact text and line.
- Lines 1086-1109: Parity phase's `captureTs()` (1086) → `writeSidecar()` (1087) → `agent()` (1088-1109) template — confirmed as the structural precedent the ticket cites.
- Line 456-457: hotfix-tier default `investigation = { docs_to_update: [], findings_summary: '(hotfix — investigation skipped)' }` — this is the value `investigation.docs_to_update` holds on the hotfix path when Document-Update phase runs; it is **not** the ticket's `## Scope` text described in the design doc's hotfix data-flow rule. See Risks below — this is the one real implementation gap in an otherwise clean insertion point.
- Line 726/728: `phase('Implement')` runs unconditionally for every tier (no `if (tier !== 'hotfix')` guard around Implement itself) — confirms Document-Update, inserted right after it, can also run unconditionally exactly as AC requires, with `implementation.files_changed`/`implementation.behavior_changed` already populated by the time it would run.
- Line 730-741: `IMPL_SCHEMA` — confirms `implementation.files_changed` is the actual field name (required, `array of string`) the new phase must merge its own reported files into, matching the ticket's claim.

**`tools/gate_checks/doc_staleness_check.py`** (106 lines) — confirmed function signature: `check_doc_staleness(files_changed: List[str], behavior_changed: bool, docs_to_update: Optional[List[str]] = None) -> List[dict]`. Pure function, no internal state, no coupling to how `files_changed` was assembled — it only ever sees the final merged list the caller passes in. Confirms the ticket's Scope item 3 claim ("no internal logic change, only a different input") is accurate: the CLI wrapper (`__main__` block, lines 94-106) takes `behavior_changed`, then all remaining positional args as `files_changed`, with an optional `--docs-to-update` sentinel splitting off a second list — the orchestrator already builds this exact shape at lines 792-796, so merging doc-updater's own reported files into `docStalenessFilesArgs`'s source array (`implementation.files_changed`) before that `.map()` call is a pure JS-side change with zero Python-side impact.

## Mechanics / Engine Constraints

None — this is pure agent-orchestration tooling (`.claude/agents/*.md`, `.claude/workflows/*.js`, `docs/ai/*.md`), not simulation logic. No `docs/mechanics/` chapter or `docs/engine/` contract governs agent pipeline shape.

## Docs Requiring Update

- `docs/ai/workflows.md`: add a `Document-Update` row to the `implement-ticket` phase table (confirmed at lines 81-93, header row at 81, `Implement` row at 87, `Test` row at 89 — new row belongs between them; currently omits the phase entirely).
- `docs/ai/system_overview.md`: four separate spots need editing, not just the one the ticket names, all inside the same prose walkthrough:
  - Line 82: "**The 10-phase `implement-ticket` pipeline** (standard tier): Scope (...)" — phase count in the lead sentence.
  - Lines 100-105: "Security-Review is an 11th, conditional phase — ... the pipeline is still described as 10 standing phases plus this one conditional gate. Parity itself remains one of those 10 standing phases ... This matches `docs/ai/workflows.md` and `docs/ai/ticket-lifecycle.md`, both of which match `.claude/workflows/implement-ticket.js` exactly." — an explicit, self-referential cross-doc consistency claim across three docs; adding an 11th standing phase (Document-Update) makes this literally false the moment it lands, in all three docs it names.
  - Line 117: hotfix pipeline summary (`Scope → Implement → Test → Parity → Verify → Finalize`) — the one the ticket already names, confirmed still accurate/still missing `Document-Update`.
  - Line 118: `| standard | Full 10-phase pipeline | ... |` table row — same count claim as line 82, in table form.
- `docs/ai/ticket-lifecycle.md`: **not named in the ticket's Related Docs or Scope, but confirmed by direct read to need the same class of edit as the two docs above, and arguably more central** (it is the deep-dive doc `system_overview.md` line 104 explicitly cites as "matching"):
  - Line 22: Tier Routing table's hotfix row (`Scope → Implement → Test → Parity → Verify → Finalize`) — same gap as `system_overview.md` line 117.
  - Line 23: `| standard | Full 10-phase pipeline (default) | ... |` — same count claim.
  - Lines 36-88: the mermaid flowchart — needs a new `Document-Update` node inserted between the `Implement` node (line 58, which already branches to `DocStalenessFix` on `DOC_STALENESS_BLOCKED` and then to `ArchVerify`) and the doc-staleness-gate edge, since the phase's real orchestrator position is Implement → Document-Update → doc-staleness gate → Architecture-Verify.
  - Lines 242-296: the `### Implement` prose subsection ends at line 296 (`---` separator), immediately followed by `### Architecture Verify` at line 298 — a new `### Document-Update` subsection belongs in between, in the same prose shape as the existing `### Parity` subsection (Agent / Step 0 / What the agent does / Gate-or-non-gate behavior).
  - Line 538 ("Hotfix runs push three `skipped` events for the Investigate/Plan/Review phases") does **not** need editing — Document-Update runs unconditionally for hotfix per this ticket's own design (Scope item 2, "no tier !== 'hotfix' guard"), so it is never a fourth skipped event; this line stays accurate as-is. Flagged here only so Plan doesn't over-edit it.
- `docs/ai/agents.md`: the epic's own Assumptions/Open Questions section asked Investigate to confirm whether this is "effectively required" given the established per-agent-section pattern. **Confirmed required** — every other pipeline agent (`ticket-scoper`, `investigator`, `planner`, `architecture-reviewer`, `implementer`, `test-scoper`, `parity-updater` at line 237, `mechanics-auditor` at line 273, `done-checker`, `security-reviewer`) has both (a) a `### agent-name` subsection under one of the file's category headings (`## Ticket Lifecycle Agents`, `## Quality and Compliance Agents`, etc.) and (b) a row in the `## Agent Summary Table` at lines 405-422. `doc-updater` is a new, permanent pipeline agent exactly like `parity-updater` — omitting it here leaves the one canonical "what does each agent do" reference doc silently incomplete for a real agent already wired into a real phase. Recommend a new `### doc-updater` subsection immediately after `### parity-updater` (ends line 271, before the `---` at line 271/272) under `## Quality and Compliance Agents`, plus a new row in the Agent Summary Table (e.g. between the `parity-updater` row at 416 and `done-checker` row at 417, matching post-Implement ordering). This should be folded into Plan now rather than deferred, per the ticket's own Assumptions guidance.

**Not required, flagged only as a known residual drift outside this ticket's reach:** `CLAUDE.md` line 108 (`| standard | Full 10-phase pipeline (+1 conditional: Security-Review) | ... |`) makes the identical stale count claim, but `CLAUDE.md` is not under `docs/` — it is outside `doc-updater`'s jurisdiction per the design doc's scope table ("docs/ minus the four rows"), and outside `check_docs_to_update_coverage`'s parse surface (which only reads `investigation.md`'s bullets against `git status` on `docs/` paths). No mechanism in this pipeline edits `CLAUDE.md`. Left as a discovered-but-unaddressed gap for a human or a future ticket, not folded into this ticket's Docs Requiring Update — editing `CLAUDE.md` is out of every agent's designed scope in this repo, not just doc-updater's.

## Parity Ledger Overlap

None. Grepped `docs/parity_ledger/infrastructure.yaml` (the subsystem covering replay/telemetry/observability/workers, and in practice the one that carries INFRA-* entries for `implement-ticket.js`'s own orchestration mechanics, e.g. INFRA-183/263-265/274/299) for any existing entry referencing `doc_staleness_check.py`, `DOC-STALENESS`, `GATE-CHECK-WIRING`, or `DOC-UPDATE-DISCIPLINE` — zero hits. This is a real, checked precedent, not an assumption: the doc-staleness gate's own wiring ticket (`TCK-20260720-GATE-CHECK-WIRING-DECISIONS`, which added the exact bash() call site this ticket's Document-Update phase sits next to) never got its own `infrastructure.yaml` entry either. Not every `implement-ticket.js` orchestration addition gets a parity-ledger row — only ones with a clear runtime/telemetry-shape claim (e.g. registry regeneration timing, sidecar seq math) have needed one historically. This ticket's phase-wiring change is the same class as the doc-staleness gate's own (deterministic pipeline plumbing, not a new telemetry claim), so no new `infrastructure.yaml` entry is expected. Confirms the ticket's own Assumptions/Open Questions claim ("No `docs/parity_ledger/*.yaml` overlap found ... zero hits").

## Prior Work

- `stored_artifacts/TCK-20260803-DOCS-STRUCTURE-AUDIT/` (done) — the design doc's own prerequisite; its per-family rule table is reproduced in `docs/architecture/doc_updater_agent.md`'s Decision section. Independently spot-checked its two central claims by direct read rather than trusting the audit blindly (per that same design doc's own "audits are dated snapshots, verify before trusting" rule): (1) all 26 top-level `docs/` subfolders are real (`ls -d docs/*/` confirms: agent-monitoring, ai, architecture, archive, audits, cognition, combat, compliance, content, core, engine, entity, guidelines, guides, mechanics, observability, parity_ledger, performance, plans, scenarios, simulation, simulation_quality, strategy, systems, testing, world — exactly 26); (2) `tools/generate_registry.py::_SKIP_DOC_SUBDIRS = {"archive", "parity_ledger", "scenarios", "entity"}` (line 57) matches the design doc's claim exactly. The design doc's "17-folder catch-all" count also reconciles arithmetically: 26 total − 4 skip-dirs − `audits` (cite-only) − `mechanics`/`engine`/`guides`/`plans` (4 folders with their own dedicated Family|Rule rows) = 17. Both audit foundations check out.
- `TCK-20260802-DOC-UPDATE-DISCIPLINE` (done) — built the three-layer enforcement (`## Docs Requiring Update` format, `doc_staleness_check.py`'s `docs_to_update` advisory extension, `check_docs_to_update_coverage`) this Document-Update phase becomes a new consumer of. Confirmed unchanged by this ticket — `docs_to_update` is read, never redefined.
- `TCK-20260720-GATE-CHECK-WIRING-DECISIONS` / `TCK-20260711-DOC-STALENESS-GATE-CHECK` (done) — shipped and wired `doc_staleness_check.py` at the exact seam this ticket inserts before. `tests/tools/test_doc_staleness_gate_wiring.py` (regression surface, see test_plan.md) is this prior work's own static-source-text guard suite.
- `TCK-20260705-GATE-DET-PARITY-UPDATER` (done) — built `parity_updater_static.py`'s `expected_subsystems_for_files`/`cross_reference_touched` split, the orchestrator-computes-the-"what" precedent this ticket's Scope item 2 explicitly cites as the model for Document-Update's own "what" computation.

## Risks and Open Questions

1. **Hotfix "what" input is not literally a JS variable today — confirmed implementable, but requires a specific design choice Plan must make explicit.** The design doc says hotfix tier should get "the ticket's own `## Scope` text plus `implementation.files_changed`." Today, `investigation.docs_to_update` is a real JS array on the hotfix path too (defaulted to `[]` at line 456), but there is no JS variable holding the ticket's raw `## Scope` prose — nothing parses it out today. The clean, low-risk implementation (matching how the existing hotfix-tier Implement-phase prompt already handles this exact same problem at line 750, `- ${ticketInfo.ticket_path} (hotfix — implement the fix directly from the ticket scope)`) is: the Document-Update agent prompt itself instructs the agent to read `${ticketInfo.ticket_path}` and use its own `## Scope` section directly, branched by `tier === 'hotfix'` in the prompt template exactly as Implement's own prompt already does — not a new orchestrator-side text-extraction step. This is implementable with zero new parsing code. Flagging as a Plan-level decision to make explicit (which of the two shapes: agent reads the file itself vs. orchestrator pre-extracts the Scope section), not a blocker, since the file-read approach is already proven at this exact call site.
2. **`meta.phases` array (lines 4-16, the file's own self-declared phase list) is not named anywhere in this ticket's Scope, but `tools/gate_checks/workflow_meta_conformance.py::extract_meta_phases` reads it directly, and `tests/tools/test_workflow_meta_conformance.py::test_parses_meta_phases_from_implement_ticket_js` (lines 64-69) asserts its exact contents as a literal list.** If `Document-Update` is added as a real `phase('Document-Update')` call but never added to the `meta.phases` array, `extract_meta_phases` under-reports the pipeline's own phases (silent, not a test failure — the test only checks the array matches itself, not that it matches actual `phase()` calls in the file body). But leaving `meta.phases` stale is the exact kind of self-inconsistency `workflow_meta_conformance.py` exists to catch elsewhere in the pipeline. Recommend Plan explicitly decide: add `{ title: 'Document-Update', detail: '...' }` to `meta.phases` (between `Implement` and `Architecture-Verify`, matching real execution order) AND update `test_parses_meta_phases_from_implement_ticket_js`'s expected list in the same diff. This is not optional polish — the AC bullet "Existing tests covering `implement-ticket.js` phase structure ... still pass" only holds trivially if `meta.phases` is left untouched; touching it (the architecturally-consistent choice) requires updating this test in lockstep. Not a blocker — just confirming this is in scope for whoever implements, since the ticket's own Related Code Areas list doesn't call out lines 4-16 specifically.
3. **Insertion-point text hygiene**: the new Document-Update phase block's own comments/prompt text must not contain the literal substring `doc_staleness_check.py` anywhere before the real gate comment block (line 773 today) — `tests/tools/test_doc_staleness_gate_wiring.py` uses `text.find("doc_staleness_check.py")` (first occurrence) as an anchor for several ordering assertions. As long as the new phase is inserted entirely before that literal string's first appearance (which the design doc's own ordering requires anyway), this is a non-issue — flagged only so the implementer doesn't casually reference the gate script by name in the new phase's own explanatory comments.
4. **`.claude/workflows/implement-ticket.js` is itself a `.claude/workflows/*.js` file** — per `doc_staleness_check.py`'s own flagged-path logic (`path.startswith(".claude/workflows/") and path.endswith(".js")`), this ticket's own Implement-phase diff will itself trip the doc-staleness gate (`behavior_changed=true`, a `.claude/workflows/*.js` path with no `docs/` path) unless `docs/ai/workflows.md`/`system_overview.md`/`ticket-lifecycle.md`/`agents.md` are ALL included in this same ticket's `files_changed`. This is self-consistent with the ticket's own Scope item 5 (it already commits to editing `workflows.md`/`system_overview.md`) — just confirming the mechanism will actually fire correctly on this ticket's own diff, which is a good sanity check that the gate still works as intended post-change.

No open question here blocks starting implementation — all four are either already resolved by direct evidence above or are Plan-level decisions with a clear low-risk default.

## Anti-Drift Hazards

- **Do not let Document-Update touch `docs/parity_ledger/*.yaml`.** Per the design doc's Scope boundary table, that is `parity-updater`'s exclusive territory, still running in its own later Parity phase. `doc-updater.md`'s per-family rules (Scope item 1) must explicitly exclude `docs/parity_ledger/` the same way the design doc's Family|Rule table does — the "everything else" catch-all row must not swallow it.
- **Do not let Document-Update touch `docs/audits/` content.** Cite-only per the design doc — audits are dated snapshots, not living docs; a doc-updater run that "fixes" a stale audit finding as a side effect silently breaks the audit programme's own point-in-time semantics (this exact trap already surfaced once during the design's own review, per the design doc's Context section).
- **Do not widen `doc_staleness_check.py`'s pass/fail logic.** This ticket's Scope item 3 requires `git diff -- tools/gate_checks/doc_staleness_check.py` to be empty — any temptation to add a `Document-Update`-aware branch inside `check_doc_staleness()` itself (rather than merging inputs before the call) is exactly the anti-pattern the design doc's Decision section explicitly rejects ("The fix is structural, not a new check").
- **Do not introduce a new blocking `return { status: ... }` for Document-Update failures.** AC explicitly forbids this — mirror Parity's `pushEvent(..., 'failed', ...)`-without-early-return shape, not `DOC_STALENESS_BLOCKED`'s hard-stop shape.
- **Do not touch `parity-updater.md`, `finalizer`, `planner`, or `plan.md`'s shape** — explicitly out of scope; `plan.md` stays one ordered step list per the design doc, doc-updater works from `investigation.md` + the real diff, not a relabeled plan.md.
- **Do not silently skip the `meta.phases` decision** (Risk 2 above) — either update it consistently with a matching test update, or explicitly leave it and document why in Implementation Notes; don't leave it in an undecided, half-updated state.
