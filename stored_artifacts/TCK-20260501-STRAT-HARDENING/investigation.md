# Strategic Cognition Investigation

## Current State
- `StrategicComponent` contains all necessary lists (blockers, leads, projects, concerns).
- `SimulationDomainLogic.execute_brain` orchestrates the loop: Concerns -> Blockers -> Intent -> Tactical.
- `DetourSuggestionSystem` exists but has limited match rules (mostly material/location).
- `RoutineService` handles biological needs but ignores inventory/safety status.

## Key Findings
- `StrategicUpdate` is correctly used as the "intent" carrier.
- `TurningPointState` is defined but NOT currently generated by any system.
- `boredom` is a Dict but `StrategicIntelligenceSystem` uses it as a simple counter or simple increment.
- `ProjectStatus.ABANDONED` is supported by the Enum but rarely triggered.

## Dependencies
- `CognitionProfile` determines bandwidth (max projects, max leads).
- `GoalRegistry` (likely in `src/ai/goals.py`) provides the "natural" utilities that strategy biases.
