# TCK-20260606-DOCSITE-REGISTRY

## Title
Generate docs/REGISTRY.yaml and integrate with agents for doc discovery

## Status
OPEN

## Tier
standard

## Type
chore

## Priority
P1

## Request Summary
Build a Python script that reads frontmatter from all content types (docs, tickets, artifacts, archive) and generates a flat `docs/REGISTRY.yaml`. Update CLAUDE.md and the relevant agents to use the registry for fast doc discovery without reading 542 files.

## Scope

### Registry generation script
`tools/generate_registry.py`:
- Walks frontmatter-tagged content roots: `docs/`, `tickets/done/`
- Does NOT walk `stored_artifacts/` directly — artifacts are joined from `tickets/done/` by ticket_id (see below)
- Reads frontmatter from each `.md` file
- Emits a flat YAML list with one entry per file

**Doc entry schema:**
```yaml
- type: doc
  path: docs/mechanics/02_combat_laws.md
  title: Combat Laws
  status: authoritative
  layer: mechanics
  authority: P0
  audience: developer
  tags: [combat, damage, durability]
  last_verified: 2026-06-06
```

**Ticket entry schema** (joined with stored_artifacts by ticket_id):
```yaml
- type: ticket
  path: tickets/done/TCK-20260606-PHASE26-REGISTRY-ADAPTERS-REPAIR.md
  ticket_id: TCK-20260606-PHASE26-REGISTRY-ADAPTERS-REPAIR
  title: Phase 26 Registry Adapter Repair
  tier: standard
  type: chore
  priority: P1
  date: 2026-06-06
  related_code_areas:
    - src/core/registries.py
  artifact_files:
    - stored_artifacts/TCK-20260606-PHASE26-REGISTRY-ADAPTERS-REPAIR/investigation.md
    - stored_artifacts/TCK-20260606-PHASE26-REGISTRY-ADAPTERS-REPAIR/plan.md
    - stored_artifacts/TCK-20260606-PHASE26-REGISTRY-ADAPTERS-REPAIR/test_plan.md
  tags: []
```

The `artifact_files` list is generated by checking which files exist under `stored_artifacts/{ticket_id}/` at registry generation time. Tickets with no artifact folder (hotfix tier or old tickets) emit `artifact_files: []`.

This join is the canonical link between tickets and their artifacts. Agents and Docusaurus use this — not directory scans of `stored_artifacts/`.

- Sorts by `type` (doc first, ticket second), then `authority`/`priority`, then `path`
- Reports count by type/status/layer/authority at the end
- Exits non-zero if any in-scope doc file is missing frontmatter (CI gate); ticket files without full frontmatter emit a warning but do not fail (backward compat with pre-schema tickets)

### Makefile target
`make docs-registry` — regenerates `docs/REGISTRY.yaml`

### Agent integration
Update `CLAUDE.md` Graphify Integration section to mention the registry.
Update these agents to query the registry before reading raw files:
- `investigator` — already updated (see `.claude/agents/investigator.md`). Queries ticket entries by `related_code_areas` overlap; reads `artifact_files` directly from registry — no directory scan of `stored_artifacts/`
- `mechanics-auditor` — "use registry to find all P0 doc entries for the relevant layer before auditing"
- `architecture-reviewer` — "use registry to find active architecture doc entries relevant to the plan's layer"

## Out of Scope
- Building or updating Docusaurus from the registry (Ticket 7)
- Vector embedding of docs (future work)
- Automated registry update on every file edit (future CI hook)
- Updating `docs/README.md` navigation (covered in Ticket 7)

## Acceptance Criteria
- [ ] `docs/REGISTRY.yaml` exists and contains `type: doc` entries for all frontmatter-tagged docs
- [ ] `docs/REGISTRY.yaml` contains `type: ticket` entries for all `tickets/done/*.md` files
- [ ] Each ticket entry includes `artifact_files` — list of files that exist in `stored_artifacts/{ticket_id}/` (empty list for hotfix/old tickets)
- [ ] Each ticket entry includes `related_code_areas` from the ticket's `## Related Code Areas` section
- [ ] Registry is sorted: docs first (P0 authoritative → P1 active → P2 archive), tickets second (newest first)
- [ ] `tools/generate_registry.py` runs without error after Tickets 3, 4, 5 are complete
- [ ] `make docs-registry` regenerates the file in place
- [ ] Script exits non-zero on missing doc frontmatter; warns (not fails) on missing ticket frontmatter
- [ ] `CLAUDE.md` references the registry in the Graphify Integration section
- [ ] `investigator` agent already updated — verify it reads `related_code_areas` and `artifact_files` from registry
- [ ] `mechanics-auditor` agent updated to use registry for P0 layer lookup
- [ ] `architecture-reviewer` agent updated to use registry for relevant active docs

## Related Tickets
- TCK-20260606-DOCSITE-SCHEMA (dependency)
- TCK-20260606-DOCSITE-FM-LIVE (dependency — must complete before registry is complete)
- TCK-20260606-DOCSITE-FM-TICKETS (dependency)
- TCK-20260606-DOCSITE-FM-ARCHIVE (dependency)
- TCK-20260606-DOCSITE-INTEGRATION (consumer)

## Related Docs
- `docs/guidelines/frontmatter_schema.md`
- `tickets/todos/PLAN-DOCSITE.md`
- `CLAUDE.md` (Graphify Integration section)

## Related Stored Artifacts
None yet.

## Related Code Areas
- `tools/generate_registry.py` (new)
- `docs/REGISTRY.yaml` (generated, committed)
- `Makefile`
- `CLAUDE.md`
- `.claude/agents/investigator.md`
- `.claude/agents/mechanics-auditor.md`
- `.claude/agents/architecture-reviewer.md`

## Assumptions / Open Questions
- `docs/REGISTRY.yaml` should be committed to git (not gitignored) so agents can read it without running the script
- `tickets/todos/` and `tickets/inprogress/` are NOT indexed — only `done` tickets are stable references
- `stored_artifacts/` is NOT walked directly — it is joined from `tickets/done/` by ticket_id at registry generation time
- `artifact_files` is a presence check at generation time; if a file is added to stored_artifacts/ later, registry must be regenerated
- How often should the registry be regenerated? Manually via `make docs-registry` for now; CI hook is future work
- Tickets without `## Related Code Areas` emit `related_code_areas: []` — no error

## Implementation Notes

## Test Summary

## Files Changed

## Completion Summary
