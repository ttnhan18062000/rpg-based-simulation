---
status: active
layer: ai
authority: P1
audience: agent
ticket_id: TCK-20260904-PROVIDER-PORTABILITY-CONFORMANCE-TEST
phase: open
date: 2026-09-04
tags: [testing, ai, architecture]
---

# TCK-20260904-PROVIDER-PORTABILITY-CONFORMANCE-TEST

## Title
Provider adapter conformance test for gate policy and artifacts

## Status
OPEN

## Tier
standard

## Type
feature

## Priority
P2

## Request Summary
docs/architecture/agent_orchestration_contract.md's Provider-Adapter Boundary ADR requires .claude/ and .codex/ to stay thin translations of the shared agent-orchestration/ contract on 4 named axes (workflow phases, terminal statuses, gate policy, artifact requirements), enforced today only at the field-name level. The plan calls for a test diffing .claude/agents/*.md and .codex/config.toml's translated equivalents against the canonical contract. Investigation found the literal framing doesn't match reality on two points: .codex/config.toml is deliberately empty of phase/gate/status content by design (the real Codex-side translated artifact is the generated AGENTS.md), and the gate-policy/artifact-requirement axes aren't yet represented as structured, diffable fields anywhere in agent-orchestration/ — building a real diff for those 2 axes may require first extending the contract schema itself. This ticket retargets the Codex-side comparison at AGENTS.md and reuses the existing TCK-20260721-CLAUDE-CONFORMANCE-ADAPTER pattern and divergence-approval mechanism for the 2 new axes.

## Scope
- Write a new conformance test extracting per-phase/per-role gate conditions from implement-ticket.js (or agents/*.md where documented) and assert they match agent-orchestration/'s per-phase gate representation — if that representation doesn't exist yet as a structured field, either add it to contract.yaml/roles/*.yaml (with a versioning bump per agent-orchestration/README.md's bump rule) or explicitly scope that as a prerequisite step within this ticket
- Write a new conformance test extracting the required-artifact set per tier (plan.md/investigation.md/test_plan.md for standard/epic, none for hotfix) and assert it matches a corresponding agent-orchestration/ field
- Include at least one deliberately-introduced-mismatch fixture per new axis proving the test actually fails on a real divergence, not just passes today
- Retarget the Codex-side comparison at AGENTS.md (the real translated artifact generated by tools/agent_orchestration_codex_adapter/generator.py), or explicitly document why .codex/config.toml is a no-op check and redirect to AGENTS.md
- Reuse the existing agent-orchestration/intentional-divergences.md approval mechanism for the 2 new axes (gate_policy, artifact_requirements) rather than inventing a new one
- Diff live code -> contract (matching the 2 existing Claude tests' direction), not the reverse

## Out of Scope
- Rebuilding or duplicating the 2 already-covered axes (workflow phases, terminal statuses) — TCK-20260721-CLAUDE-CONFORMANCE-ADAPTER already built these
- Treating .codex/config.toml as if it holds real phase/gate/status content — it is deliberately inert per TCK-20260721-CODEX-GUIDANCE-FIXTURE-CAPTURE
- Inventing a new divergence-approval mechanism when agent-orchestration/intentional-divergences.md already exists and should be reused

## Acceptance Criteria
- [ ] A new gate-policy axis test extracts per-phase/per-role gate conditions from implement-ticket.js (or agents/*.md where documented) and asserts they match agent-orchestration/'s per-phase gate representation — if that representation doesn't exist yet as a structured field, this ticket either adds it to contract.yaml/roles/*.yaml or explicitly scopes that as a prerequisite
- [ ] A new artifact-requirements axis test extracts the required-artifact set per tier and asserts it matches a corresponding agent-orchestration/ field
- [ ] The test suite includes at least one deliberately-introduced-mismatch fixture proving the test actually fails on a real divergence, not just passes today
- [ ] A Codex-side equivalent test compares AGENTS.md (the real translated artifact) against the same 4-axis contract data, or the ticket explicitly documents why config.toml is a no-op and redirects to AGENTS.md

## Related Tickets
- TCK-20260721-CLAUDE-CONFORMANCE-ADAPTER
- TCK-20260721-ORCHESTRATION-CONTRACT-CORE
- TCK-20260721-ORCHESTRATION-CONTRACT-ADR
- TCK-20260721-CODEX-GUIDANCE-FIXTURE-CAPTURE
- TCK-20260710-WORKFLOW-META-CONFORMANCE-CHECK

## Related Docs
- docs/architecture/agent_orchestration_contract.md
- agent-orchestration/README.md

## Related Stored Artifacts
None.

## Related Code Areas
- docs/architecture/agent_orchestration_contract.md
- tools/retrieval_event_parity_check.py
- agent-orchestration/contract.yaml
- agent-orchestration/workflows/implement-ticket.yaml
- agent-orchestration/roles
- agent-orchestration/terminal-statuses.yaml
- agent-orchestration/rendered/claude-adapter.yaml
- tests/agent_orchestration_claude_adapter/test_phase_order_conformance.py
- tests/agent_orchestration_claude_adapter/test_terminal_status_conformance.py
- tools/agent_orchestration_claude_adapter/generator.py
- tools/agent_orchestration_claude_adapter/terminal_status_extractor.py
- tools/agent_orchestration_claude_adapter/terminal_status_loader.py
- tools/agent_orchestration_claude_adapter/divergence_log.py
- .codex/config.toml
- tools/agent_orchestration_codex_adapter/generator.py
- tests/agent_orchestration_codex_adapter
- .claude/agents

## Assumptions / Open Questions
- The literal 'config.toml' framing from the source concern doesn't match reality and must be retargeted to AGENTS.md, or the ticket must explicitly document config.toml as a deliberate no-op and redirect there
- Gate-policy/artifact-requirement axes may not exist as structured/diffable fields in agent-orchestration/ yet, which may push this ticket toward a contract-schema-extension prerequisite rather than a pure test-only change — this choice should be made explicit during Plan
- A literal per-agent-file (.claude/agents/*.md) diff may not be extractable without also changing how those files declare phase/gate info — this is a scope-creep risk to flag explicitly, not silently absorb

## Implementation Notes

## Test Summary

## Files Changed

## Completion Summary
