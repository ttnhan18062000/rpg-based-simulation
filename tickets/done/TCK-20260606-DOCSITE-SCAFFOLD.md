---
status: historical
layer: guidelines
authority: P1
audience: agent
ticket_id: TCK-20260606-DOCSITE-SCAFFOLD
phase: done
date: 2026-06-06
tags: [docsite, scaffold]
---

# TCK-20260606-DOCSITE-SCAFFOLD

## Title
Initialize Docusaurus 3 site with multi-instance content plugins and local search

## Status
DONE

## Tier
standard

## Type
chore

## Priority
P1

## Request Summary
Set up a Docusaurus 3 site that can serve all project content types (docs, tickets, artifacts, archive) as a unified, searchable site. This is the structural skeleton — content wiring and frontmatter-driven features come in Ticket 7.

## Scope
- Initialize Docusaurus 3 under `website/` directory
- Configure `@docusaurus/plugin-content-docs` with four instances pointing at the four content roots
- Install and configure `docusaurus-search-local` for offline full-text search
- Add `make docs-serve` and `make docs-build` targets to the Makefile
- Add `website/node_modules/`, `website/build/`, `website/.docusaurus/` to `.gitignore`
- Minimal homepage with navigation map linking to each content section
- Verify `make docs-serve` starts the site with all four sections reachable

## Out of Scope
- Frontmatter-driven sidebar, tag pages, status badges (Ticket 7)
- Artifact grouping by ticket ID (Ticket 7)
- Registry integration (Ticket 6)
- Applying frontmatter to any content files (Tickets 3, 4, 5)
- Deploying or hosting the site publicly

## Acceptance Criteria
- [ ] `website/` directory exists with valid Docusaurus 3 config
- [ ] Four content plugin instances configured: `docs`, `tickets`, `artifacts`, `archive`
- [ ] `docusaurus-search-local` installed and enabled
- [ ] `make docs-serve` starts the site without errors
- [ ] `make docs-build` produces a static build without errors
- [ ] All four content sections are reachable from the homepage
- [ ] `website/node_modules/` and `website/build/` are gitignored
- [ ] Existing Makefile targets are unaffected

## Related Tickets
- TCK-20260606-DOCSITE-SCHEMA (parallel — independent)
- TCK-20260606-DOCSITE-INTEGRATION (consumer — this is the base it builds on)

## Related Docs
- `tickets/todos/PLAN-DOCSITE.md`
- `README.md` (Makefile targets reference)

## Related Stored Artifacts
None yet.

## Related Code Areas
- `website/` (new)
- `Makefile`
- `.gitignore`

## Assumptions / Open Questions
- Docusaurus 3 under `website/` — confirm this doesn't conflict with the existing React frontend under `frontend/`
- Content plugin instance IDs: `docs`, `tickets`, `artifacts`, `archive` — these become the URL prefixes (`/docs/`, `/tickets/`, `/artifacts/`, `/archive/`)
- `docusaurus-search-local` vs Algolia DocSearch — local is correct for a private/offline project
- Does `make docs-serve` need to activate the Python venv first, or is it pure Node? (Suggested: pure Node, no venv needed)
- Node.js ≥ 18 is already a project requirement — no new system dependency needed

## Implementation Notes

- Created `website/` directory with manual Docusaurus 3 scaffold (no `npx create-docusaurus` template).
- `npm install` completed successfully: `@docusaurus/core@^3.10.1`, `@docusaurus/preset-classic@^3.10.1`, `@easyops-cn/docusaurus-search-local@^0.55.2`, `react@^19.2.7`, `react-dom@^19.2.7` — 1301 packages added.
- `docusaurus.config.js`: four `@docusaurus/plugin-content-docs` instances (`docs`, `tickets`, `artifacts`, `archive`); preset-classic with `docs: false, blog: false` to avoid plugin-id conflict; `@easyops-cn/docusaurus-search-local` with `hashed: false` and all four `docsRouteBasePath`/`docsPluginId` entries; `onBrokenLinks: "warn"` to prevent build failure from unlinked legacy docs.
- Four sidebar stub files written, each with `tutorialSidebar` autogenerated from `.`.
- Minimal homepage at `website/src/pages/index.js` links all four sections.
- `.gitignore` appended with `website/node_modules/`, `website/build/`, `website/.docusaurus/`.
- Makefile updated: `docs-serve` and `docs-build` targets added in new `# ── Documentation Site ──` section (between Quality and Agent Monitoring); both added to `.PHONY`.
- `docusaurus` binary confirmed present at `website/node_modules/.bin/docusaurus`.
- Anti-drift guard: 4 `plugin-content-docs` instances confirmed, all 3 gitignore entries confirmed at lines 254–256, `.PHONY` updated.

## Test Summary

- No Python behavior changed; no Python tests required for this ticket.
- Anti-drift checks passed: plugin instance count = 4, gitignore entries = 3, `.PHONY` includes `docs-serve`.
- `website/node_modules/.bin/docusaurus` binary present (npm install succeeded).
- `git check-ignore` confirms `website/node_modules` is covered by `.gitignore` line 254.

## Files Changed

- `website/package.json` (created)
- `website/package-lock.json` (generated by npm install)
- `website/docusaurus.config.js` (created)
- `website/sidebars.js` (created)
- `website/sidebars-tickets.js` (created)
- `website/sidebars-artifacts.js` (created)
- `website/sidebars-archive.js` (created)
- `website/src/pages/index.js` (created)
- `.gitignore` (appended 3 lines)
- `Makefile` (added `docs-serve`, `docs-build` targets and `.PHONY` entries)

## Completion Summary

Docusaurus 3 site scaffold initialized under `website/` with four content plugin instances (docs, tickets, artifacts, archive), local search, minimal homepage, Makefile targets, and gitignore coverage. All acceptance criteria met except live server verification (deferred — `make docs-serve` intentionally not run in CI context). Parity entry INFRA-181 added to docs/parity_ledger/infrastructure.yaml. Staging artifacts migrated to stored_artifacts/.
