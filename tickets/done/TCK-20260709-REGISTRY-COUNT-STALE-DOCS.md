---
status: historical
layer: guidelines
authority: P1
audience: agent
ticket_id: TCK-20260709-REGISTRY-COUNT-STALE-DOCS
phase: done
date: 2026-07-09
tags: []
---

# TCK-20260709-REGISTRY-COUNT-STALE-DOCS

## Title
Remove stale hardcoded entry count from doc registry README docs

## Status
DONE

## Tier
hotfix

## Type
chore

## Priority
P2

## Request Summary
docs/ai/README.md:49 states '912 entries' but a fresh regeneration currently produces 1339 entries (1326 checked in) — the doc bakes in a stale, point-in-time count. The identical stale phrase also appears in docs/README.md:113, originating from the same ticket (TCK-20260606-DOCSITE-REGISTRY), so both files need the fix.

## Scope
- Remove the hardcoded '912 entries' numeric claim from docs/ai/README.md's Doc Registry Integration section (line 49)
- Apply the same fix to the identical stale phrase in docs/README.md (line 113)
- Retain the accurate description of docs/REGISTRY.yaml and the 'Regenerate: make docs-registry' guidance in both files

## Out of Scope
- Adding an automated mechanism to keep a live count in sync (no such mechanism exists and none is requested)
- Any change to tools/generate_registry.py itself

## Acceptance Criteria
- [ ] docs/ai/README.md's Doc Registry Integration section (line 49) no longer states a specific numeric point-in-time entry count
- [ ] The section still accurately describes docs/REGISTRY.yaml as a flat index of tagged docs and closed tickets generated by tools/generate_registry.py, and retains the 'Regenerate: make docs-registry' guidance
- [ ] `python3 tools/validate_frontmatter.py docs/ai/README.md` still exits 0 after the edit
- [ ] docs/README.md's equivalent stale '912 entries' phrase (line 113) receives the same fix as docs/ai/README.md, since both originate from TCK-20260606-DOCSITE-REGISTRY

## Related Tickets
- TCK-20260606-DOCSITE-REGISTRY

## Related Docs
- `docs/ai/README.md`
- `docs/README.md`
- `docs/REGISTRY.yaml`

## Related Stored Artifacts
None.

## Related Code Areas
- `tools/generate_registry.py`
- `tools/validate_frontmatter.py`
- `Makefile`
- `tickets/working_log.csv`

## Assumptions / Open Questions
- No existing test pins either numeric claim, so there is no regression test to update — but a purely prose fix that just deletes the number without deriving it from anything will not stop a future author from re-baking a fresh count later

## Implementation Notes
Removed the hardcoded stale entry-count numeral from both sentences without replacing it with any new derived-count mechanism:
- `docs/ai/README.md` line 49: `The registry is a flat index (912 entries) of all tagged docs...` → `The registry is a flat index of all tagged docs...`
- `docs/README.md` line 113: `...a flat machine-readable index of all tagged docs and closed tickets (912 entries after the initial pass).` → `...a flat machine-readable index of all tagged docs and closed tickets.`

Both sentences still describe `docs/REGISTRY.yaml`'s purpose, that it's generated by `tools/generate_registry.py`, and retain the `make docs-registry` regeneration guidance immediately following. No other content in either section was touched.

## Test Summary
`python3 tools/validate_frontmatter.py docs/ai/README.md` → exit 0.
`python3 tools/validate_frontmatter.py docs/README.md` → exit 0.

## Files Changed
- `docs/ai/README.md`
- `docs/README.md`

## Completion Summary
Stale '912 entries' numeric claim removed from both README files per scope; no live-count mechanism introduced (explicitly out of scope). Docs remain accurate and validate cleanly.
