---
status: historical
layer: testing
authority: P1
audience: agent
ticket_id: TCK-20260817-TESTS-TOOLS-LANE-STALE-REFERENCE-SWEEP
phase: done
date: 2026-08-17
tags: [testing, bug]
---

# TCK-20260817-TESTS-TOOLS-LANE-STALE-REFERENCE-SWEEP

## Title
Fix the `tests/tools` CI-lane's 23 pre-existing failures + 7 errors (stale references, stale
frozen-hash guards, one intentional-but-untested config change) and mark 2 real-gateway corpus
tests `slow` so they stop fighting the fast lane's 60s budget

## Status
DONE

## Tier
standard

## Type
repair

## Priority
P2

## Request Summary
While closing out the Knowledge Gateway MCP budget/dedup ticket, a full local run of the
`tests/tools` CI lane (matching `.github/workflows/test.yml`'s `api-tools` job,
`-m "not slow"`) surfaced 23 failed + 7 errors of 2272 collected. All were traced and confirmed
pre-existing, unrelated to that ticket's own diff. The user then asked to actually fix these
("I think there still failed tests, also update CI CD for tests if need") rather than leave them
disclosed-but-broken. This ticket does that.

## Scope
Fix all 23 failures + 7 errors in the `tests/tools` CI lane, and update `.github/workflows/test.yml`
and/or `pyproject.toml` markers if a real CI/CD gap is found. Confirmed root causes (8 groups):

1. **2 KGMCP real-gateway "zero mutation" tests** (`test_kgmcp_phase2_baseline_recomparison.py`,
   `test_kgmcp_phase3_pilot_acceptance_measurement.py`) — genuinely take 49s-91s (measured directly,
   `--resource-budget off`), right at/over `tests/conftest.py`'s default 60s "medium" budget. Not a
   code bug — a CI/CD gap: these belong in the `slow` job (`--resource-budget large`, 600s), not the
   fast `api-tools` lane. Fix: mark both `@pytest.mark.slow`.
2. **`docs/mechanics/content_usage_matrix.md` has no frontmatter block** — breaks
   `test_generate_registry.py::TestRealDocsTree` (2 tests) and
   `test_validate_frontmatter.py::TestPreviouslyFrontmatterMissingDocs` (1 test), and is the same
   error `make docs-registry` has surfaced as non-blocking throughout the KGMCP session. Fix: add a
   frontmatter block to the generated report (check whether it's static or regenerated by a script
   first — do not hand-edit a script-generated file without updating the generator).
3. **3 old "decision-document-only" tickets' frozen-content-hash guards are stale**
   (`test_context_kind_priority_decision.py`, `test_exact_lookup_convention_decision.py`,
   `test_stored_artifact_kind_decision.py` — `TCK-20260802-CONTEXT-KIND-PRIORITY`,
   `TCK-20260802-EXACT-LOOKUP-CONVENTION`, `TCK-20260802-STORED-ARTIFACT-KIND`). Each pinned a
   SHA-256 snapshot of `tools/parity_index.py` (2 of them) or `tools/generate_registry.py` (1 of
   them) "as of that ticket's own Implementation start" to prove that specific historical ticket's
   own diff was clean. Both files have legitimately changed many times since via unrelated later
   work — `git log` confirms neither of this session's 2 tickets touched either file. Same
   established precedent already applied 3x this session to KGMCP's own sibling tickets: narrow the
   stale entries out of each dict, keep the rest, with a disclosure comment. `test_exact_lookup_convention_decision.py`
   also has a 2nd, unrelated failure (see #6).
4. **`test_gate_a_readpath_review.py`'s `_CORPUS_PATH`/`_RESULTS_PATH` point to
   `staging_artifacts/TCK-20260731-PARITY-READPATH-GATE/`**, but that ticket closed and its
   `investigation.md`/`plan.md`/`test_plan.md` moved to `stored_artifacts/` per the normal Finalize
   lifecycle — 2 direct failures + 5 cascading errors (all downstream of the same `_load_corpus()`
   call). **Real complication found, not just a stale path**: `gate_a_corpus.json` and
   `gate_a_results.json` were never actually committed to the repo at all —
   `git log --all --diff-filter=A` finds zero history for either file, in neither
   `staging_artifacts/` nor `stored_artifacts/`. The decision doc
   (`docs/ai/parity_readpath_gate_a_decision.md`) describes this corpus as "frozen before any
   result was computed" and cites specific real findings from it (e.g. `WORLD-076`'s `divergent`
   status). The GO decision is already made and narrated in the decision doc; the raw JSON evidence
   behind it appears to have never been checked in. Needs Investigate to confirm there is no
   copy anywhere (e.g. an old commit, a different branch) before deciding whether to: reconstruct
   the corpus from the decision doc's own citations (risky — could look like retroactively
   "re-freezing" already-computed results, which the doc's own methodology explicitly guards
   against), or honestly disclose the data as unrecoverable and adjust the corpus-file-dependent
   tests to skip with a clear, dated explanation rather than silently pass or stay broken.
5. **`test_epic_staleness_check.py`'s 2 failures use a real, live ticket folder as their fixture**:
   `REAL_OBSISO_DIR = tickets/todos/obs-isolation` and the epic `TCK-20260702-OBSISO-EPIC` no longer
   exist at those paths — the whole `obs-isolation` folder completed and moved to
   `tickets/done/obs-isolation/` per the standard "move entire folder when all children are done"
   convention. Fix: point the fixture at a real folder/epic that is still genuinely in a
   "never-started" state today (re-derive, don't guess), or restructure the test to use a synthetic
   fixture instead of a live, mutable repo path — Investigate should determine which the test's own
   authoring intent supports.
6. **`test_exact_lookup_convention_decision.py::test_epic_ticket_decision_9_marked_resolved`** reads
   `_EPIC_TICKET_PATH = tickets/inprogress/TCK-20260728-CONTEXT-EFFICIENT-RETRIEVAL-EPIC.md`, but
   that epic now lives in `tickets/backlogs/TCK-20260728-CONTEXT-EFFICIENT-RETRIEVAL-EPIC.md` (a
   3rd distinct ticket-location type, not `done/` or `todos/`). Fix: update the path constant.
7. **`test_search_mcp.py::TestMcpJson::test_command_is_python3`** asserts `.mcp.json`'s
   `knowledge-search` server command is `python3`, but it is intentionally `bash` (running
   `tools/start_search_mcp.sh`) — `tickets/working_log.csv`'s own `TCK-20260624-FIX-TOOLS-SERVER`
   row documents this as a deliberate "`.mcp.json` python3 command portability" fix, already
   shipped. The test itself is the stale artifact. Fix: update the assertion to the real, current,
   intentional contract (`command == "bash"`, args point at the wrapper script, and the wrapper
   script itself invokes python3 — verify that chain, don't just flip the literal).
8. **`test_entity_event_ledger.py::test_entity_ledger_evidence_citations_are_real_files`** — `ENTITY-003`
   cites evidence path `event_extractor.py` (bare filename), but the real file is
   `src/observability/event_extractor.py`. The file exists; only the citation's path is malformed
   (missing its real relative-path prefix). Fix: correct the citation in
   `docs/event_ledger/entity.yaml` (or wherever `ENTITY-003` is authored) to the real path.

## Out of Scope
- Any change to Knowledge Gateway MCP code, tests, or docs (already closed and committed this
  session under separate tickets).
- Any `src/` engine/simulation behavior change.
- Fixing any OTHER pre-existing failure outside `tests/tools` (e.g. other CI lanes) unless directly
  entangled with one of the 8 groups above.
- Retroactively reconstructing `gate_a_corpus.json`/`gate_a_results.json` if Investigate confirms no
  real copy exists anywhere — honest disclosure + a clean skip is preferred over a fabricated
  after-the-fact "frozen" corpus, per this repo's Gate Integrity rule.

## Acceptance Criteria
- [ ] `tests/tools -m "not slow"` (the real `api-tools` CI-lane command) passes with 0 failures,
      0 errors, after the 2 real-gateway tests are correctly marked `slow` and excluded from that
      lane (matching CI's own `-m "not slow"` filter). **Partially met — see Implementation Notes.**
      All 9 of this ticket's own diagnosed groups are individually clean (verified via each group's
      own scoped command). A full-lane re-run surfaced 6 additional failures beyond the original 9
      groups' scope. 3 were the exact same "stale path after archival" pattern this ticket already
      fixes elsewhere (`test_parity_index_baseline.py`'s `DECISION_DOC` pointed at
      `docs/plans/agent_infrastructure/...v1_decisions_phase0.md`, which now lives at
      `docs/plans/archive/agent_infrastructure/...`) plus one adjacent stale hardcoded historical
      count (`missing_test_path_count`, pinned at `1347`, now genuinely `1343` as the real parity
      ledger gained more `test_path` entries over time) — **folded in and fixed** as a disclosed,
      same-pattern scope extension (Group 10). The remaining 3 are a genuinely different character
      and are **left unfixed, disclosed here** for a follow-up ticket, not this one:
      - `test_agent_monitoring_legacy_reader.py::test_recent_runs_records_classify_as_current_and_agree_with_validate`
        — a real, historical monitoring-data-quality gap: the `EPIC-TCK-20260816-KNOWLEDGE-GATEWAY-MCP-PHASE4-EPIC`
        run record in `agent-monitoring/runs.jsonl` has `final_status` set but no `end_ts`, which the
        legacy-schema classifier flags. Fixing this would mean editing an already-committed
        append-only monitoring record from a different, already-closed ticket — needs its own
        investigation into whether to backfill via a correction record or accept as a known gap.
      - `test_agent_ops_dashboard_frontend_api_surface.py::test_no_source_file_imports_full_echarts_bundle`
        — a real frontend bundle-size architecture violation
        (`dashboard-frontend/src/views/ProgressTimelineView.tsx` imports the full `echarts` bundle
        instead of `echarts/core` + tree-shaken submodules). Completely unrelated to this ticket's
        scope (frontend code change, not a stale test reference); may be a real regression worth its
        own ticket.
      - `test_kgmcp_phase3_pilot_acceptance_measurement.py::test_stale_rejection_and_unrelated_change_live_real_corpus_round_trip`
        — investigated directly: the test's own error message suggested Q1's real cited path
        (`docs/simulation/domains/domain_ownership_map.md`) might have drifted, but a live check
        confirmed that path IS still a real, current dependency (`assemble_packet()`'s own
        `evidence_dependencies` includes it). The real cause is more likely test-isolation/cache-state
        pollution: this test assumes `budget_tokens=3333` is "a brand-new, previously-untouched Level
        2 identity" in the shared local `retrieval_cache.db`, an assumption that degrades the more
        times this test (or any test touching the same identity) runs locally across a long session
        like this one. Needs its own investigation into proper cache isolation for this test, not a
        quick path swap — out of this ticket's scope.
      All 3 confirmed via `git stash` + re-run on a clean tree to reproduce identically without any
      of this ticket's changes — genuinely pre-existing, not introduced by this ticket's diff.
- [x] The 2 re-marked tests still pass under `-m "slow"` with a large resource budget, and
      `.github/workflows/test.yml`'s `slow` job scope covers `tests/tools` (verify it already does,
      or add it) so they're not silently dropped from CI coverage entirely.
- [x] Every fix is a genuine correction to a stale reference, stale guard, or CI/CD gap — never a
      loosened assertion to force a pass. Each fix's real root cause is documented.
- [x] The `gate_a_corpus.json`/`gate_a_results.json` provenance question is resolved with an
      explicit, disclosed answer (found and restored, or confirmed unrecoverable and the dependent
      tests adjusted accordingly) — not silently skipped without explanation.
- [x] `docs/mechanics/content_usage_matrix.md`'s frontmatter gap is fixed at its real source (the
      generator, if generated; the file directly, if static) — resolving this also clears the
      long-standing `make docs-registry`/`make knowledge-index-update` non-blocking error this
      session repeatedly disclosed as pre-existing.

## Related Tickets
- `TCK-20260731-PARITY-READPATH-GATE`, `TCK-20260731-PARITY-INDEX-BASELINE` (gate_a / parity index
  provenance)
- `TCK-20260802-CONTEXT-KIND-PRIORITY`, `TCK-20260802-EXACT-LOOKUP-CONVENTION`,
  `TCK-20260802-STORED-ARTIFACT-KIND` (stale frozen-hash guards)
- `TCK-20260702-OBSISO-EPIC` (epic-staleness fixture)
- `TCK-20260728-CONTEXT-EFFICIENT-RETRIEVAL-EPIC` (epic ticket path)
- `TCK-20260624-FIX-TOOLS-SERVER` (`.mcp.json` python3→bash portability change)
- `TCK-20260808-ENTITY-EVENT-LEDGER` (entity ledger citations)
- `TCK-20260816-KGMCP-BUDGET-TOLERANCE-DEDUP-COVERAGE-CLOSURE` (the ticket whose regression check
  surfaced this whole sweep)

## Related Docs
- `docs/ai/parity_readpath_gate_a_decision.md`
- `docs/event_ledger/entity.yaml`
- `docs/mechanics/content_usage_matrix.md`
- `.github/workflows/test.yml`

## Related Stored Artifacts
- `stored_artifacts/TCK-20260731-PARITY-READPATH-GATE/`
- `stored_artifacts/TCK-20260731-PARITY-INDEX-BASELINE/` (if present — verify)

## Related Code Areas
- `tests/tools/test_kgmcp_phase2_baseline_recomparison.py`,
  `tests/tools/test_kgmcp_phase3_pilot_acceptance_measurement.py`
- `tests/tools/test_generate_registry.py`, `tests/tools/test_validate_frontmatter.py`
- `tests/tools/test_context_kind_priority_decision.py`,
  `tests/tools/test_exact_lookup_convention_decision.py`,
  `tests/tools/test_stored_artifact_kind_decision.py`
- `tests/tools/test_gate_a_readpath_review.py`
- `tests/tools/test_epic_staleness_check.py`
- `tests/tools/test_search_mcp.py`
- `tests/tools/test_entity_event_ledger.py`
- `.github/workflows/test.yml`, `pyproject.toml` (markers)

## Assumptions / Open Questions
- Whether `gate_a_corpus.json`/`gate_a_results.json` exist anywhere recoverable (another branch, a
  local uncommitted copy elsewhere on disk, etc.) is the single biggest open question — Investigate
  must search exhaustively before concluding they're unrecoverable.
- Whether `docs/mechanics/content_usage_matrix.md` is generated by a script (in which case the fix
  belongs in the generator) or hand-maintained (in which case a direct frontmatter addition is
  correct) is unconfirmed — Investigate must check for a generating tool/Makefile target.
- Whether a still-genuinely-never-started real epic/folder exists to replace
  `test_epic_staleness_check.py`'s stale `obs-isolation` fixture, or whether the test should move to
  a synthetic fixture instead, needs a real look at current `tickets/todos/` state.

## Implementation Notes
All 9 plan.md steps implemented as specified, plus 4 additional real stale-reference fixes
discovered mid-Implement that the plan's/investigation's own verification method had masked (see
"Deviations" note below and `staging_artifacts/.../plan.md`'s own Deviations section).

- **Step 1** (`test_context_kind_priority_decision.py`, `test_exact_lookup_convention_decision.py`,
  `test_stored_artifact_kind_decision.py`): removed the one confirmed-stale
  `tools/parity_index.py` / `tools/generate_registry.py` entry per file, each with a disclosure
  comment citing this ticket, matching the KGMCP-sibling-ticket precedent.
- **Step 1 (extended, disclosed deviation)**: while verifying Step 1, discovered
  `test_no_code_changes_to_named_tools_modules` in all 3 files asserts inside a `for` loop over
  `dict.items()` — the loop raises `AssertionError` on the *first* mismatched entry and never
  evaluates the rest, so investigation.md's "confirmed via direct pytest run" method could only
  ever see the first stale entry per file. Re-running each file individually after Step 1's fix
  surfaced 2 more real stale entries the plan never named: `tools/generate_registry.py`'s hash is
  also stale in `test_context_kind_priority_decision.py` and `test_exact_lookup_convention_decision.py`
  (it was 2nd/4th in dict order, masked by `parity_index.py`'s earlier failure), and
  `tools/validate_frontmatter.py`'s hash is also stale in `test_stored_artifact_kind_decision.py`
  (masked by `generate_registry.py`'s earlier failure in that file). Confirmed via `git log` that
  none of the 3 `TCK-20260802-*` decision tickets touched either file, and both files' real
  last-touch commit (`29d78798`, 2026-08-14) postdates all 3 tickets (2026-08-02) — same
  legitimately-changed-via-unrelated-later-work rationale as the originally-named entries. Removed
  all 3 additional stale entries with matching disclosure comments.
- **Step 2**: updated `_EPIC_TICKET_PATH` in `test_exact_lookup_convention_decision.py` from
  `tickets/inprogress/...` to `tickets/backlogs/...`; confirmed the file exists at the new path.
- **Step 3**: fixed `ENTITY-003`'s 2nd bare `event_extractor.py-only` mention in
  `docs/event_ledger/entity.yaml` to carry the real `src/observability/` prefix.
- **Step 3 (extended, disclosed deviation)**: `test_entity_ledger_evidence_citations_are_real_files`
  has the same loop-stops-at-first-failure shape as Step 1's guard. Fixing `ENTITY-003` alone
  surfaced `ENTITY-007` (same bare `event_extractor.py-only` pattern, same fix), then `ENTITY-014`
  (bare `evolution.py` mention — corrected to `src/engine/evolution.py`, confirmed as the real
  goblin-evolution gear-grant producer by reading the file), then `ENTITY-015` (a 3-file citation
  malformed as a single concatenated path `src/engine/tactical.py/executor.py/worker_logic.py` —
  corrected to 3 properly comma-separated real paths, all confirmed to exist under `src/engine/`).
  All 4 entity.yaml fixes are the same root-cause class (malformed bare/glued filename citations)
  the plan's Step 3 already described for `ENTITY-003`, just more instances of it than the plan's
  single-loop-run verification could see.
- **Step 4**: prepended a `status: active` / `layer: mechanics` / `authority: P1` /
  `audience: developer` frontmatter block to `generate_matrix_report()`'s output in
  `src/content/matrix.py`. Re-ran `test_generate_and_save_report` to regenerate
  `docs/mechanics/content_usage_matrix.md` with the new frontmatter, then re-ran
  `python3 tools/validate_frontmatter.py docs/mechanics/content_usage_matrix.md` — clean.
- **Step 4 (extended, disclosed deviation)**: the regenerated `content_usage_matrix.md` now has
  frontmatter and is picked up by `generate_registry.py`, which put `docs/REGISTRY.yaml` out of
  sync with a fresh regeneration (`TestRealDocsTree::test_check_flag_detects_no_drift_against_real_registry`
  failed: `+ docs/mechanics/content_usage_matrix.md` added). Regenerated
  `docs/REGISTRY.yaml` via `python3 tools/generate_registry.py --output docs/REGISTRY.yaml` per
  CLAUDE.md's own "regenerated unconditionally... stage the regenerated file" convention — not a
  plan.md-named step, but a direct, necessary consequence of Step 4 and squarely inside CLAUDE.md's
  existing ticket-close convention, not scope creep.
- **Step 5**: added `_CORPUS_AVAILABLE`/`_SKIP_REASON` to `test_gate_a_readpath_review.py`; applied
  `@pytest.mark.skipif` to `TestCorpusIntegrity` and to
  `TestAdjudication::test_gate_a_no_phase2_synthetic_fixture_relabeled_as_ticket_derived_corpus`;
  added `pytest.skip()` guard as the first line of the `gate_a_full_run` fixture. Verified exactly
  6 passed, 10 skipped, 0 failed, 0 errors — matches the plan's predicted split exactly.
- **Step 6**: replaced `test_does_not_flag_never_started_real_obsiso_epic` and
  `test_advisory_only_no_file_mutation` in `test_epic_staleness_check.py` with synthetic
  `tmp_path`-based equivalents (`test_does_not_flag_never_started_synthetic_folder_epic`,
  `test_advisory_only_no_file_mutation_synthetic`), factored the shared folder-construction logic
  into `_write_synthetic_never_started_folder_epic()`. Removed the now-unused `REAL_INPROGRESS_DIR`
  / `REAL_TODOS_DIR` / `REAL_WORKING_LOG` / `REAL_RUNS_JSONL` / `REAL_OBSISO_DIR` constants (re-grepped
  first, confirmed no other test in the file referenced them) and updated the module docstring to
  state the never-started case is now proven synthetically. Full file: 14 tests, all pass (not the
  plan's estimated "~17" — the plan's own count was explicitly a hedge, not a hard prediction; no
  test was silently dropped, confirmed by re-reading the full file before and after).
- **Step 7**: rewrote `test_command_is_python3` (`== "bash"`) and `test_args_point_to_search_mcp`
  (`== "tools/start_search_mcp.sh"`) in `test_search_mcp.py`; added
  `test_start_search_mcp_wrapper_invokes_python3` proving the full `.mcp.json` → wrapper →
  interpreter chain end-to-end via a path derived from `entry["args"][0]`, not a hardcoded string.
- **Step 8**: added `import pytest` + `@pytest.mark.slow` to
  `test_kgmcp_phase2_baseline_recomparison.py`'s `test_zero_mutation_of_agent_monitoring_and_manifest_across_full_run`;
  stacked `@pytest.mark.slow` alongside the existing `@pytest.mark.extra_slow` on the same-named
  function in `test_kgmcp_phase3_pilot_acceptance_measurement.py` (did not remove `extra_slow`).
  Verified exactly 46 tests run under `-m "not slow"` (16+30, confirming only the 2 intended
  functions moved) and both pass under `-m "slow" --resource-budget large` (93.57s combined,
  consistent with the ~41s/~61s measured in Investigate). No `.github/workflows/test.yml` change
  made — confirmed the `slow` job's `pytest tests/ -m "slow or extra_slow" ...` already covers
  `tests/tools` recursively.
- **Step 9**: full-lane verification — see Test Summary (filled at Test/Finalize per this
  session's convention).
- **Group 10 (disclosed scope extension, orchestrator-applied)**: full-lane re-run surfaced 6
  failures beyond the original 9 groups. 3 matched this ticket's own established pattern exactly
  (stale path to an archived doc) and 1 adjacent stale hardcoded count — folded in and fixed:
  `tests/tools/test_parity_index_baseline.py`'s `DECISION_DOC` constant updated
  (`docs/plans/agent_infrastructure/...` → `docs/plans/archive/agent_infrastructure/...`, disclosure
  comment added), and `missing_test_path_count`'s hardcoded historical pin updated from `1347` to
  the real current `1343` (disclosure comment explaining this count naturally drifts as ledger
  entries gain `test_path` over time — the substantive check is the self-consistency assertion
  above it, unchanged). The remaining 3 failures are a genuinely different character (a real
  monitoring-data gap, a real frontend bundle-size violation, and likely test-cache-isolation
  pollution in a KGMCP live-corpus test) and are disclosed in Acceptance Criteria as out of scope,
  not fixed here.

## Test Summary
Two full, clean `pytest tests/tools -m "not slow"` lane runs with no concurrent session activity
(the first run in the same window showed a 4th, transient failure —
`test_agent_monitoring_manifest.py::test_manifest_cli_reproducible_byte_identical_across_two_runs` —
traced to this session's own concurrent Edit/Bash tool calls touching `agent-monitoring/tools.jsonl`
mid-run via the harness's own auto-hooks; confirmed passing in isolation, not a real bug, not
reproduced on the final clean run): **3 failed, 2256 passed, 11 skipped, 30 deselected, 1 xfailed**.
All 3 remaining failures are explicitly disclosed in Acceptance Criteria as out of this ticket's
scope, confirmed pre-existing via `git stash` bisection.

Dedicated Test phase (test-scoper) independently re-verified: combined 12-changed-file scoped run —
268 passed, 11 skipped, 1 failed (the same disclosed pre-existing KGMCP cache-pollution failure,
correctly still present since this ticket doesn't touch that mechanism), 2 deselected (the 2
slow-marked tests, correctly excluded from `-m "not slow"`). Confirmed both subtleties are
dynamically correct, not stale-prone: the gate_a skip guard re-evaluates `Path.exists()` on every
fresh pytest import (auto-reenables if the corpus is ever restored, doesn't silently skip forever);
the `slow` marker only excludes from an explicit `-m "not slow"` filter, not from plain/default
invocations (both marked tests still run and pass with no `-m` flag). No coverage gap found for any
of the 10 fix groups.

Architecture-Verify independently re-ran every group's own scoped test file, re-hashed the 3
narrowed frozen-hash files itself, re-scanned the parity ledger from scratch to confirm `1343`, and
`git stash`-bisected the KGMCP live-round-trip failure to confirm it pre-dates this diff.

## Files Changed
- `tests/tools/test_context_kind_priority_decision.py` — narrowed `_EXPECTED_TOOLS_HASHES`
  (removed `tools/parity_index.py` + `tools/generate_registry.py` entries, disclosure comments)
- `tests/tools/test_exact_lookup_convention_decision.py` — narrowed `_EXPECTED_TOOLS_HASHES`
  (removed `tools/parity_index.py` + `tools/generate_registry.py` entries, disclosure comments);
  updated `_EPIC_TICKET_PATH` (`inprogress` → `backlogs`)
- `tests/tools/test_stored_artifact_kind_decision.py` — narrowed `_EXPECTED_TOOLS_HASHES` (removed
  `tools/generate_registry.py` + `tools/validate_frontmatter.py` entries, disclosure comments)
- `docs/event_ledger/entity.yaml` — corrected 4 malformed evidence citations (`ENTITY-003`,
  `ENTITY-007`, `ENTITY-014`, `ENTITY-015`)
- `src/content/matrix.py` — `generate_matrix_report()` now prepends a frontmatter block
- `docs/mechanics/content_usage_matrix.md` — regenerated (build artifact; frontmatter now present)
- `docs/REGISTRY.yaml` — regenerated to include the now-frontmatter-carrying
  `content_usage_matrix.md`
- `tests/tools/test_gate_a_readpath_review.py` — added `_CORPUS_AVAILABLE`/`_SKIP_REASON`; applied
  `@pytest.mark.skipif`/`pytest.skip()` guards to the 10 corpus-dependent tests
- `tests/tools/test_epic_staleness_check.py` — replaced the real-`obs-isolation`-backed tests with
  synthetic `tmp_path` equivalents; removed unused `REAL_*` constants; updated module docstring
- `tests/tools/test_search_mcp.py` — rewrote `test_command_is_python3` /
  `test_args_point_to_search_mcp`; added `test_start_search_mcp_wrapper_invokes_python3`
- `tests/tools/test_kgmcp_phase2_baseline_recomparison.py` — added `import pytest` +
  `@pytest.mark.slow` on the budget-busting test
- `tests/tools/test_kgmcp_phase3_pilot_acceptance_measurement.py` — stacked `@pytest.mark.slow`
  alongside the existing `@pytest.mark.extra_slow` on the budget-busting test
- `tests/tools/test_parity_index_baseline.py` (Group 10, disclosed extension) — updated
  `DECISION_DOC` path to the archived location; updated `missing_test_path_count`'s hardcoded
  historical pin from `1347` to the real current `1343`, both with disclosure comments

## Completion Summary
Fixed 10 groups of stale test references, stale frozen-hash guards, and one intentional-but-
untested config change across the `tests/tools` CI lane (the original 9 diagnosed groups plus a
disclosed same-pattern extension found via a full-lane re-run: `test_parity_index_baseline.py`'s
stale archived-doc path and a stale hardcoded historical count). Confirmed via multiple independent
verification passes (Architecture-Verify's own re-hashing/re-scanning/re-bisecting, a dedicated Test
phase's own coverage-gap check) that every fix is a genuine correction — never a loosened assertion,
never fabricated data, never gate-gaming.

AC1 ("0 failures, 0 errors") is **partially met, honestly**: `pytest tests/tools -m "not slow"` now
runs clean at **2256 passed, 11 skipped (Gate A corpus, dated disclosure), 3 failed**. All 3 residual
failures were found during this ticket's own full-lane verification, confirmed via `git stash`
bisection to be genuinely pre-existing and unrelated to this diff, and are a different character
from this ticket's own "stale reference" scope (a real historical monitoring-data gap, a real
frontend bundle-size violation, and likely test-cache-isolation pollution in a KGMCP live-corpus
test) — not something this ticket's own Investigate/Plan authorized fixing. Rather than leave these
as bare prose disclosure, 3 concrete follow-up tickets were filed in `tickets/todos/`:
- `TCK-20260817-AGENT-MONITORING-LEGACY-CLASSIFIER-MISSING-END-TS`
- `TCK-20260817-DASHBOARD-PROGRESS-TIMELINE-ECHARTS-BUNDLE-VIOLATION`
- `TCK-20260817-KGMCP-P3-LIVE-CORPUS-TEST-CACHE-ISOLATION`

No known material gap is left unstated: every deviation (the 4 mid-Implement extensions, the
Group 10 post-Implement extension, the transient false-alarm, and the 3 genuinely out-of-scope
failures) is disclosed in this ticket's own Implementation Notes/Acceptance Criteria, not hidden.
