# TCK-20260407-PH0-FIX: Phase 0 Corrective Alignment & Misdirection Fix

## Status: INPROGRESS

## Tier
standard

## Type
chore

## Priority
P1

## Request Summary:
Address critical architectural misalignments and "fake" narrative logic identified in `phase_0_update_implementation.md`. This includes fixing mutation discipline violations, social pipeline bugs, divergent registry wiring, and upgrading the EntityInspector from a debug panel to a curated spectator lens.

## Scope:
- **EntityInspector**: Implement the "curated lens" answering Who/What/Risk.
- **Salience**: Integrate `MemorySalienceService` into `MindAspect` and `ActionSystem`.
- **Mutation Discipline**: Refactor `AIBrain` and `ActionSystem` to use typed updates for all narrative changes.
- **Social Registry**: Fix `update_bond` return values and unify `WorldLoop` wiring.

## Out of Scope:
- Adding new Phase 2 features (Social Memory, Group Dynamics) beyond what is required to fix Phase 0 foundations.

## Acceptance Criteria:
- [ ] `EntityInspector` displays "Likely Next Choices" and "Ongoing Arc".
- [ ] `MindAspect.prune_memories` uses weighted salience logic.
- [ ] `AIBrain` is genuinely stateless (no `.prune_memories()` calls).
- [ ] `ActionSystem` uses `PerceptionUpdate` for all memory additions.
- [ ] `SocialRegistry.update_bond` returns `(old_bond, new_bond)` or equivalent.
- [ ] `WorldLoop` and `WorldState` share exactly one `SocialRegistry`.

## Related Tickets:
- `TCK-20260407-PHASE1-PERSONALITY` (Phase 1 which this stabilizes)

## Related Docs:
- `docs/architecture/macro_interest_constraints.md`
- `phase_0_update_implementation.md`

## Current Status:
- PLANNING: Researching target files and identifying specific fixes.
