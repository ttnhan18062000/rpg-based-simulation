# Engine Domain Logic

Core authoritative gameplay mechanics.

Rules:
- Logic must be deterministic.
- Logic must use AuthoritativeState and return updates.
- Separate from orchestration (pipeline) and lifecycle (systems).
